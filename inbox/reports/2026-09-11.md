# Разбор inbox — 2026-09-11

## Утренний прогон (автоматический)

Ветка: `claude/inbox-2026-09-11` · в очереди 9 · взято 8 · сделано 4 ·
на ревью 4 · не бралась 1

**Ничего из этого не собиралось и с макетом не сверялось: Xcode в облаке нет.**
`swiftc` тоже нет, поэтому `./tools/run-pure-checks.sh` не запускался —
13 новых проверок `PhoneFormat` написаны, но не прогнаны. `check-project.py`
не запускался: ему нужен `plutil`, команда macOS. Из проверок работала одна,
и она прошла: `python3 tools/check-typography.py` → «Проверено строк
интерфейса: 618. Ошибок: 0, предупреждений: 36» (столько же предупреждений,
сколько было до правок, при 47 новых строках).

### Сделано

- **Рекомендации по расходникам после прослушивания** — `CarDiagnosis.swift`,
  `IssuesScreen.swift`, `EngineCheck.swift`. Под каждой версией неисправности
  строка «Купить: … Проверить: …», по тем же ключам, что и названия деталей.
  Незнакомая деталь остаётся без совета. Совет уезжает в историю вместе
  с находкой. Показывается только при вердикте «fault» — ровно «если есть
  неисправность», как в заметке. *Сборкой и сверкой с макетом не проверено.*

- **Телефон места на карте вместо записи на СТО** — `NumberFormat.swift`
  (`PhoneFormat`), `MapController.swift`, `MapScreen.swift`, `PlacesList.swift`,
  `Place.swift`, `docs/MAP.md`. Записи в коде не сделать: своего расписания
  у сервисов нет. Телефон организации приходит из поиска Яндекса, на карточке
  это строка с самим номером, в списке — свайп слева направо, у избранного
  номер сохраняется. *Сборкой не проверено; отдельный риск — вызов SDK
  `metadataContainer.getItemOf(YMKSearchBusinessObjectMetadata.self)` написан
  по документации: самого SDK в облаке нет. Если что-то не соберётся, то это.*

- **Превью машины в форме «По названию»** — `CarFoundSheet.swift`
  (`CarCatalogPreview`), `AddCarView.swift`, `AddCarSheet.swift`. По номеру
  превью уже показывалось везде, включая форму с главной; вслепую машину
  добавляли только по названию. *Сборкой и сверкой с макетом не проверено —
  блока превью в макете нет вовсе, высоту взял по соседнему блоку фото.*

- **Ложное предупреждение в `tools/check-typography.py`** — правило про
  однобуквенный предлог ругалось на строки, где неразрывный пробел уже стоит.
  Проверено прогоном: число предупреждений на старом коде не изменилось.

### На ревью (решать тебе, а не коду)

- **Избранное на карте** — *уже сделано* веткой
  `feature/dark-main-and-map-favorites`, заметка закрыта с перечнем, где это
  лежит. Не нажато пальцем ни разу: в симуляторе тапнуть нечем.
- **«Допилить карту (чипсы и избранное)»** — и чипсы, и избранное в `main`
  есть. В заметке пять конкретных вариантов, что можно допилить (чип
  «Избранное», сброс фильтров, прокрутка чипсов, свой значок избранного
  на карте, сортировка). Скажи какой — уедет в код.
- **Бекенд (бд, vps)** — 152-ФЗ, деньги и выбор того, что вообще уезжает
  с телефона. Предложен первый шаг: вынести на VPS уже готовый
  `diagnosis_server.py` и снять зависимость разбора звука от домашней сети.
- **Заколлабиться с отцом Сани** — договорённость с человеком; в заметке
  четыре вопроса, которые надо задать, и предложение вывести часы работы
  (приходят теми же метаданными, что и телефон).
- **Комментарии к местам без накрутки** — в клиенте не делается в принципе:
  любую проверку на телефоне обходят. Разделено надвое — «моя заметка о месте»
  (поле `Place.note` уже есть, работа на один прогон) и настоящие отзывы
  после сервера.

### Не бралась

- **Прелоудеры скелетоны** — осталась `todo` на завтра. Очередь намеренно
  не берётся разом: три настоящих дифа лучше семи полуфабрикатов.

### Что посмотреть первым

Собрать ветку. Если что-то не соберётся — почти наверняка вызов SDK Яндекса
в `MapController.phone(of:)`; остальное от него не зависит. Дальше — экран
«Ошибки» с находками (карточка стала выше на строку совета) и форма
«По названию» на обоих экранах.

---

## Ручной прогон (был раньше в тот же день)


Ветка: `claude/epic-darwin-axs48i` · взято 3 · починено 2 · на ревью 1 · застряло 0

Прогон ручной, не утренний: проверка самой процедуры. Ветка та же, на которой
живёт автоматика, а не отдельная `claude/inbox-2026-09-11` — почему, сказано
в конце.

## Починено

- **`XCODE_SETUP.md` звал несуществующий проект** — `docs/XCODE_SETUP.md`.
  Теперь `open Wheelly.xcodeproj` из папки проекта; абсолютный путь к чужому
  рабочему столу убран. Заодно снята неправда в шапке («полный Xcode
  отсутствует» — он стоит, 26.6) и версия в примере.
  Проверено: `grep -n "AppMVP.xcodeproj" docs/XCODE_SETUP.md` — пусто.

- **`DEVICE_TESTING.md` упоминал снятый флаг** — `docs/DEVICE_TESTING.md`.
  Состояние описано как есть: онбординг — `hasCompletedOnboarding`
  в `UserDefaults`, наличие машины — запрос к SwiftData. Добавлено, чем
  отличается удаление машины от удаления приложения.
  Проверено: `grep -rn "hasAddedCar" AppMVP/` — остался один комментарий,
  объясняющий, что флага нет.

Оба хвоста вычеркнуты в `docs/TAILS.md`.

## На ревью

- **Адрес разбора звука захардкожен** — `AppMVP/Core/Services/CarDiagnosis.swift:24`.
  Код не трогал: `Core/Services/` вне зоны автоматики.
  **Заметка ошибалась в главном.** Разбор идёт локально: в бандле лежат
  `ClapAudioEncoder.mlmodelc` и `ClapMelFrontend.mlmodelc`, `IssuesScreen`
  сначала пробует `LocalDiagnosis` и до сервера не доходит. Сервер —
  отладочный запасной путь. Ошибка на экране тоже есть: карточка
  «Не удалось разобрать запись». То есть описанного отказа вне домашней сети
  быть не должно, и прежде чем править `baseURL`, надо убедиться, что отказ
  вообще существует — иначе чинится не то.
  Разбор целиком — в самой заметке, `inbox/bugs/`.

## Чем не проверялось

Xcode, симулятора и устройства в окружении нет — ни одна правка этого прогона
не проверена сборкой. `run-pure-checks.sh` не запускался: нет `swiftc`, и Swift
этот прогон не трогал. `check-project.py` не работает вовсе — нет `plutil`
(команда macOS). `check-typography.py` неприменим: он читает только
`AppMVP/**/*.swift`.

## Что прогон вскрыл в самой процедуре

1. **Автоматический прогон в 17:45 не оставил ничего** — ни ветки, ни коммита.
   Сессия отработала 10 минут и закончилась, но `claude/inbox-2026-09-11`
   на GitHub нет. Причина не установлена: прочитать её журнал из другой сессии
   нечем.
2. **Заметке нельзя верить на слово.** Здесь она уверенно описывала отказ,
   которого нет. Шаг «сначала воспроизвести» в навыке есть, но слаб: стоит
   написать прямо, что вывод «баг не воспроизводится / описан неверно» —
   нормальный результат разбора, а не повод искать, что бы всё-таки починить.
3. **`TAILS.md` сам расходится с кодом** и как источник задач ненадёжен:
   в нём был устаревший адрес сервера и удалённый файл `IssuesScreen 2.swift`.
4. **Ветка.** Навык велит `claude/inbox-<дата>` от `origin/main`. Этот прогон
   шёл в сессии, которой разрешено пушить только в `claude/epic-darwin-axs48i`,
   поэтому ветка одна. Для утреннего прогона правило остаётся прежним.
