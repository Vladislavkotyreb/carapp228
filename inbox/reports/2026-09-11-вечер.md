# Разбор inbox — 2026-09-11, вечер

Ветка: `claude/epic-darwin-axs48i` · взято 9 · сделано 1 · размечено 5 · на ревью 3

Прогон ручной, по просьбе: проверка того, что разбор теперь **пишет код**,
а не отдаёт план. Первые правила были туже, чем нужно, и это их первый прогон
после исправления.

## Сделано

- **Превью машины не только при онбординге** —
  `AppMVP/Features/Main/AddCarSheet.swift`.
  Форма добавления с главной показывала превью снимка и выбор фото только
  под вкладкой «По названию» и без подсказки про съёмку — тот же экран при
  онбординге показывал и то, и другое. Со второй машины человек попадал
  в форму беднее, чем с первой. Блок вынесен под обе вкладки, подсказка
  добавлена.

  Попутно закрылась мёртвая ветка кода: `CarMainView.confirmFoundCar` читает
  `newCarPhoto`, но на вкладке «По номеру» выбрать снимок было нечем —
  дойти до этой ветки с главной было невозможно.

  Проверено: `check-typography.py` — 571 строка, 0 ошибок; `--audit` — ревизия
  чистая, 50 файлов. **Сборкой и сверкой с макетом не проверено: Xcode
  в окружении нет.** Смотреть на устройстве: форма «Добавить авто» с главной,
  обе вкладки, с фото и без.

## Размечено, делается завтра

Пяти задачам проставлен `scope` — завтрашний прогон возьмёт их уже со знанием
места, а не будет искать заново. В один прогон вместе с превью не влезли.

| Задача | Где живёт |
|---|---|
| Рекомендации по расходникам после прослушивания | `Features/Issues/IssuesScreen.swift` |
| Допилить карту (чипсы и избранное) | `Features/Map/MapScreen.swift` |
| Прелоудеры-скелетоны | `Features/` |
| Комментарии к местам без накрутки | `Features/Map/` |
| Бизнесы в избранное | `Features/Map/` |

## На ревью — работа не в коде

- **Бекенд (бд, vps)** — сервер, деньги, размещение в РФ по 152-ФЗ. Чеклист
  того, что нужно от тебя, уже в `docs/BACKEND.md`.
- **Запись на СТО через приложение** — экран сделать можно, сама запись
  упирается в партнёра и его систему. Сначала договорённость, потом код.
- **Заколлабиться с отцом Сани** — договориться с человеком.

## Чем не проверялось

Xcode, симулятора и устройства в окружении нет: ни одна правка не собрана.
`run-pure-checks.sh` не запускался — нет `swiftc`, и `Core/Pure` не тронут.
`check-project.py` не работает без `plutil` (команда macOS); вместо него
`--audit`, который эту проверку пропускает.

## Что прогон показал про сам разбор

Скрипт разметки искал заметки по подстроке имени, и «избранное» попало
в «допилить карту (чипсы и **избранное**)» вместо «бизнесы в **избранное**»:
одна заметка получила раздел дважды, другая не получила `scope` вовсе.
Поймано глазами на выводе `inbox-scan.py`, исправлено. Вывод на будущее:
сверять результат разметки со списком, а не доверять совпадению подстроки.
