# Разбор inbox — 2026-09-12

Ветка: `claude/inbox-2026-09-12` · в очереди 1 · взято 1 · сделано 1 ·
на ревью 0 · застряло 0

**Не проверено сборкой: Xcode в облаке нет.** `swiftc` тоже нет, поэтому
`./tools/run-pure-checks.sh` не запускался — четыре новые проверки `MapPhase.of`
и сверка каталога состояний написаны, но не прогнаны. `check-project.py` не
запускался: ему нужен `plutil`, команда macOS. Из проверок работала одна, и она
прошла: `python3 tools/check-typography.py` → «Проверено строк интерфейса: 657.
Ошибок: 0, предупреждений: 36».

## Сделано

- **Прелоудеры скелетоны** — закрыта целиком. Скелетоны поставлены в двух
  местах, где ожидание показывалось неправдой:

  1. **Список мест на карте.** При пустой базе он писал «Рядом ничего
     не нашлось» всё время, пока поиск ещё шёл, — фаза считалась только
     по наличию мест. Теперь у `MapPhase` есть случай `listLoading`, и на это
     время рисуются строки-скелетоны с раскладкой настоящих.
  2. **Плитка «Цена авто» на главной.** Пока рыночную цену считает сеть, там
     стоял прочерк, а прочерк значит «цены нет». Теперь скелетон шириной под
     «≈ 1 200 000 ₽» — когда цена доедет, плитка не дрогнет.

  Файлы: `UIState.swift`, `UIStateCatalog.swift`, `FormControls.swift`
  (`SkeletonBlock`), `LiquidGlass.swift` (`Motion.skeleton`), `PlacesList.swift`,
  `MapScreen.swift`, `CarMainView.swift`, `tools/pure-checks/main.swift`.
  *Сборкой и сверкой с макетом не проверено.*

Скелетонов намеренно не получили: поиск машины по номеру (там уже есть тост,
а шторка с находкой ещё не открыта — скелетон рисовать не во что) и история
прослушиваний (читается синхронно, ждать нечего).

## Что важно знать про эту ветку

Первый кусок работы (список мест) был написан вчера в `claude/inbox-2026-09-11`,
но в `main` не доехал: вчерашнюю ветку свели не целиком. Здесь он перенесён
`cherry-pick` поверх свежего `main`, так что ветка самодостаточна — сливать
вчерашнюю ради него не нужно.

## Что посмотреть первым

Главная: плитка цены — экран сверен попиксельно, и плитка его часть. Дальше —
раздел «Карта» с пустой базой: список должен показывать скелетоны, пока идёт
поиск, и «Рядом ничего не нашлось» только после него.
