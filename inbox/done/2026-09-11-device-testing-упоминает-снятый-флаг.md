---
status: done
kind: task
scope: docs/DEVICE_TESTING.md
---

`docs/DEVICE_TESTING.md`, строка 97, в разделе про повтор онбординга называет
флаг `hasAddedCar` в `UserDefaults`. Флага больше нет: наличие машины
выводится из SwiftData — см. `AppMVP/Navigation/AppState.swift:9` и
`docs/DECISIONS.md`. По этой инструкции онбординг повторно не откроется.

## Готово, когда
- Документ описывает настоящий способ сбросить онбординг.
- `hasCompletedOnboarding` перепроверен: он-то существует или тоже снят?
- Строка про этот хвост вычеркнута в `docs/TAILS.md`.

## Что сделано

`docs/DEVICE_TESTING.md`, раздел «Что проверять»: состояние описано так, как
оно устроено на самом деле — онбординг флагом `hasCompletedOnboarding`
в `UserDefaults`, наличие машины запросом к SwiftData (`@Query private var cars`
в `RootView.swift`). Флага `hasAddedCar` нет, ссылка на `DECISIONS.md` добавлена.

Про `hasCompletedOnboarding` из «готово, когда»: он существует, проверено
грепом — `AppState.swift:5,11,16,20` и `RootView.swift:13,21`.

Заодно уточнено, почему для повтора с нуля нужно удалять приложение: снести
надо и флаг, и базу. Удаление одной машины внутри приложения возвращает
к экрану добавления, но не к онбордингу — этого в документе не было, а
проверяющий на это натыкается.

Строка хвоста вычеркнута в `docs/TAILS.md`.

Проверено: `grep -rn "hasAddedCar" AppMVP/` — только комментарий в
`AppState.swift`, объясняющий, что флага больше нет. Сборкой не проверялось:
Xcode в этом окружении нет.
