---
status: done
kind: task
scope: AppMVP/Features/Main/AddCarSheet.swift
priority:
source: inbox/backlog.md:26
---

Когда добавляешь машину необходимо выводить превью не только при онбординге

Раздел заметки: продуктовые фичи

Заведено автоматически из заметки Obsidian. `scope` пуст, значит разбор не тронет код: он выяснит, где это делается, и напишет план.

## Что сделано

`AppMVP/Features/Main/AddCarSheet.swift` — форма добавления машины с главной
теперь показывает превью снимка и выбор фото **под обеими вкладками**, а не
только под «По названию», и получила подсказку «Сфотографируйте машину спереди
для лучшего вида» — ровно тот же блок, что на экране онбординга
(`AddCar/AddCarView.swift`). Со второй машины человек попадал в форму без
превью и без подсказки, хотя задача та же.

Попутно закрылась мёртвая ветка: `CarMainView.confirmFoundCar` читает
`newCarPhoto`, но на вкладке «По номеру» выбрать снимок было нечем — код жил,
а дойти до него с главной было невозможно. Загрузка снимка висит на
`onChange(of: carPhotoItems)` в `CarMainChrome`, от вкладки не зависит,
поэтому проводка уже была готова.

Проверено: `python3 tools/check-typography.py` — 571 строка интерфейса,
0 ошибок; `.claude/hooks/require-journal.sh --audit` — ревизия чистая,
50 файлов. **Сборкой и сверкой с макетом не проверено: Xcode в окружении
нет.** Смотреть на устройстве: обе вкладки формы «Добавить авто» с главной,
с фото и без.
