---
status: done
kind: task
scope: docs/XCODE_SETUP.md
priority: high
---

`docs/XCODE_SETUP.md` предлагает открыть проект, которого нет: строка 25 —
`open /Users/vladislavkotyrev/Desktop/ios-app/AppMVP.xcodeproj`, а проект
называется `Wheelly.xcodeproj`. Человек, который ставит Xcode по этой
инструкции, упирается в «файл не найден» на первом же шаге.

## Готово, когда
- В документе нет упоминаний `AppMVP.xcodeproj`.
- Путь и имя совпадают с тем, что лежит в репозитории.
- Строка про этот хвост вычеркнута в `docs/TAILS.md`.

## Что сделано

`docs/XCODE_SETUP.md`:
- «Открыть проект» теперь `open Wheelly.xcodeproj` из папки проекта, со схемой
  `AppMVP` рядом. Абсолютный путь `/Users/…/Desktop/ios-app/` убран вовсе:
  он привязан к одной машине и разъезжается при первом же переезде папки.
- Шапка говорила «полный Xcode отсутствует» — неправда с тех пор, как он
  встал (26.6, `CLAUDE.md`). Документ переформулирован как инструкция для
  новой машины, а не как описание текущего состояния.
- `xcodebuild -version` в примере показывал `Xcode 16.x`, стало `26.6`.
- Строка хвоста вычеркнута в `docs/TAILS.md`.

Проверено: `grep -n "AppMVP.xcodeproj" docs/XCODE_SETUP.md` — пусто.
Сборкой не проверялось: Xcode в этом окружении нет.
