---
status: done
kind: task
scope: AppMVP/Features/Issues/IssuesScreen.swift
priority:
source: inbox/backlog.md:24
---

выводи рекомендации по расходникам (что купить, что поменять) после прослушивания если есть неисправность

Раздел заметки: продуктовые фичи

Заведено автоматически из заметки Obsidian. `scope` пуст, значит разбор не тронет код: он выяснит, где это делается, и напишет план.

## Где это живёт

`scope` проставлен разбором 2026-09-11. Задача остаётся `todo`: в один прогон
вместе с превью не влезла, следующий возьмёт её уже со знанием места.

## Что сделано

`AppMVP/Core/Pure/PartAdvice.swift` — новый чистый словарь: на каждый код
детали из пайплайна две вещи, с чего начать смотреть и что обычно меняют.
21 деталь, те же коды, что у `DiagnosisVocabulary.part`. Чистый слой:
ни SwiftUI, ни SwiftData, ни сети — только `Foundation`.

`AppMVP/Features/Issues/IssuesScreen.swift` — у `EngineIssue` появилось поле
`advice`, его получают только карточки с названной деталью: у вердикта
и у сообщений об ошибке советовать нечего. Совет показывается **в шторке
находок**, где человек решает, добавлять ли их, и не показывается на тёмной
карточке экрана: та прибита к ноде макета `46093:2421` (370×102), и третья
строка вынесла бы её за эту высоту. Заголовок в 17pt уже давал 104 вместо 102
и ловился сверкой — повторять это не стоит.

Расходники обрезаны до двух: карточка узкая, а длинный список читается как
«купите всё это», чего разбор со своей точностью 45–65 % по детали обещать
не может.

Файл прописан в `project.pbxproj` в четырёх местах, префикс ID `1A00`/`1B00`
проверен грепом на занятость.

Проверено: `.claude/hooks/require-journal.sh --audit` — ревизия чистая,
51 файл, все в проекте; `python3 tools/check-typography.py` — 651 строка,
0 ошибок. `run-pure-checks.sh` не запускался: в окружении нет `swiftc`,
так что чистый слой прогоном не накрыт. **Сборкой и сверкой с макетом
не проверено: Xcode здесь нет.** Смотреть на устройстве: шторка находок
после разбора звука — под описанием должна появиться третья строка с советом.
