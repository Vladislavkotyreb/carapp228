---
status: done
kind: task
scope: AppMVP/Features/Map/
priority:
source: inbox/backlog.md:14
---

Сделать возможность добавлять бизнесы в избранное (карта или список)

Раздел заметки: карта и списки

Заведено автоматически из заметки Obsidian. `scope` пуст, значит разбор не тронет код: он выяснит, где это делается, и напишет план.

## Что сделано

Ничего: **это уже работает** — ветка `feature/dark-main-and-map-favorites`
слита в `main` (коммит `73e6f1d`, «Тёмная главная под новую редакцию макета
и избранное на карте»). Проверил по коду:

- `AppMVP/Models/Place.swift` — `PlaceOrigin.favorite` рядом с `.mine`:
  избранный бизнес и своя точка различаются, а не сведены в один флаг.
- `AppMVP/Features/Map/MapScreen.swift` — звезда в карточке места на карте
  (`star(_:)`, `toggleFavorite(_:)`), разделы списка «Избранное» / «Мои точки»
  / «Рядом», и вычитание найденного из сохранённого, чтобы избранное не
  двоилось после нового поиска.
- `AppMVP/Features/Map/PlacesList.swift` — звезда в строке и свайп
  «В избранное» / «Убрать».
- `docs/MAP.md` — раздел «Избранное».

То есть обе подачи из заметки («карта или список») закрыты. Единственное, чего
по-прежнему нет, — проверки пальцем: звезда и свайпы не нажимались ни разу,
в симуляторе тапнуть нечем. Это записано и в `docs/MAP.md`.

Сегодня к избранному добавился телефон организации (`Place.phone`) — см.
заметку про запись на СТО.
