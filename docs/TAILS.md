# Хвосты

Мелочи, которые остаются после работы и теряются: не цель и не дефект
компонента, а «осталось» размером в один вечер. Новое — в конец, снял —
вычеркнул галочкой и удалил через месяц.

**Что сюда не идёт.** Дефект компонента — [`STATE.md`](STATE.md), Часть I.
Решение — [`DECISIONS.md`](DECISIONS.md). Механизм поломки —
[`TRAPS.md`](TRAPS.md). Что сделано — [`JOURNAL.md`](JOURNAL.md). Крупное
«дальше» — [`../CHECKLIST.md`](../CHECKLIST.md).

**Откуда берётся.** В записи журнала появился блок «Осталось» — то из него,
что является чьей-то будущей работой, переезжает сюда строкой. Свойства
сделанного («тестам недоступен симулятор») остаются в записи: это не работа,
а ограничение.

Собрано 2026-08-20 при разрезе журнала на слои.

## Открыто

- [ ] **`AppMVP/Features/Issues/IssuesScreen 2.swift` — мёртвые 500 строк.**
  478 строк общие с живым `IssuesScreen.swift` (682), в `project.pbxproj` нет
  ни строкой, то есть файл не компилируется вовсе. Добавить его туда нельзя:
  оба объявляют `struct IssuesScreen`. Это решение, а не работа — снести или
  оставить. ← 2026-08-20
- [ ] **`docs/XCODE_SETUP.md` разошёлся с кодом.** Предлагает открыть
  `AppMVP.xcodeproj`, которого нет: проект называется `Wheelly.xcodeproj`.
  ← 2026-08-20
- [ ] **`docs/DEVICE_TESTING.md` разошёлся с кодом.** В разделе «Что проверять»
  ссылается на флаг `hasAddedCar` в `UserDefaults` — он снят, наличие машины
  выводится из SwiftData. ← 2026-08-20
- [ ] **`project.yml` расходится с настоящим проектом.** Помечен справочным, но
  расхождение нигде не перечислено — а `xcodegen generate` затрёт `pbxproj`.
  Либо выписать разницу, либо удалить файл. ← 2026-08-20
- [ ] **Затухание надписей не проверено визуально** — симулятор завис на съёмке
  кадра. ← перенесено из «Технического долга»
- [ ] **`baseURL` разбора звука вынести из кода.** Сейчас
  `http://192.168.1.108:8077` захардкожен; как минимум — в настройку, как
  максимум — вместе с сервером в [`BACKEND.md`](BACKEND.md). Дефект описан в
  [`STATE.md`](STATE.md), Часть I, п. 5; здесь — как работа на вечер. ← 2026-08-20
