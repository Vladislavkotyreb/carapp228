# Журнал работы

Что сделано, зачем, как проверено, что осталось — по каждому куску работы.
Здесь **не** пересказ `git log`: историю правок смотри в коммитах, здесь то,
что из истории не восстановить.

Записи требует заслонка `.claude/hooks/require-journal.sh`: коммит, меняющий
код без записи за сегодня, не проходит.

**Что переехало отсюда 2026-08-20**, чтобы журнал не был четырьмя документами в
одном файле на 78 КБ:

| Было разделом | Стало файлом |
|---|---|
| Грабли | [`TRAPS.md`](TRAPS.md) — читается перед работой над вью |
| Принятые решения | [`DECISIONS.md`](DECISIONS.md) |
| Технический долг, Открытые вопросы к дизайнеру, Переменные в Figma | [`STATE.md`](STATE.md) — источник правды о состоянии |
| Как проверять, Ограничения окружения | [`../CLAUDE.md`](../CLAUDE.md) — нужны в начале сессии |
| Где что лежит | [`../CLAUDE.md`](../CLAUDE.md) и [`../README.md`](../README.md) |
| — | [`TAILS.md`](TAILS.md) — мелкое «осталось» |

---

## Хроника

Новое сверху, заголовок `### ГГГГ-ММ-ДД — тема`. Одна запись на кусок работы:
что изменилось и в каких файлах, зачем, **как проверено** (точная команда и
настоящий вывод — или прямо «не проверено»), что осталось. То из «осталось»,
что является чьей-то будущей работой, переезжает строкой в [`TAILS.md`](TAILS.md).

### 2026-09-12 — скелетоны: список мест и плитка цены

Утренний разбор inbox, ветка `claude/inbox-2026-09-12`. В очереди была одна
задача — «прелоудеры скелетоны». Первый её кусок был написан 2026-09-11
в ветке прогона, но в `main` не доехал (свели только часть), поэтому он
перенесён сюда `cherry-pick` и дополнен вторым куском.

**Где ожидание показывалось неправдой.**

1. Список мест на карте при пустой базе показывал «Рядом ничего не нашлось»
   **всё время, пока идёт поиск**: фаза считалась только по наличию мест.
   Экран уверенно сообщал результат, которого ещё не было.
2. Плитка «Цена авто» на главной показывала прочерк, пока рыночную цену
   считает сеть. Прочерк значит «цены нет», а не «идёт расчёт».

**Что изменилось.** `MapPhase` получил случай `listLoading` и параметр
`isSearching` (поиск младше мест: пока список не пуст, идущий поиск ничего
не меняет — подменять скелетонами то, что человек уже читает, нельзя);
`UIStateCatalog` — кадр `map-listLoading`, иначе каталог расходится
с `allCases`. Общий `SkeletonBlock` встал в `FormControls.swift`, токен
`Motion.skeleton` — в `LiquidGlass.swift`. `PlacesListSkeleton` повторяет
раскладку настоящих строк, поэтому список не прыгает, когда места доезжают.
В `CarMainView` появилось множество `pricingCars` и своя плитка `priceCard`:
флаг снимается в `defer`, потому что из цикла выходят и по ошибке,
и по отмене задачи — залипший скелетон пережил бы оба случая.

Блик двигает одно состояние, поставленное в `onAppear`, а не запись в `@State`
на каждом кадре: последнее ломает прокрутку (`TRAPS.md`). При Reduce Motion
блика нет вовсе, остаётся спокойная подложка.

Проверено: `python3 tools/check-typography.py` → «Проверено строк интерфейса:
657. Ошибок: 0, предупреждений: 36». **Сборкой не проверено: Xcode в облаке
нет.** `run-pure-checks.sh` не запускался — нет `swiftc`, и четыре новые
проверки `MapPhase.of` вместе со сверкой каталога состояний не прогнаны;
именно они поймали бы расхождение `allCases` с каталогом.

Осталось: поиск машины по номеру скелетона не получил намеренно — там уже есть
тост «Ищем машину по номеру…», а шторка с находкой ещё не открыта, и рисовать
скелетон не открытой шторки не во что.

### 2026-09-12 — дубли снимает сам сканер, отдельной команды нет

Замечание пользователя: «зачем мне писать эту команду в Routine, если это не
влияет на приложение?» Справедливо: проверка на дубли зависела от того,
вписана ли строка в промпт, который из чата не правится. Так не должно быть.

`tools/inbox-scan.py` теперь сам вызывает `inbox-dedup.py --mark` перед тем,
как отдать очередь. Любой, кто спрашивает «что делать сегодня», получает
список уже без сделанного — что бы ни было написано в инструкции Routine.
`--no-dedup` оставлен для отладки. В навыке — одна команда вместо двух.

Как проверено. `tools/inbox-dedup-test.sh` — 17 из 17: он проверяет очередь
именно через сканер, так что покрывает и встроенный вызов. На настоящем
репозитории сканер отдаёт прежнюю очередь без сообщений о дублях.

### 2026-09-12 — галочки за сделанное закрываются сами

Вопрос пользователя: сможет ли прогон закрывать чекбоксы выполненных задач
и подписывать ссылкой, где сделанное теперь лежит. Сможет.

1. **`tools/inbox-close.py`** — для каждой заметки `status: done` из `inbox/done/`
   находит исходную строку `- [ ] …` по тексту (не по номеру из `source:` —
   номера уезжают при любой правке) и переписывает в
   `- [x] … — [[done/имя-заметки]]`. Вики-ссылка ведёт на заметку с «Что
   сделано» и веткой; в Obsidian на телефоне открывается тапом. Ищет сначала
   в записанном источнике, потом во всех заметках корня: `backlog.md`, из
   которого заводились первые девять задач, уже удалён, а строки живут
   в `Таски.md`. `blocked`, `needs-review`, `duplicate` не закрывает.
2. **`tools/inbox-close-test.sh`** — 11 проверок на временной папке: строка
   с лишними пробелами находится по норме, чужая `[x]` не трогается,
   `needs-review` не закрывается, исчезнувший источник — запасной поиск,
   несуществующий текст — честное «не найдено», повтор не меняет ни байта.
3. Шаг вписан в навык после переноса в `done/`. В README — единственное
   исключение из правила «в заметки человека прогон не пишет»: галочка
   и хвост строки, больше ничего.

Про владение — честно. `Таски.md` остаётся файлом пользователя; мак несёт
его обратно в хранилище с `--update`, то есть правка с телефона, сделанная
позже прогона, победит и галочка отложится. Шаг идемпотентный: каждое утро
доставляет недостающие, и через день всё сходится.

Как проверено. Тест — 11 из 11, дважды: до и после запасного поиска.
На настоящем репозитории `--dry-run` показал 6 закрытий (две задачи по два
раза — у них по две записи в `done/` после утреннего сведения) и два «не
найдено» — рукописные заметки про документы, они из галочек не рождались.
Настоящий запуск закрыл четыре строки в `inbox/Таски.md`; повторный —
«уже стояло». Файл не тронут ничем, кроме этих четырёх строк: проверено
`git diff`.

### 2026-09-11 — телефон СТО: `formattedValue` → `formattedNumber`

Пользователь прислал строку из `MapController.swift:312` — судя по всему,
Xcode на ней встал. Автоматический прогон писал вызов SDK по документации
и сам это пометил как главный риск; риск сработал: у `YMKSearchPhone`
свойство с отформатированным номером называется `formattedNumber`,
а `formattedValue` не существует.

Как проверено. **Проверить по документации не удалось**: `yandex.ru`
и `yandex.com` закрыты egress-прокси, поиск точной страницы класса не даёт.
Имя взято по памяти о MapKit (у Android-версии тот же `getFormattedNumber`).
Окончательная проверка — сборка у пользователя; если и это имя не примется,
автодополнение Xcode на `business.phones.first?.` покажет настоящее.
Типографика — 654 строки, 0 ошибок; ревизия проекта чистая.

### 2026-09-11 — дубли: закрытое в другой ветке в работу не берётся

Просьба пользователя после утреннего дубля: «при виде дубля процесс
останавливался и работа не повторялась — только сделай тест, чтобы ничего
не сломать сразу».

Механизм дубля: прогон работает от `main`, а закрытая задача лежит в `done/`
другой, ещё не слитой ветки. Для `main` она `todo`, и её делают заново.
Значит, проверка обязана смотреть не в свою папку, а во все ветки на GitHub.

1. **`tools/inbox-dedup.py`** — для каждой `todo` считает тот же ключ, что
   `inbox-extract.py` (sha1 первой строки тела), и сверяет с `done/` во всех
   `refs/remotes/origin/*` и в своей папке. Совпало — дубль. Две одинаковые
   `todo` в одной очереди — вторая дубль первой. С `--mark` ставит
   `status: duplicate` и дописывает раздел «Дубль» с веткой и файлом; сканер
   берёт только `todo`, так что помеченное в очередь не попадает — на дубле
   всё останавливается само. Код возврата 3 при дублях, чтобы это было видно
   и скриптам.
2. **`tools/inbox-dedup-test.sh`** — тест на отдельном репозитории
   (`mktemp`, bare-remote, две ветки): закрытие в другой ветке под другим
   именем файла, две одинаковые `todo`, отсутствие ложных срабатываний на
   похожем тексте, идемпотентность `--mark` (файлы байт в байт те же),
   своя `done/`. 17 проверок.
3. Шаг вписан в навык между извлечением и сканером, в «чего не делать» —
   запрет снимать пометку самому. README: статус `duplicate`.

Тест писался первым и трижды падал, и два падения из трёх были не в тесте:
`git ls-tree` отдавал кириллические пути в кавычках с восьмеричными кодами,
и инструмент не находил `done/` в чужой ветке вовсе — те же грабли, что днём
у `git diff --name-only`. Лечение одно: `-c core.quotepath=false`. Без теста
это вышло бы наружу завтра утром как «дубли не ловятся», и искать причину
пришлось бы по живому прогону.

Как проверено. `tools/inbox-dedup-test.sh` — 17 из 17. На настоящем
репозитории `python3 tools/inbox-dedup.py` — «Дублей нет», код 0: после
утреннего сведения так и должно быть. Ни один файл в `inbox/` не изменён.

### 2026-09-11 — сведены ветка прогона и рабочая ветка

Первый автоматический прогон отработал целиком (`claude/inbox-2026-09-11`,
6 коммитов), но шёл от `main` с устаревшей очередью — и сделал заново две
задачи, уже закрытые в `claude/epic-darwin-axs48i`. Ветки сведены сюда;
конфликты в трёх файлах и семи заметках.

Решения при сведении:

1. **Совет по расходникам — только в шторке находок.** Прогон выводил его на
   все карточки, включая тёмную на экране (нода `46093:2421`, 370×102),
   с аргументом «дизайна на эту строку нет, 102 — нижняя граница». Пользователь
   выбрал не рвать макет. Взята вёрстка прогона (иконка `cart`, три строки),
   но под моим `showAdvice`, который включён только в `findingsScroll`.
2. **Источник текста советов один — `Core/Pure/PartAdvice`.** Прогон положил
   свой словарь в `DiagnosisVocabulary` (`Core/Services`); `CLAUDE.md` требует
   чистый слой. Второй словарь снят. Формулировки прогона («Купить: …
   Проверить: …», с неразрывными пробелами) местами лучше моих — лежат
   в коммите `610cc38`, можно перенести в `PartAdvice` отдельно.
3. **Сохранение совета в историю** (`EngineFinding.advice`, необязательное
   поле) — взято у прогона целиком: миграция облегчённая, старые записи
   читаются как «совета нет».
4. **Форма добавления с главной**: и то, и другое. Блок фото под обеими
   вкладками — мой; превью каталога по названию, пока своё фото не выбрано, —
   прогона, внутри ветки «По названию».
5. **`check-typography.py`** — та же починка с двух сторон, взята моя.
6. **Заметки очереди** — версии прогона: он разметил их позже и подробнее,
   а две перенёс в `done`. В `done` теперь по две записи на «превью» и
   «расходники» — это следы двух реализаций, обе настоящие, не чищу.

Не сведено намеренно: телефон СТО на карте (`PhoneFormat`, `Place.phone`,
вызов `getItemOf(YMKSearchBusinessObjectMetadata.self)`) — целиком от прогона,
конфликтов не было, но вызов SDK написан по документации и без Xcode
не проверяется. Это первое, что смотреть при сборке.

Как проверено. `--audit` — ревизия чистая, 51 файл; `check-typography.py` —
654 строки, 0 ошибок, 36 предупреждений (столько же, сколько до обеих веток);
маркеров конфликта нет, неслитых файлов в индексе 0; `PartAdvice.line` —
единственный вызов, `advice(forPart:)` в коде отсутствует; скобки в трёх
тронутых файлах сходятся. **Сборкой не проверено — Xcode здесь нет**, и после
сведения двух непроверенных веток это важнее обычного.

Осталось: собрать; посмотреть шторку находок, форму добавления с главной
на обеих вкладках, карточку места на карте с «Позвонить».

### 2026-09-11 — утренний разбор inbox: советы по расходникам, телефон СТО, превью по названию

Первый автоматический прогон навыка `/inbox`, ветка `claude/inbox-2026-09-11`.
В очереди девять задач из `backlog.md`, все со `scope`, взято четыре куска
работы; остальное разобрано в заметках без правок кода — почему, сказано там.

**Рекомендации по расходникам после прослушивания.**
`AppMVP/Core/Services/CarDiagnosis.swift`: рядом с `DiagnosisVocabulary.part(_:)`
встал `advice(forPart:)` — что купить и что проверить до покупки, по тем же
ключам семейств. Незнакомый ключ и классы «other»/«none» остаются без строки:
выдуманный совет хуже отсутствующего. `AppMVP/Features/Issues/IssuesScreen.swift`:
у `EngineIssue` появилось поле `advice`, карточка рисует его третьей строкой со
значком корзины, и только на карточках-версий — у вердикта советовать нечего.
`AppMVP/Models/EngineCheck.swift`: `EngineFinding.advice` (необязательное, как
`Place.originRaw`, — облегчённая миграция), так что совет виден и в истории,
а не только в шторке разбора.

**Телефон места на карте.** Заметка просила «запись на СТО через приложение
или выводить телефон»; первое в коде не делается — своего расписания у сервисов
нет, а договариваться надо с каждым. Сделано второе.
`AppMVP/Core/Pure/NumberFormat.swift`: `PhoneFormat` — разбор номера в `tel:`,
выбор первого годного из списка, подпись неразрывными пробелами; номер, который
не разобрался, до экрана не доезжает, потому что кнопка, которая никуда
не звонит, хуже отсутствующей. `MapController`: номер берётся из контейнера
метаданных ответа поиска (`YMKSearchBusinessObjectMetadata`), а не из
гео-объекта — у адреса и остановки телефона нет по определению.
`MapScreen`: строка «Позвонить» с самим номером на карточке места.
`PlacesList`: свайп слева направо — правый край занят удалением и звездой.
`Place.phone`: у избранного номер сохраняется, иначе позвонить в свой сервис
можно было бы только после нового поиска. Плюс 13 проверок в
`tools/pure-checks/main.swift`.

Риск здесь один и он назван в заметке: SDK Яндекса в облаке нет, и вызов
`metadataContainer.getItemOf(...)` написан по документации, а не по заголовкам.
Разойдись имя класса или поля — не соберётся ровно `MapController.phone(of:)`.

**Превью машины в форме добавления по названию.** Заметка просила превью
«не только при онбординге». По номеру оно уже везде: форма с главной ведёт
находку через ту же шторку «Это ваш автомобиль?», что и первый вход. Дыра была
в другом месте — по названию машину добавляли вслепую на обоих экранах, и кадр
она получала только на главной. Общее вью `CarCatalogPreview` в
`AppMVP/Features/AddCar/CarFoundSheet.swift` (туда, а не в новый файл:
`project.pbxproj` ведётся руками, и вслепую его не трогают) подбирает кадр
каталога по набранному названию; включено в `AddCarView` и в `AddCarSheet`,
пока своё фото не выбрано.

Две тонкости, обе из-за раскладки. Первая: решает, показывать ли превью,
**вызывающий** — `CarCatalog.slug(name:) != nil`. Пустое вью внутри `VStack`
всё равно получает отступы соседей, и незнакомая машина оставляла бы в форме
дыру в высоту `spacing`. Вторая: подложка держит высоту, пока кадр
декодируется, — иначе блок возникает из ничего и толкает форму вниз уже после
того, как человек начал печатать. Декодирование идёт через `ImageLoader`:
`UIImage(contentsOfFile:)` отдаёт кадр с отложенным разворачиванием, и делает
он это потом на главном акторе.

**Ложное предупреждение в `tools/check-typography.py`.** Правило про
однобуквенный предлог искало пробел вокруг буквы через `\s`, а `collapse()` к
этому моменту уже превратил `\u{00A0}` в настоящий неразрывный пробел, который
Python тоже считает пробельным. Правило ругалось ровно на те строки, где перенос
уже запрещён: новые сорок пять строк советов дали двадцать четыре таких
предупреждения. Теперь в правиле обычные пробелы.

Проверено: `python3 tools/check-typography.py` → «Проверено строк интерфейса:
615. Ошибок: 0, предупреждений: 36» (до правок — 570 строк и те же 36
предупреждений, то есть новые строки чистые). **Сборкой не проверено: Xcode
в облаке нет**, `swiftc` тоже нет, поэтому `./tools/run-pure-checks.sh` не
запускался; `check-project.py` не запускался — он требует `plutil`, команды
macOS. Ни одна правка этого прогона не собиралась и с макетом не сверялась.

Осталось: строку совета никто не видел на экране — высота карточки в макете
(102) теперь нижняя граница, а не высота, и как это ляжет на шести карточках
подряд, покажет только сборка.
### 2026-09-11 — рекомендации по расходникам и починка проверки типографики

1. **`Core/Pure/PartAdvice.swift`** — словарь на 21 деталь: что проверить
   и что обычно меняют, по кодам пайплайна разбора звука. Чистый слой:
   только `Foundation`. Прописан в `project.pbxproj` в четырёх местах,
   префикс ID `1A00`/`1B00` проверен грепом на занятость.

2. **`Features/Issues/IssuesScreen.swift`**: у `EngineIssue` поле `advice`,
   заполняется только для карточек с названной деталью. Показывается
   параметром `showAdvice`, и включён он **только в шторке находок**.
   На тёмной карточке экрана выключен намеренно: нода `46093:2421` —
   370×102, третья строка выносит карточку за высоту. Тут уже был урок:
   заголовок в 17pt давал 104 вместо 102 и ловился сверкой.

3. **`tools/check-typography.py`**: правило про однобуквенные предлоги
   предлагало чинить неразрывным пробелом и продолжало ругаться на строку,
   починенную ровно так. В Python `\s` покрывает и неразрывный пробел,
   поэтому правая граница `(?=\s)` не давала правилу исполниться. Справа
   теперь обычный пробел. Предупреждений 36 вместо 51: ушли ровно те 15,
   что в новом файле уже починены, остальные настоящие и на месте.

Как проверено. `--audit` — ревизия чистая, 51 файл, все в проекте, скобки
в pbxproj сходятся (158/158), уникальных ID 141. `check-typography.py` —
651 строка, 0 ошибок. Регулярка проверена отдельно на трёх строках: обычный
пробел ловится, неразрывный — нет, то есть правило не ослаблено, а исправлено.
`run-pure-checks.sh` не запускался: нет `swiftc`, чистый слой прогоном
не накрыт. **Сборкой и сверкой с макетом не проверено: Xcode здесь нет.**

Отчёт — `inbox/reports/2026-09-11-вечер-2.md`. Четыре задачи очереди остались
`todo` со `scope`: правило навыка — не хвататься за всю очередь разом.

Осталось: собрать в Xcode и посмотреть шторку находок; прикрепить репозиторий
к Routine через интерфейс claude.ai — из чата это не делается.

### 2026-09-11 — сборка после переименования прошла

Пользователь собрал проект: переименование `Wheelly` → `Canary` чистое,
таргет, схема и ссылки на пакет живы.

Вторая попытка дала более внятную ошибку, чем первая: не просто «Missing
package product», а `The file "swiftpm" doesn't exist` и `Resolving Package
Graph Failed`. То есть дело было не в испорченном состоянии, а в отсутствующей
папке `project.xcworkspace/xcshareddata/swiftpm` — сам Xcode её не создаёт.
Помогло: `mkdir -p` этой папки, снос `DerivedData` старого имени, затем
Reset Package Caches и Resolve Package Versions. Запись в `TRAPS.md` уточнена
настоящим лечением вместо предполагавшегося.

Как проверено. Сборка на маке у пользователя прошла — это его вывод, не мой:
Xcode в этом окружении нет. **Правка `AddCarSheet` сборкой по-прежнему не
проверена**: она лежит в ветке `claude/epic-darwin-axs48i`, а собирался `main`.

### 2026-09-11 — переименование потеряло состояние SPM

Xcode на переименованном проекте сказал `Missing package product
'YandexMapsMobileFull'`. Пакет ни при чём: ссылка на него в `project.pbxproj`
цела (`XCRemoteSwiftPackageReference "mapkit-ios"`, `repositoryURL`
`https://github.com/yandex/mapkit-ios`). Потерялось разрешённое состояние.

Причина моя: переименовывал папку проекта через `git mv`, а он переносит
**только отслеживаемые** файлы. `Package.resolved` и `.swiftpm/` стояли
в `.gitignore`, git о них не знал, и после переименования они остались лежать
в старой `Wheelly.xcodeproj`. Новая папка приехала без
`project.xcworkspace/xcshareddata/swiftpm` — для Xcode это «продукта нет».

Что сделано:

1. `Package.resolved` снят с игнора. Для приложения (в отличие от библиотеки)
   его и надо держать в git: это пин версий. Отдельно важно здесь, потому что
   требование пакета — `upToNextMajorVersion 4.0.0`, и повторное разрешение
   молча подтянет другую 4.x.
2. Грабли записаны в `docs/TRAPS.md`, Часть I: `git mv` и непереносимое
   неотслеживаемое. Проверять перед переименованием — `git status --ignored`
   в старой папке.

Лечение у пользователя (папка `swiftpm` сохраняет ровно те версии, что были):

```
cp -R "Wheelly.xcodeproj/project.xcworkspace/xcshareddata/swiftpm" \
      "Canary.xcodeproj/project.xcworkspace/xcshareddata/"
```

Как проверено. Ссылка на пакет в `project.pbxproj` прочитана глазами — цела,
URL и требование на месте; в новой папке действительно нет `swiftpm`
(`find Canary.xcodeproj -type f` — шесть файлов, все отслеживаемые).
**Сам Xcode не запускался: его в этом окружении нет**, поэтому что ошибка
уйдёт — проверит пользователь.

Осталось: подтвердить сборку после копирования; удалить старую
`Wheelly.xcodeproj`, когда убедились.

### 2026-09-11 — превью машины в форме с главной, первый прогон по новым правилам

Первая задача, сделанная разбором после того, как ему разрешили писать код.

**Что было.** Форма «Добавить авто» с главной (`Features/Main/AddCarSheet.swift`)
показывала превью снимка и выбор фото только под вкладкой «По названию»
и без подсказки про съёмку. Тот же шаг при онбординге
(`Features/AddCar/AddCarView.swift`) показывал и превью, и подсказку. То есть
со второй машины человек попадал в форму беднее, чем с первой, — ровно то,
о чём была заметка.

**Что стало.** Блок фото вынесен из ветки `tab == 1` в общий `VStack(spacing: 24)`
под обеими вкладками, отдельным `photoBlock`, с подсказкой «Сфотографируйте
машину спереди для лучшего вида» — тем же текстом, что в онбординге. Вложенный
`VStack(spacing: 24)` добавлен намеренно: вынести блок в родительский
`VStack(spacing: 32)` значило поменять отступ между полями и фото с 24 на 32,
то есть уехать от макета ради удобства правки.

**Побочно закрылась мёртвая ветка.** `CarMainView.confirmFoundCar` читает
`newCarPhoto`, но на вкладке «По номеру» выбрать снимок было нечем: код жил,
а дойти до него с главной было невозможно. Загрузка висит на
`onChange(of: carPhotoItems)` в `CarMainChrome` и от вкладки не зависит,
поэтому проводка уже была готова — не хватало только точки входа.

Остальным пяти задачам из очереди проставлен `scope`, они остались `todo`:
следующий прогон возьмёт их со знанием места. Три ушли в `needs-review` —
это работа не в коде (сервер, партнёр, договорённость).

Как проверено. `python3 tools/check-typography.py` — 571 строка интерфейса,
0 ошибок (36 предупреждений, все прежние). `.claude/hooks/require-journal.sh
--audit` — ревизия чистая, 50 файлов. Баланс скобок в тронутом файле сходится.
**Сборкой и сверкой с макетом не проверено: Xcode в окружении нет.** Смотреть
на устройстве обе вкладки формы с главной, с фото и без; отступ 24 между
полями и фото — по макету.

Что прогон показал про сам разбор: скрипт разметки искал заметки по подстроке
имени, и «избранное» совпало с «допилить карту (чипсы и избранное)» вместо
«бизнесы в избранное» — одна заметка получила раздел дважды, другая осталась
без `scope`. Поймано на выводе `inbox-scan.py` и исправлено. Разметку надо
сверять со списком, а не доверять совпадению подстроки.

Осталось: собрать в Xcode; пять размеченных задач.

### 2026-09-11 — разбор пишет код, а не план вместо кода

Замечание пользователя: «ты же будешь решать задачи, а не только отдавать
отчёт как и что делать?» Справедливое. Ограничение было моё, и оно оказалось
туже, чем требует реальность.

Логика была такая: в облаке нет Xcode, симулятора и рендеров Figma, значит
правку вью нечем проверить, значит её нельзя писать. Первая посылка верна,
вывод — нет. Из «нечем проверить» следует «пометить непроверенной», а не
«не писать»: отказ от кода риск не снимает, а перекладывает работу обратно
на человека, ради избавления от которой всё и затевалось. Готовый диф в ветке
человек соберёт и сверит за минуту; писать его с нуля по плану будет час.

1. `tools/inbox-scan.py`: зона `auto` расширена с `Core/Pure` до всего
   `AppMVP/`. Добавлена третья зона `find` — `scope` пуст (так приходит всё,
   что завёл `inbox-extract.py` из списков): сперва выяснить, где это живёт,
   проставить `scope`, дальше как `auto`. В `review` осталось то, что в коде
   не делается вовсе — сервер, деньги, договориться с человеком, — и
   `Canary.xcodeproj`: ручной pbxproj ломается молча.
2. Навык переписан под это: делать работу, а не описывать; при вью держать
   открытым `TRAPS.md` (правка вслепую обязана хотя бы не повторять известные
   грабли); в отчёте рядом с такой правкой — «сборкой и сверкой с макетом
   не проверено». Добавлено правило про размер: задача велика — сделать
   первый осмысленный кусок и оставить `todo`, а не браться за всё.
3. В «чего не делать никогда» вместо «править вью» встало «выдавать план
   за сделанную работу там, где код написать было можно».

Что осталось неизменным и не должно меняться: не заявлять собранным то, что
не собиралось. Список непроверяемого в облаке теперь назван прямо — сборка,
симулятор, жесты, попиксельная сверка, гироскоп, микрофон.

Как проверено. `python3 tools/inbox-scan.py` — все девять задач из списка
получили зону `find` вместо прежнего `review`, то есть завтрашний прогон
будет их делать, а не описывать. `--audit` чист.

### 2026-09-11 — второй прогон синхронизации, чтобы отчёт возвращался в тот же день

Агент launchd запускался раз в сутки, в 08:45 — отнести заметки перед разбором.
Забрать результат было некому: отчёт, написанный облаком в 09:00, лежал бы
на GitHub до следующего утра, и на телефоне пользователь увидел бы его через
сутки. Добавлен второй запуск в 09:30, после разбора.

Как проверено. `plistlib.load` — `StartCalendarInterval` читается массивом
из двух словарей, 8:45 и 9:30. На маке не запускалось: launchd здесь нет.

### 2026-09-11 — приложение переименовано в Canary

Три написания разом — `Wheelly.xcodeproj`, `Beepy` на устройстве, `Whelly`
в хранилище — сведены к одному имени.

Что переименовано: папка проекта (`Canary.xcodeproj`), имя таргета и продукта
внутри `project.pbxproj` и схемы, `CFBundleDisplayName`, строка онбординга
(«Canary — сервис, который поможет…»), ссылки в `tools/check-project.py`
и в заслонке, агент launchd (`dev.canary.inbox-sync`, журнал
`/tmp/canary-inbox-sync.log`), переменные `CANARY_VAULT` и `CANARY_BRANCH`,
все документы кроме журнала.

Две вещи сделаны с оглядкой, а не в лоб:

1. **Bundle id `com.vladislavkotyrev.appmvp` не тронут.** Ключ Яндекс MapKit
   привязан к нему (`Core/Services/MapKitKey.swift`): смена идентификатора
   выключит карту, пока ключ не перевыпущен в кабинете. Это работа с внешним
   сервисом, а не правка строки, и делать её походя нельзя.
2. **Прежние имена продолжают пониматься.** `tools/inbox-sync.sh` читает старый
   `~/.config/beepy/vault`, если нового ещё нет, а пишет всегда в новый — путь
   переезжает сам. `tools/inbox-extract.py` принимает и `canary: ignore`,
   и `beepy: ignore`: заметки в хранилище помечены прежним ключом, и
   переименование не должно втащить их в разбор.

`docs/JOURNAL.md` намеренно не переписан. Это хроника: заменить в прошлых
записях «Beepy» на «Canary» значит подделать запись о том, как было.
По той же причине не тронуты разобранные заметки в `inbox/done` и отчёт
за сегодня.

Как проверено. `.claude/hooks/require-journal.sh --audit` — ревизия чистая,
50 файлов, все в проекте. `python3 tools/check-typography.py` — 570 строк
интерфейса, 0 ошибок (36 предупреждений, все прежние). Структура pbxproj:
338 объектных ссылок, 139 уникальных ID, 156 открывающих скобок против 156
закрывающих — ни один идентификатор правкой не задет. `inbox-extract.py`
на трёх файлах (`beepy: ignore`, `canary: ignore`, без пометки) берёт ровно
один — непомеченный. `bash -n` и `plistlib` на скрипте и агенте — целы.
**Сборкой не проверялось: Xcode в этом окружении нет**, и переименование
таргета — ровно тот случай, где он и нужен. Первую сборку на маке стоит
сделать до следующей правки.

Осталось: `launchctl unload` старого агента и `load` нового, иначе их станет
два; первая сборка в Xcode; решение про bundle id и схему `AppMVP`.

### 2026-09-11 — папка «Личное» мимо git и починенная заслонка

1. **`Личное/` в хранилище остаётся на маке.** `tools/inbox-sync.sh` заводит
   её при первом прогоне вместе с `Черновик.md` и исключает из переноса
   (`--exclude "$LOCAL_DIR/"`), так что в репозиторий она не попадает вовсе.
   В `.gitignore` она тоже прописана — вторая застава на случай ручного
   `add -A`: репозиторий публичный. Отличие от `beepy: ignore`: помеченная
   заметка в репозитории лежит, просто не разбирается, а `Личное` до
   репозитория не доезжает.

2. **Заслонка `require-journal.sh` останавливала коммиты на пустом месте** —
   дважды за день, и оба раза я списывал это на ложное срабатывание по тексту
   команды. Причина оказалась другая и хуже.

   `git show "HEAD:$journal" | grep -q "^###…"` под `set -o pipefail`: grep
   находит запись в первых строках и закрывается, `git show` получает SIGPIPE,
   статус трубы — 141, и заслонка заключает, что записи за сегодня нет.
   Пока журнал был короткий, git успевал дописать раньше — с ростом до 80 КБ
   перестал. То есть заслонка ломалась тем сильнее, чем длиннее журнал,
   и останавливала коммит при существующей записи.

   Труба убрана: содержимое читается в переменную, проверка — сравнением
   по шаблону. Тот же приём применён к `printf … | grep -qxF "$file"` в
   проверке pbxproj — там SIGPIPE отменял «файл удалён» и удалённый файл
   требовали прописать в проект.

   Заодно сужен поиск `commit -a`: раньше флаг искали по всей команде, и `-a`
   у соседнего `rsync` читался как `commit -a`. Теперь — только в хвосте
   самого коммита, до ближайшего разделителя.

Как проверено. Заслонка на настоящем payload: команда, которая её
останавливала, теперь проходит (код 0); ревизия `--audit` чистая, 50 файлов.
Регекс проверен отдельно на четырёх строках: запись за сегодня в начале файла
и после перевода строки — находится, вчерашняя и пустой вход — нет, то есть
сторож продолжает сторожить. Сквозной прогон синхронизации
(`scratchpad/synctest4`): папка `Личное` завелась в хранилище, дописанный
в неё пароль не появился ни в рабочей копии (`grep -rl`), ни в истории
(`git log -S`). `rsync` — заглушка, как и раньше.

### 2026-09-11 — личный список задач, который разбор не читает

Просьба: отдельный файл с задачами для себя, мимо автоматики.

1. Признак, а не список имён: заметка с шапкой `beepy: ignore` (или `игнор`)
   извлечением пропускается целиком. Пометить можно любую, и переименование
   её не расколдует — список имён такого не умеет.
2. `inbox/Мои задачи.md` — сам файл, с этой шапкой и двумя разделами.
3. Пустая `- [ ]` больше не становится задачей. Вылезло на том же тесте:
   заготовка в шаблоне заводила задачу с именем «задача» и пустым текстом.
4. `tools/inbox-sync.sh`, шаг 5: заметки из корня `inbox/` уезжают в хранилище.
   Раньше поток был односторонний, и файл, заведённый не с телефона, до
   телефона не добирался — включая этот и `README.md`. Ключевое здесь
   `--update`: если та же заметка в хранилище новее, она остаётся. Иначе
   правка, сделанная с телефона между синхронизациями, затиралась бы копией
   из репозитория. `--delete` на этом шаге нет вовсе: чужие заметки не наше
   дело.

Как проверено. `inbox-extract.py --dry-run` на файле с шапкой — «Новых строк
с галочкой нет»; на копии без шапки и с одним настоящим пунктом среди пустых
галочек — ровно один пункт. Сквозной прогон в `scratchpad/synctest3`: после
синхронизации «Мои задачи.md» лежит в хранилище; дописанная туда с «телефона»
строка после следующего прогона на месте (не затёрта копией из репозитория)
и задачей не стала. `rsync` снова заглушка — расширена до `--update`.

### 2026-09-11 — синхронизация с маком заработала, `backlog.md` снят

Пользователь прогнал `tools/inbox-sync.sh` на маке — прошло. В ветке
появился коммит `4bd8265 inbox: заметки из Obsidian, 2026-09-11 21:37`
с файлом `inbox/Таски.md`: это тот же список, что я накануне положил руками
в `inbox/backlog.md`, только теперь он приехал из настоящего хранилища.

Новых задач прогон не завёл, и это правильно: `inbox-extract.py` помнит
заведённое по хешу текста строки, а не по имени файла, поэтому те же девять
пунктов из другого файла дублями не стали. Дедупликация по тексту, задуманная
против «одна строка — задача каждое утро», заодно выдержала переезд списка
между файлами.

`inbox/backlog.md` удалён. Он был моей копией списка и в хранилище не лежит,
значит синхронизация его не обновляет: жил бы вечным двойником `Таски.md`,
расходящимся с ним с первой же правки с телефона. Источник теперь один —
то, что приезжает из `Whelly`.

Как проверено. `git show origin/…:inbox/Таски.md` — список на месте, оба
пункта с `[x]` сохранены. `python3 tools/inbox-scan.py` — девять задач
в очереди, все в зоне `review`, плюс одна заметка `needs-review` в «не взято».
Повторных задач из `Таски.md` не появилось.

Осталось: разобрать девять задач (работа утреннего прогона); понять, почему
автоматический прогон 17:45 не запушил ветку.

### 2026-09-11 — inbox-sync не требует чистой рабочей копии

Третья остановка у пользователя: `pull не прошёл, чиню руками`. Причина —
`git pull --rebase` отказывается работать при незакоммиченных правках, а они
в рабочей копии есть почти всегда. К заметкам они отношения не имеют, но
синхронизацию останавливали целиком.

Добавлен `--autostash`: недописанная правка прячется на время перебазирования
и возвращается обратно. Плюс диагностика — при отказе `checkout` или `pull`
скрипт печатает `git status --short`, а не одну строку «чиню руками»,
из которой ничего не понять.

Как проверено. Сквозной прогон в отдельном репозитории (`scratchpad/synctest2`)
с намеренно грязной рабочей копией: `M код.txt` до прогона, задача из заметки
заведена и запушена, после прогона `M код.txt` на месте и текст правки цел.
`rsync` снова подменён заглушкой — в окружении его нет.

Осталось: прогнать на маке; понять, почему автоматический прогон 17:45
не запушил ветку.

### 2026-09-11 — inbox-sync: путь мимо скрипта и порядок зеркал

Две поломки подряд у пользователя, обе мои.

1. **Путь ломался редактором.** Я посоветовал править `VAULT` в TextEdit,
   а он подменяет прямые кавычки «умными» — bash перестаёт видеть строку и
   падает с `unexpected EOF while looking for matching '"'`. Лечение не в
   выборе редактора, а в том, что править скрипт больше не нужно: путь
   передаётся аргументом один раз, скрипт кладёт его в `~/.config/beepy/vault`
   и дальше берёт оттуда, launchd запускает без аргументов. `REPO` тоже
   больше не константа — считается от места самого скрипта, так что запуск
   из любой папки работает (первая попытка была из `~`, и всё не нашлось).
2. **Разобранная задача возвращалась в очередь.** Порядок был: импорт из
   хранилища → разбор → зеркало обратно. Хранилище хранило копию заметки
   в `tasks/`, и импорт затаскивал её назад **до** того, как зеркало успевало
   её убрать: перенос в `done/` отменялся каждое утро. Теперь папки разбора
   (`tasks`, `bugs`, `done`, `reports`) зеркалятся с `--delete` **до** импорта
   и ещё раз после, а импорт из хранилища их исключает вовсе. Заметки человека
   по-прежнему без `--delete`: удалять их может только разбор, переносом.

Как проверено. Сквозной прогон в отдельном репозитории
(`scratchpad/synctest`: bare-remote, клон, поддельное хранилище с пробелом
и двоеточием в имени — как настоящее «тестовая мобила:мак»):
- прогон 1 — снял обратные слэши от Finder, завёл задачу из `- [ ]`, пункт
  с `[x]` пропустил, запушил, зеркалил;
- перенос задачи в `done/` руками, прогон 2 — «новых заметок нет»,
  репозиторий `tasks: 0 done: 1`, хранилище `tasks: 0 done: 1`. **До правки
  здесь было `tasks: 1` — задача воскресала**;
- прогон 3 — новая строка в заметке хранилища превратилась в задачу,
  старая осталась в `done/`.

`rsync` в этом окружении нет, поэтому в тесте он подменён заглушкой на bash,
понимающей `-a`, `--delete` и `--exclude`. Значит проверены порядок операций,
разбор аргументов, извлечение, коммит и пуш — но не сам `rsync`. На маке он
штатный.

Осталось: прогнать на маке; понять, почему автоматический прогон 17:45
не запушил ветку.

### 2026-09-11 — inbox-sync терпит путь, перетащенный из Finder

Первая же попытка настроить синхронизацию упёрлась в то, что я сам и советовал:
«перетащи папку из Finder в Терминал». Finder вставляет путь с экранированием
(`Mobile\ Documents`), а внутри кавычек обратный слэш экранированием быть
перестаёт и остаётся буквой — папка не находится. Скрипт на это отвечал сухим
«нет папки хранилища», и понять из него, что не так, нельзя.

Теперь `tools/inbox-sync.sh` сначала пробует снять обратные слэши и, если такая
папка есть, работает с ней. Если нет — печатает три причины по именам:
слэши в кавычках, путь до корня хранилища вместо папки с заметками, хранилище
не в iCloud.

Как проверено. `bash -n` цел. Подстановка проверена отдельно на настоящей
папке: `/tmp/проба\ папки/Whelly` не существует как путь, `${V//\\/}` даёт
`/tmp/проба папки/Whelly`, которая существует. Сам скрипт целиком по-прежнему
не запускался — пути macOS.

### 2026-09-11 — из обычных заметок Obsidian делаются задачи

Уточнение просьбы: синхронизировать папку `Whelly` целиком и заводить задачи
из того, что в ней написано, — а пишутся там обычные списки с галочками,
не заметки формата «одна задача — один файл».

1. `tools/inbox-extract.py` — берёт строки `- [ ]` из заметок в корне `inbox/`
   и в его подпапках (кроме хозяйства разбора), заводит под каждую файл
   в `tasks/` с пустым `scope` и полем `source: файл:строка`. `- [x]` не берёт:
   галочка и значит «не надо». Заведённое помнит `inbox/.extracted.json`
   по хешу текста строки, иначе один и тот же пункт превращался бы в задачу
   каждое утро. Переписал строку — заведётся заново, и это лучше молчания.
2. Пустой `scope` — намеренно: такие задачи попадают в зону `review`, где код
   не правится. Для хотелки вроде «прелоудеры скелетоны» правильный результат
   разбора — план и `scope`, а не самовольная правка вью.
3. `tools/inbox-sync.sh` наведён на папку `Whelly` и прогоняет извлечение между
   переносом и пушем. В комментарии — про двоеточие: хранилище называется
   «тестовая мобила:мак», Finder показывает слэш, а rsync считает удалённым
   адресом путь, где двоеточие встретилось раньше первого слэша. Абсолютный
   путь это снимает.
4. `backlog.md` из «не очереди» стал обычным источником: раньше он лежал в
   корне именно чтобы сканер его не видел, теперь извлечение по нему и работает.
   README переписан под два способа поставить задачу — списком (план) и
   заметкой со `scope` (правка кода).

Как проверено. `python3 tools/inbox-extract.py --dry-run` — 9 строк из
`backlog.md`, два пункта с `[x]` пропущены. После настоящего прогона
`inbox-scan.py` показывает те же 9 в зоне `review`, повторный запуск
извлечения — «Новых строк с галочкой нет». `bash -n tools/inbox-sync.sh` цел.
**`inbox-sync.sh` по-прежнему не запускался**: пути macOS, проверить может
только мак.

Осталось: заполнить `VAULT` и прогнать синхронизацию на маке; разобрать
9 заведённых задач (это работа утреннего прогона); понять, почему
автоматический прогон 17:45 не запушил ветку.

### 2026-09-11 — доставка заметок с телефона делается маком

Просьба: писать задачи и слать скриншоты с телефона, разбор — раз в день,
нового репозитория не заводить.

Клонировать `carapp228` в Obsidian на iPhone нельзя: 85 МБ истории и 71 МБ
картинок в `AppMVP/`, а плагин Git на мобиле — git, переписанный на JS. Отсюда
и брался «отдельный лёгкий репозиторий». Он не нужен, если доставкой займётся
мак: хранилище там и так лежит после синхронизации с телефоном.

1. `tools/inbox-sync.sh` — переносит папку хранилища в `inbox/`, коммитит,
   пушит, а затем возвращает `inbox/` в хранилище зеркалом. Зеркало
   обязательно: без него разобранная заметка, уехавшая в `done/`, вернулась бы
   из хранилища в `tasks/` и разбиралась каждое утро заново. В сторону
   репозитория `--delete` нет намеренно — удалять заметки может только разбор,
   переносом; пропавший в хранилище файл чаще промах пальцем, чем решение.
2. `tools/dev.beepy.inbox-sync.plist` — launchd на 08:45 и на вход в систему,
   за четверть часа до облачного разбора. Журнал `/tmp/beepy-inbox-sync.log`.
3. `inbox/attachments/` — папка вложений; в навык добавлен шаг «открыть
   скриншот». Заметка с телефона часто несёт картинку, и разобрать её, не
   посмотрев, — разобрать наполовину.
4. Туда же в навык: «не воспроизводится» и «описано неверно» — нормальный
   исход разбора, а не повод искать, что бы всё-таки починить. Правило
   написано по следам заметки про `baseURL`, где чинить по тексту значило
   сломать рабочий код.

Как проверено. `bash -n tools/inbox-sync.sh` — синтаксис цел;
`plistlib.load` на plist — разбирается, `Label` и `StartCalendarInterval`
читаются. **Сам скрипт не запускался**: он ходит в пути macOS
(`~/Library/Mobile Documents/…`), которых в этом окружении нет, и проверить
его может только мак. Пути `REPO` и `VAULT` в начале файла — заготовка,
их надо заполнить.

Осталось: заполнить пути и прогнать `tools/inbox-sync.sh` на маке; понять,
почему автоматический прогон 17:45 не запушил ветку.

### 2026-09-11 — первый разбор inbox: два документа починены, один баг оказался не багом

Ручной прогон навыка `/inbox` по трём заметкам — проверка самой процедуры.

1. `docs/XCODE_SETUP.md`: «Открыть проект» звал
   `/Users/…/Desktop/ios-app/AppMVP.xcodeproj` — проекта с таким именем нет,
   и абсолютный путь привязан к одной машине. Стало `open Wheelly.xcodeproj`
   из папки проекта. Заодно снята неправда в шапке: документ утверждал, что
   полного Xcode нет, хотя он стоит (26.6) — переформулирован как инструкция
   для новой машины. Версия в примере `xcodebuild -version` с 16.x на 26.6.
2. `docs/DEVICE_TESTING.md`: раздел «Что проверять» называл флаг `hasAddedCar`
   в `UserDefaults`. Флага нет: онбординг держится
   `hasCompletedOnboarding`, наличие машины — запросом к SwiftData
   (`RootView.swift:15`). Дописано, чем удаление машины отличается от удаления
   приложения — на это натыкается проверяющий.
3. Оба хвоста вычеркнуты в `docs/TAILS.md`, там же поправлен устаревший адрес
   сервера разбора (`.108` → `.141`).
4. Заметка про захардкоженный `baseURL` — `needs-review`, код не тронут:
   `Core/Services/` вне зоны автоматики. Но важнее другое: **заметка ошибалась**.
   Разбор звука идёт локально — модели лежат в бандле
   (`ClapAudioEncoder.mlmodelc`, `ClapMelFrontend.mlmodelc`), `IssuesScreen`
   пробует `LocalDiagnosis` первым и до сервера не доходит; ошибка на экране
   тоже есть («Не удалось разобрать запись»). Описанного отказа вне домашней
   сети быть не должно. Разбор — в самой заметке.
5. Отчёт: `inbox/reports/2026-09-11.md`.

Как проверено. `grep -n "AppMVP.xcodeproj" docs/XCODE_SETUP.md` — пусто;
`grep -rn "hasAddedCar" AppMVP/` — остался один комментарий о том, что флага
нет; `python3 tools/inbox-scan.py` после разбора — «Очередь пуста», в «не взято»
одна заметка со `status: needs-review`. Сборкой не проверялось: Xcode в этом
окружении нет. `run-pure-checks.sh` не запускался — нет `swiftc` и Swift
не тронут. `check-typography.py` неприменим: читает только `AppMVP/**/*.swift`.

Что прогон вскрыл. Автоматическая сессия в 17:45 отработала десять минут
и не оставила ни ветки, ни коммита — причина не установлена, прочитать её
журнал из другой сессии нечем. И заметке нельзя верить на слово: в навыке
стоит написать прямо, что «баг не воспроизводится» — нормальный исход разбора,
а не повод искать, что бы всё-таки починить.

Осталось: понять, почему автоматический прогон не запушил ветку.

### 2026-09-11 — автоматический разбор inbox из Obsidian

Просьба: чтобы задачи и баги, записанные в Obsidian, разбирались сами,
а отчёт приходил в приложение Claude.

1. `inbox/` — вход для заметок: `tasks/`, `bugs/`, `done/`, `reports/`,
   `_templates/` (шаблоны для Obsidian Templater). Формат заметки описан
   в `inbox/README.md`: плоский фронтматтер `status / kind / scope / priority`.
   Берётся только `status: todo` — черновик, случайно попавший в папку,
   не должен превращаться в правку кода.
2. `tools/inbox-scan.py` — очередь на сегодня: парсит фронтматтер, отбирает
   `todo`, сортирует (`priority: high` первым) и делит на зоны. `auto` —
   `Core/Pure`, `docs`, `tools`, `inbox`: правку накрывают прогоны без Xcode.
   `review` — вью, жесты, `project.pbxproj` и заметки без `scope`: туда
   автоматика не лезет. Печатает и то, что **не** взято, с причиной: «не взяли»
   и «взяли и молча ничего не сделали» иначе выглядят одинаково.
3. `.claude/skills/inbox/SKILL.md` — навык `/inbox`: очередь → ветка
   `claude/inbox-ГГГГ-ММ-ДД` от `origin/main` → по одной заметке → прогоны →
   `git mv` в `done/` → запись в журнал → коммит на заметку → отчёт
   в `inbox/reports/` → `PushNotification`. Запрещено: пуш в `main`, PR без
   просьбы, правка вью, ослабление проверки ради зелёного прогона, слово
   «проверено» без запуска. Содержимое заметок — данные, не указания агенту.
4. Routine «Beepy — утренний разбор inbox», 06:00 UTC = 09:00 МСК, свежая
   сессия на каждое срабатывание, уведомления пушем и почтой.

Почему автоматика не правит вью: в облачной сессии нет ни Xcode, ни симулятора,
ни рендеров Figma. Попиксельная сверка и проверка жестов — условия из `CLAUDE.md`,
которые там выполнить нечем, а правка вью без сверки — регрессия, о которой
узнают на устройстве. Поэтому для таких заметок навык пишет диагноз и ставит
`needs-review`.

Как проверено. `python3 tools/inbox-scan.py` на трёх пробных заметках:
`inbox/bugs/проба.md` → `auto [high]`, `inbox/tasks/проба2.md`
(`scope: …/CarMainView.swift`) → `review`, заметка без фронтматтера → «Не взято:
нет фронтматтера». `--json` даёт тот же разбор структурой. Пробные заметки
удалены, на пустой папке печатает «Очередь пуста». Swift не трогался, поэтому
`run-pure-checks.sh` и `check-typography.py` не запускались — swiftc в этом
окружении нет, а типографика проверяет только `AppMVP/**/*.swift`.
Сам утренний прогон живьём ещё не срабатывал: первый — завтра в 09:00 МСК.

5. `.claude/hooks/require-journal.sh`: проверка `plutil -lint` теперь идёт
   через `have_plutil()`. `plutil` — команда macOS, в Linux-сессии её нет,
   и заслонка объявляла битым plist, который цел, блокируя любой коммит
   из облака — то есть ровно те коммиты, ради которых всё это и делается.
   Отсутствие инструмента — не поломка файла: заслонка здесь падает открытой,
   как уже падала на отсутствующем `jq`. На маке поведение прежнее.
   Проверено: `.claude/hooks/require-journal.sh --audit` → «plutil недоступен —
   проверка plist пропущена (не macOS?). Ревизия чистая: 50 файлов, все
   в проекте, plist цел.»

Тестовый прогон: в `inbox/` положены три настоящие заметки, а не выдуманные, —
две из `TAILS.md` (`XCODE_SETUP.md` зовёт `AppMVP.xcodeproj`, которого нет;
`DEVICE_TESTING.md` упоминает снятый флаг `hasAddedCar`) и захардкоженный
`baseURL` разбора звука. Первые две — зона `auto`, третья `review`: так прогон
проверяется на обеих ветках процедуры. Заодно выяснилось, что `TAILS.md` сам
разошёлся с кодом дважды: `IssuesScreen 2.swift` уже удалён, а адрес сервера
теперь `192.168.1.141`, а не `.108`.

`inbox/backlog.md` — продуктовый список из Obsidian как есть (карта и списки,
продуктовые фичи, верхнеуровневое). Лежит в корне `inbox/`, а не в `tasks/`,
поэтому сканер его не видит: это человеческий список хотелок, а не очередь.
Созревшая строка превращается в отдельную заметку по шаблону. Соседи по смыслу
названы в шапке файла: `CHECKLIST.md` — что нужно от пользователя, `TAILS.md` —
мелкое «осталось».

Раздел «Как это связать с Obsidian» в `inbox/README.md` переписан: симлинк,
который я советовал первым, не работает. Obsidian Git коммитит репозиторий
хранилища, а файлы за симлинком принадлежат репозиторию проекта — git хранит
симлинк ссылкой и внутрь не заходит, заметки не попадают ни в один коммит,
и выясняется это утром на пустой очереди при полной папке. Рабочий вариант:
хранилище — сама папка проекта, `.git` в корне хранилища, плагин работает
штатно.

Осталось: открыть папку проекта как хранилище Obsidian и включить плагин Obsidian Git с автокоммитом — без этого заметки
до GitHub не доезжают и очередь всегда пуста.

### 2026-09-11 — светофор ТО, починка полосы и напоминание

1. Полоса прогресса ТО чинена: `RoundedRectangle(cornerRadius: 24)` при
   малом прогрессе вырождался в кривой огрызок, а сразу после ТО полоса
   исчезала вовсе. Теперь `Capsule` с минимальной шириной в высоту полосы
   и анимацией `.snappy` вместо скачка.
2. Светофор (пороги — просьба пользователя, живут в `ServiceMath`):
   `Urgency.normal/soon/urgent`, жёлтый при остатке ≤ 5 000 км, красный
   при < 1 000 (и при просрочке). Красит полосу и само число «ТО через».
3. Напоминание: `Core/Services/ServiceReminder.swift` (CA/CB…000C) —
   локальное уведомление (push требует платного аккаунта, а данные и так
   на телефоне): «Пора на ТО» + текст `ServiceMath.reminderText` —
   «{название} {номер} — через {N} км необходимо провести ТО», номер
   подставляется только если он есть. Разрешение спрашивается при первом
   попадании в красную зону; дубли не плодятся (проверка pending);
   пересчёт по ключу «пробег + число записей» в `.task(id:)`.

Проверено: `run-pure-checks.sh` — «Все 157 проверок прошли» (+7 на
светофор и текст); `check-project.py` — «Проект цел»; сборки симулятор и
девайс BUILD SUCCEEDED; установлено и запущено на iPhone. Само
уведомление на устройстве проверяет пользователь: нужно довести остаток
до < 1 000 км (пробег правится плиткой) и разрешить уведомления.

### 2026-09-11 — тёмная карта и кнопка добавления под полями

1. Карта переведена в тёмную тему лейтмотива: `map.isNightModeEnabled =
   true` в `MapController.attach`. Это встроенный ночной стиль Яндекса, а
   не наш фильтр поверх — подписи и дороги остаются читаемыми.
2. Кнопка «Добавить» переехала из низа экрана под поля с отступом 32
   (просьба пользователя) — в обеих формах: шторке из карусели
   (`AddCarSheet`) и первом экране (`AddCarView`), чтобы флоу не
   разъезжались.

Проверено: сборка симулятора BUILD SUCCEEDED, кадр addcar-button.png —
кнопка стоит под полем на 32pt. Тёмная карта на симуляторе не снята
(нужен тап по вкладке, а тапать симулятор нельзя) — проверяет
пользователь на устройстве. Установка на iPhone заблокирована: из Xcode
пропал Apple ID («No Accounts»), подписывать нечем — ждём входа
пользователя в Xcode → Settings → Accounts.

### 2026-09-09 — каретка, тост о сохранении, возврат рыночной цены

1. Каретка (текстовый курсор) в поле цены и пробега встала между числом
   и единицей — выбор пользователя из трёх вариантов: видно, что
   стираются цифры, а «₽»/«км» неизменяемы. В `MorphingNumberField`
   `display` теперь только число, суффикс — отдельной вью за кареткой.
2. Галочка сохранения закрывает шторку и отчитывается: тост «Цена
   обновлена!» / «Пробег обновлён!» / «Цена очищена» плюс успех-хаптик
   (addedServiceTick). Раньше значение уезжало молча при открытой шторке.
3. Рядом с чипсом «ваша цена» — маленькая кнопка «вернуть рыночную»:
   стирает свою цену и возвращает оценку по объявлениям, с тем же тостом.
   Видна, только когда есть и своя цена, и рыночная.

Проверено: сборка симулятора BUILD SUCCEEDED. Девайс-сборка и установка —
следующим шагом; вид и хаптику проверяет пользователь.

### 2026-09-09 — починка морф-поля и одно фото на машину

1. Морф-ввод цифр (цена, пробег) сломался на длинных числах: посимвольная
   стопка Text не умеет `minimumScaleFactor`, «₽» выпихивало на вторую
   строку, а десяток анимируемых вью на каждое нажатие давали лаги (скрин
   пользователя). Переписано на одну строку с системным
   `contentTransition(.numericText())` + lineLimit(1) +
   minimumScaleFactor(0.45): морф остался (это тот же роллинг цифр,
   который Apple использует в своих счётчиках), ужимается, не лагает.
   Единица теперь часть строки — оторваться переносом не может.
   Посимвольный морф остаётся только в номерной рамке: там строка
   короткая и фиксированная.
2. Фото машины: `PhotosPicker` получил `maxSelectionCount: 1` — галерея
   предлагала мультивыбор, хотя берётся первый снимок.

Проверено: сборки симулятор и девайс BUILD SUCCEEDED, установлено на
iPhone (запуск не прошёл — телефон был заблокирован, открывает
пользователь).

### 2026-09-09 — цена в общей капсуле и тряска на пустую отправку

1. `FigmaGroupedTextField` получил необязательную третью строку: название,
   пробег и цена теперь одна капсула. Раньше цена была отдельным
   `FigmaTextField` — читалась чужой и в неактивном виде красилась иначе
   (замечание пользователя). В макете 45854:2880 цены нет вовсе, это наша
   добавка — поэтому решение наше, а не по ноде.
2. Отправка формы с пустыми полями больше не молчит: `AddCarSheet.submit()`
   проверяет номер (`PlateFormat.isValid`) или непустое название, иначе
   поле трясётся (`.shake`) и уходит `.sensoryFeedback(.error)`. Кнопки
   намеренно НЕ дизейблим — пользователь просил отклик, а не мёртвую
   кнопку. Проверка стоит на всех трёх входах: кнопка «Добавить»,
   галочка тулбара и Return/Go с клавиатуры.

Проверено: сборки симулятор и девайс BUILD SUCCEEDED, установлено и
запущено на iPhone. Тряску и хаптик проверяет пользователь.

### 2026-09-09 — морф-ввод цифр в шторке цены и пробега

`Core/Design/Components/MorphingNumberField.swift` (CA/CB…000B): ввод
числа тем же приёмом, что номерная рамка — каждая цифра своя вью с
переходом scale+opacity, разряды перестраиваются пружиной 0.26/0.74,
единица («₽»/«км») рядом, мигающий курсор при фокусе; ввод ловит
невидимое TextField поверх (вставка и автоповтор системные).
Подключено в `PriceInfoSheet` вместо обычного TextField — работает и для
цены, и для пробега.

Проверено: `check-project.py` — «Проект цел» (файл прописан в четырёх
местах); сборки симулятор и девайс BUILD SUCCEEDED; установлено и
запущено на iPhone. Ощущение морфа при наборе проверяет пользователь.

### 2026-09-09 — номерная рамка, морф цифр, пробег той же шторкой

Пользователь принёс torph.lochie.me (морфинг текста) как референс. Сама
библиотека веб-JS и в SwiftUI не переносится — сделаны нативные аналоги:

1. `Core/Design/Components/PlateInputField.swift` (CA/CB…000A в проекте) —
   ввод госномера «номерной рамкой»: поле во всю ширину шторки (86pt),
   крупные знаки основной части, за разделителем уменьшенный регион с
   подписью RUS; незаполненные позиции — тусклый шаблон. Ввод ловит
   невидимое TextField поверх (системная вставка/диктовка сохраняются),
   символы появляются посимвольным морфом (scale+opacity, пружина
   0.28/0.72). Подключено в AddCarSheet вместо FigmaTextField.
   **Откат:** `git revert` этого коммита — FigmaTextField не тронут.
2. Морф цифр: `.contentTransition(.numericText())` на крупном значении
   шторки (цена/пробег) и на значениях плиток главной — цифры перетекают.
3. Пробег правится той же шторкой, что и цена: `PriceInfoSheet.Kind`
   (.price/.odometer) меняет заголовок, значение, бейдж, объяснение и
   единицу в поле; `CarSheet.odometerInfo`, плитка «Пробег» стала кнопкой.

Проверено: `run-pure-checks.sh` — «Все 150 проверок прошли»;
`check-project.py` — «Проект цел»; сборки симулятор и девайс BUILD
SUCCEEDED; установлено и запущено на iPhone. Жесты и вид проверяет
пользователь.

### 2026-09-08 — безрамочная шторка машины и живой граббер

1. «Это ваш автомобиль?» безрамочно по 45854:2921 — с третьей попытки
   понято буквально: не блюр и не свечение, а ЗАЛИВКА всей шторки цветом
   фона кадра (previewAmbient — средний цвет рамки); граница картинки
   исчезла, машина стоит прямо в шторке. Кадр scaledToFit без клипа.
   Блюр-«Apple Music»-вариант удалён.
2. Граббер стал ручкой во всех bottomSheet: невидимая хит-зона 140×30 по
   центру верха с тем же drag-жестом ПОВЕРХ содержимого — в шторках со
   ScrollView (форма ТО, находки) скролл перехватывал общий жест, и
   граббер был декором (замечание пользователя). Узкая зона не задевает
   кнопки тулбаров.

Проверено: BUILD SUCCEEDED, установлено и запущено на iPhone; заливку и
перетаскивание за граббер подтверждает пользователь («ахуенно» — по
заливке).

### 2026-09-08 — глубина превью и откаты по слову пользователя

1. Тост по-настоящему обнял надпись: причиной ширины был не minWidth, а
   сама семантика frame(maxWidth:) — он растягивается до потолка
   предложением родителя. Теперь fixedSize по горизонтали для
   успеха/процесса, ошибки переносятся в пределах 320.
2. Градиент заголовков откатан отовсюду, кроме имён машин (главная и
   шторка подтверждения) — «в добавлении ТО выглядит хуёво» (пользователь).
3. Превью в «Это ваш автомобиль?» получило глубину: средний цвет рамки
   кадра (12×12-даунскейл, усреднение периметра) подкладывается фоном и
   растекается размытым свечением за карточку — приём подложки обложек;
   идея пользователя «фон щита в цвет фона картинки».

Проверено: BUILD SUCCEEDED, установлено и запущено на iPhone; вид
свечения и тоста подтверждает пользователь. Фоновых генераций нет,
расход OpenRouter зафиксирован: $5.97 из $10.

### 2026-09-07 — тост по ширине надписи, плашка номера в подтверждении

По макетам пользователя: тост (нода 45887:3742) обнимает надпись —
minWidth 202 снят, потолок 320 для переноса ошибок остался. Шторка
«Это ваш автомобиль?» приведена к тёмной ноде 45854:2921: номер стал
компактной плашкой главного экрана (узкий SF 14/−0.4, graysGray2 на
fillsPrimary, высота 28) вместо крупной строки; градиент имени уже был.
Галочка остаётся белой (решение), в макете синяя.

Проверено: сборка симулятора и generic/iOS BUILD SUCCEEDED; установка
на iPhone ждёт подключения. Вид подтвердит пользователь.

### 2026-09-07 — финал дня: экспо-покрывало, градиент заголовков, «…» к месту

1. Заглушка неизвестной машины — «премьера на автосалоне»: RX под
   свободной шёлковой драпировкой со складками до пола (банана,
   референс + промпт; первая версия-чехол отклонена — «посвободнее»).
   Одобрено пользователем («ахуенно»). Заменён сам ассет CarPhoto
   (1264×841) — накрывает главную, страницу добавления; в
   CarFoundSheet заглушка превью тоже стала покрывалом.
2. Крупные заголовки — общий токен `Figma.titleGradient` (заливка
   названий машин): «Поднесите телефон к двигателю», «История»,
   «Двигателя не слышно», «Работы», имя машины в подтверждении.
3. «…»-меню гаснет на странице «Добавьте новый авто»: «Удалить авто»
   без машины выглядел анекдотом (weight(of: addPageIndex) →
   opacity + allowsHitTesting).

Проверено: валидатор краёв покрывала ок; BUILD SUCCEEDED (сим и девайс),
установлено на iPhone; вид подтверждает пользователь.

### 2026-09-07 — исправление исправления: вазы возвращены, левым был только Polo

Пользователь: «2115 смотрит влево». Крупный контроль вскрыл системную
ошибку моего же фикса: у 2114 обнаружилась зеркальная надпись «SAMARA» —
значит исходники вазов смотрели ВПРАВО, а «влево» их развернуло моё
зеркало. В мелких 200px-тайлах сторона морды читается ненадёжно (капот
путается с кормой) — так же чуть не пострадал жираф. Реально левым был
только vw-polo. Четыре ваза отзеркалены обратно, HEIC перезаписаны.
Урок в TRAPS просится: сторону машины проверять только на крупном кадре.

Проверено: BUILD SUCCEEDED, установлено на iPhone; сторону подтверждает
пользователь.

### 2026-09-07 — сторона морды выровнена: пять кадров отзеркалены

Пользователь заметил: часть машин каталога смотрит влево (Polo — со
скрина). Прогон по двум крупным сеткам всех 76 кадров: влево смотрели
lada-2107, lada-2114, lada-kalina, lada-priora, vw-polo — вся вазовская
классика и Polo (банана рисовала их по старым пресс-фото). Отзеркалены
(надписей на кузовах нет, значки симметричны), HEIC перезаписаны.
Ложная тревога по жирафу: в мелком тайле обвес сбил оценку стороны,
кадр был правильный — зеркало отменено обратным зеркалом.

Проверено: валидатор краёв ок у всех шести после операций; контроль
глазами mirror-check.png; BUILD SUCCEEDED, установлено на iPhone.

### 2026-09-07 — «…» как в системе: UIMenu, доступно без скролла

Пользователь вернул троеточие с эталоном (контекстное меню виджета
iOS 26): деструктивный пункт там красный целиком. Сделано честно
системно: `EllipsisMenu` — UIViewRepresentable с UIButton
(`showsMenuAsPrimaryAction`) и UIMenu, пункт «Удалить авто»
`.destructive` (UIMenu красит и текст, и корзину по роли; SwiftUI-Menu
красил иконку tint'ом — источник эпопеи «синяя/белая корзина»).
Меню открывает модалку подтверждения. Задел: будущие действия — новые
UIAction в children. ToolbarFade с кнопки снят: меню видно ДО скролла,
иначе машину без записей ТО было не удалить.

Проверено: pure-checks 150 ок; typography 0 ошибок (36 старых
предупреждений); check-project цел; сборки BUILD SUCCEEDED; смоук на
чистом симуляторе — «…» видно без скролла, `cycle detected` 0; кадр
final-smoke.png. Установлено и запущено на iPhone.

### 2026-09-07 — ночь: красная корзина в тулбаре, шторка цены над клавиатурой

1. Корзина удаления машины стала видимой кнопкой тулбара с красной
   иконкой (accentsRed на darkGlassChip) — Menu «…» с destructive-ролью
   выброшен: SwiftUI красит иконку пункта tint'ом, а не ролью, отсюда
   вечные «синяя/белая корзина» у пользователя. Тап — сразу модалка
   подтверждения.
2. Шторка цены при вводе поднимается над клавиатурой (наблюдатель
   keyboardWillShow/Hide → offset), низ на время ввода теряет скругления
   и боковые поля — лист прижат к клавиатуре, как в ноде 46261:4222.
   Плашка объяснения прижата ниже (12).

Проверено: сборки BUILD SUCCEEDED, установлено и запущено на iPhone;
подъём, форму и цвет корзины смотрит пользователь. Известное: глобальный
keyboardDismissBar («Продолжить») остаётся над клавиатурой поверх
подсказки — если пользователь скажет, для этого поля его надо гасить.

### 2026-09-07 — поздний вечер: блюр-верх шторок и модалка удаления по «Фото»

1. Верх шторок был сплошной заливкой — замечание пользователя с эталоном
   iOS 26/27 UI Kit. Общий `SheetTopBlur` (ultraThinMaterial + подмес
   sheetBackground, градиентная маска до нуля) в BottomSheet.swift;
   применён к двум прокручиваемым шторкам — форме ТО и «Вот что мы
   нашли»: контент уходит под тулбар, тот стоит на размытии.
2. Подтверждение удаления машины — по эталону системной «Фото»:
   центрированная карточка на .regularMaterial, объяснение («…вместе со
   всей историей… отменить нельзя»), одно красное действие «Удалить
   авто»; тап мимо — отмена. confirmationDialog удалён; вход прежний —
   корзина в меню «…» (destructive, красная системно).
3. Галочка подтверждения цены остаётся белой — пользователь подтвердил.

Проверено: сборки симулятор и девайс BUILD SUCCEEDED, установлено и
запущено на iPhone; вид блюра и модалки на устройстве подтверждает
пользователь.

### 2026-09-07 — вечер: скролл после удаления ТО, contextMenu, белый tint

1. Удаление единственного ТО замораживало вертикальный скролл:
   `scrollDisabled(services.isEmpty)` отключал прокрутку при ненулевом
   офсете, страница застывала сдвинутой. `deleteService(_:)` сначала
   `scrollToTop()`, потом delete.
2. Карточка ТО: обратно `contextMenu` — по HIG (зажатие поднимает и
   увеличивает элемент, фон затемняется, хаптик системный; прямое
   требование пользователя). Прошлое «улетает вверх» давал кастомный
   preview — теперь его нет, подъём на месте;
   `contentShape(.contextMenuPreview, …)` держит скругление. Тап —
   отдельной кнопкой сразу в правку. Комментарий-решение в коде
   перевёрнут; Menu-вариант отменён.
3. Синие кольца/обводки на корзинах формы ТО — системный синий tint по
   умолчанию. Глобальный `.tint(.white)` в RootView, по решению «акцент
   белый»; закрывает и будущие места без ручного акцента.

Проверено: сборки симулятор и девайс BUILD SUCCEEDED, установлено и
запущено на iPhone. Поведение (скролл, зажатие, отсутствие синего)
подтверждает пользователь. Пасхалка-жираф на проде — скрин пользователя.

### 2026-09-07 — по тёмной редакции: кнопка 92, лёгкий морф, шторка цены с вводом

По прямому указанию следовать секции 43802:3566:

1. Карточка «Добавить авто» — 370×92 всегда (нода 46225:7674). Прежнее
   решение «всегда 148» отменено в DECISIONS; жалоба «стала меньше»
   оказалась про непостоянство высоты в смеси со страницей машины.
2. Лаг анимации корзины: пружина Motion.toast на смене ширины капсулы
   в перестраиваемом ряду → easeOut(0.18).
3. Лаг зажатия «Слушать»: matchedGeometryEffect перекладывал стекло и
   шар на каждом кадре — заменён transition(.scale(0.12, anchor:
   (0.5, 0.92)) + opacity): панель растёт из места кнопки, Namespace и
   morphID удалены.
4. Шторка цены по нодам 46244:4210 / 46261:4222: правка живёт в самой
   шторке — карандаш переключает в режим ввода (поле 40 bold с
   группировкой, галочка подтверждения у поля, объяснение остаётся
   внизу), системный алерт и case `priceEdit` удалены; пустое поле
   стирает цену (честный прочерк). Галочка белая, не синяя из макета —
   решение «акцент кнопок белый» в DECISIONS.

Проверено: `run-pure-checks.sh` — «Все 150 проверок прошли»; сборки
симулятор и generic/iOS — BUILD SUCCEEDED. Установка на iPhone ждёт
подключения (девайс отвалился), поведение клавиатуры у шторки цены на
устройстве не проверено — проверяет пользователь. Открытые вопросы
пользователю: что именно «не по HIG» в шторке находок; подтвердить
белую галочку в цене.

### 2026-09-07 — скролл формы ТО, двухтактная корзина, push-to-listen

Три замечания с телефона:
1. Форма ТО с распарсенным документом не прокручивалась — контент лез
   на тулбар (скрин: «Работы» поверх заголовка). Содержимое
   `AddServiceSheet` обёрнуто в ScrollView, тулбар остался фиксированным.
2. Корзина работ — красная и в два такта: тап взводит (красная капсула
   «Удалить», хаптик .warning через sensoryFeedback с condition — только
   на взвод), второй тап удаляет; 3 с бездействия или чужая корзина
   снимают взвод. Случайный тап больше не сносит заполненную работу.
3. Морф был понят неверно: пользователю нужен push-to-listen как у
   музыки в Dynamic Island — зажал кнопку, через 0.25 с модалка
   вырастает из неё и ЗАПИСЬ ИДЁТ, пока палец на экране; отпустил —
   стоп и разбор. Button заменён своим DragGesture(minimumDistance: 0):
   системной кнопке «тап» от «держу» не отличить. Короткий тап — прежний
   старт/стоп. Пресс-анимация своя (pressingListen), мёртвый
   ListenPressStyle удалён.

Проверено: сборки симулятор/девайс BUILD SUCCEEDED, установлено на
iPhone. Жесты, хаптики и скролл шторки проверяет пользователь. Примечание
по скрину: парсер честно распарсил скормленное РЕЗЮМЕ («Мужчина…»,
«Март 2026») — мусор на входе, мусор на выходе; фильтр «похоже ли на
заказ-наряд» — кандидат в TAILS.

### 2026-09-07 — скраб по точкам, превью в подтверждении, порядок в «Ошибках»

По замечаниям пользователя с телефона, четыре куска:

1. Скраб по индикатору карусели (как allowsContinuousInteraction у
   системного Page Control): DragGesture в overlay КАПСУЛЫ (снаружи frame
   растягивает зону на весь экран и координаты врут), страница под
   пальцем. Первая версия спамила полную пружину Motion.page на каждый
   пиксель — заголовки мешались, страница застревала между машинами
   (скрин пользователя). Лечение: во время ведения easeOut(0.12), на
   отпускании дожим Motion.page + dragX = 0. Хаптик — существующий
   sensoryFeedback на carPage.
2. Шторка «Это ваш автомобиль?»: вместо серой заглушки — кадр каталога
   по найденной модели (и номеру: пасхалка видна уже в превью). Грузится
   в init, не в body; scaledToFill + allowsHitTesting(false) — грабля
   хит-зоны знакомая.
3. «Ошибки» приведены к главному экрану: у «Слушать» тон стекла ослаблен
   0.86 → 0.55 (стекло не читалось вовсе) + motionRim + пресс-стиль
   (0.97/гашение, как PlainCapsuleStyle); счётчики статистики — типографика
   statColumn главного (condensed 14/−0.4 в боксе 20, значение 20 без
   трекинга в боксе 25); поверхность оставлена darkCardSurface —
   «стекло» плиток главного после оптимизации и есть эта краска.
4. История прослушиваний сгруппирована по календарному дню — раньше
   каждый чек печатал свою дату, три записи в день = три одинаковых
   заголовка. Плюс модалка записи теперь вырастает из кнопки «Слушать»:
   matchedGeometryEffect (герой-переход, референс пользователя — зажатие
   Dynamic Island), хаптик impact(heavy) на раскрытии, soft на закрытии.

Проверено: сборки симулятор и девайс BUILD SUCCEEDED, установлено на
iPhone. Жесты (скраб, морф, хаптики) проверяет пользователь — на
симуляторе они непроверяемы. Кнопка «Добавить авто» по свежему скрину
пользователя стоит в штатных 148 — жалоба, видимо, относилась к старой
сборке; слежу.

### 2026-09-07 — пасхалка: жирафий Discovery по номеру Н600НА190

Просьба пользователя: без генерации на лету — конкретный номер выводит
конкретный кадр. В `CarCatalog` — `plateOverrides` (номер без пробелов
заглавными → слаг), проверяется раньше подбора по модели; `slug(...)`
получил параметр `plate`, вью его передаёт. Кадр `giraffe-discovery`
сгенерирован однажды (OpenRouter banana, референс + описание машины с
фото: жирафья плёнка, багажник с фарами и табличкой GIRAFFE TROPHY,
кенгурятник, шноркель), края ок, HEIC в каталоге (76-й).

Сетевые обрывы OpenRouter на длинных ответах: urllib дважды упал
(timeout 10 мин, RemoteDisconnected) при том, что генерация на сервере
проходила и списывалась; curl с --retry дотащил. Урок: большие ответы
OpenRouter тянуть curl'ом.

Проверено: `run-pure-checks.sh` — «Все 150 проверок прошли» (+2 на
пасхалку), сборка generic/iOS BUILD SUCCEEDED. Установка на iPhone
блокирована туннелем CoreDevice (`paired`, `tunnelState: unavailable`)
— лечение: `sudo pkill -f remoted` на маке и передёрнуть кабель.

### 2026-09-07 — добавление по номеру: подтверждение и статусы

Жалоба пользователя: ввод номера из карусели работал молча — ни «что
нашлось», ни «хочу ли добавлять» (как на первом входе), ни индикации
процесса; на любой сбой просто ничего не происходило (старый код —
`try? await` с `return`).

Сделано в `CarMainView`:
- `CarSheet.carFound` (UIState) + `bottomSheet` с переиспользованной
  `CarFoundSheet` (нода 45854:2936, та же, что на первом входе);
  подтверждение `confirmFoundCar()` — только оно вставляет машину;
  отказ возвращает в форму с сохранёнными полями; вставка вынесена в
  `insert(_:)`, путь «по названию» через неё же.
- Тост расширен видами: `.progress` (спиннер, не гаснет сам — закрывает
  следующий тост или `hideToast()`), `.error` (красный знак, 3.5 с),
  `.success` как был. Ширина 202 стала min: ошибки длиннее, капсула
  растёт до 320 с переносом. Тексты: «Ищем машину по номеру…», «Нет
  соединения — попробуйте ещё раз» (URLError), «Машина по этому номеру
  не нашлась», «Проверьте номер», «Машина добавлена!».
- Порядок слоёв не менялся: системная шторка добавления закрывается на
  сабмите до показа тоста, `carFound` открывается после гашения.

Проверено: `run-pure-checks.sh` — «Все 148 проверок прошли»; сборка
симулятор и generic/iOS — BUILD SUCCEEDED. Жесты и реальный поиск на
устройстве проверяет пользователь (установка ждёт подключения iPhone —
вахта стоит). Не проверено на устройстве в момент записи.

### 2026-09-06 — каталог v3, финальный: OpenRouter Nano Banana, кадры как есть

Пользователь зафиксировал требование окончательно: кадр 1в1 как ключевой
референс (RX на чёрном) — тот же ракурс сверху-три-четверти, тот же белый,
и ТЕНЬ ОТ ИСТОЧНИКА СВЕТА, запечённая генерацией; никакой пост-обработки.
Все мои синтетические тени (ореол, «земля», «солнечное пятно») отвергнуты —
урок: сгенерированную сцену не улучшать руками, только менять промпт.

Путь оплаты нашёлся через OpenRouter (крипта с Bybit, ключ в .env):
модель google/gemini-3.1-flash-image, референс — канонический
~/beepy-catalog/reference.png (RX 1251×832 из экспорта пользователя),
промпт «Recreate the reference EXACTLY… only the car itself changes».
Батч в скретчпаде (or_batch.py): чек бюджета каждые 15 машин со
стоп-краном на $8, валидатор краёв, паузы. Итог: 75/75, $5.09,
~50 минут. Ретраи руками: renault-kaptur (белый фон), renault-duster и
lada-niva-travel (срисовали Lexus с референса — вылечены хинтами формы
в строке машины: «boxy upright…»). lada-xray оказался настоящим.

Проверено: `edges_are_black` по всем — ок; сетка or-sheet.png глазами;
HEIC 75 шт (5.6 МБ), BUILD SUCCEEDED, установлено и запущено на iPhone.
Вид подтверждает пользователь. Каталоги v1 (Кандинский) и v2 лежат в
~/beepy-catalog как история; generator-скрипты OpenRouter — в скретчпаде
сессии, при потере восстанавливаются по этой записи.

### 2026-09-06 — каталог v2: Nano Banana + Кандинский v5, единая земля

Каталог пересобран целиком под стиль эталона. 64 машины — Nano Banana
(gemini-3.1-flash-image, платный ключ AI Studio, референсом сам ассет
CarPhoto + мастер-промпт; ~$0.05/кадр, батч в tools нет — скрипты в
скретчпаде сессии, воспроизводятся по журналу). 11 премиалов добраны
Кандинским после исчерпания предоплатных кредитов Google — с
мастер-промптом v5 (машина стоит на тёмной земле с контактной тенью,
«dramatic rim lighting»; запреты пола сняты, у сырья края 27-51 — это
ожидаемо и лечится пост-пассом). Стиль v5 у Кандинского вплотную
приблизился к банановому.

Грабля дня: finalize(floor=10) на банановых кадрах давил их деликатную
тень (яркость 8-17) — замер «яркость пола под машиной» показал 0.8/255
на всех. Сырьё floored ушло в архив, floor для банана отключён; замер
тени включён в чек финала.

Финал: все 75 через один конвейер «Vision cutout → compose_hero (пол +
тень, glow 0.20)» — тень строго одинаковая. Проверено: cutout 0 ошибок,
«нечёрные края: нет», HEIC 75 шт (4.6 МБ), BUILD SUCCEEDED, установлено
на iPhone (запуск руками — телефон был заблокирован). Брак банана на
ручную перегенерацию пользователем в Figma: chery-tiggo7 (нарисован
Lexus), lada-niva-travel (лексусоподобный кроссовер), daewoo-matiz
(M300 вместо классики — на усмотрение).

### 2026-09-06 — не ореол, а земля: тень под машиной

Ореол вокруг силуэта пользователь отверг («не нужно свечение, нужна
тень ПОД машиной, консистентно на всех»). `compose_hero()` переписан
третий раз: светлый эллипс пола под низом машины (пик `glow`=0.20,
blur 70, ширина 0.56 машины) минус тёмный эллипс тени под днищем
(fill 110, blur 25, ширина 0.52) — снаружи светлая кромка земли, под
машиной темно, корпус и крыша не светятся. Промежуточные итерации:
силуэтная тень давала рваную кляксу, яркий пол (0.42) — «светящийся
подиум»; обе забракованы глазами по гриду «эталон — Веста — X5».

Проверено: пересбор 75 из вырезок — «нечёрные края: нет», HEIC
перезаписаны, BUILD SUCCEEDED, переустановлено на iPhone. Вид на
телефоне подтверждает пользователь. Записи «ореол по профилю эталона»
и «тень из сырья» ниже отменены этой.

### 2026-09-06 — тень единая на всех кадрах, по профилю эталона

Требование пользователя капсом: тень как у эталонного RX на ВСЕХ
кадрах, единообразно. Натуральная тень из сырья у каждой генерации
своя — от неё отказались. `compose_hero()` теперь берёт чистую вырезку
и рисует синтетический ореол по профилю, снятому с ассета сеткой
замеров (пик 30-58 у борта, спад за ~100-150 px, под машиной полоса до
~70 px ниже колёс): маска силуэта + копия, смещённая вниз на 40 px →
GaussianBlur(75) → ×0.25; машина кладётся поверх через мягкую маску
(жёсткий paste затирал ореол чёрными полями вырезки). Калибровка
глазами на тройке «ассет — glow 0.25 — glow 0.34»: 0.25 попадает,
0.34 уже «прожектор». Плюс страховочная виньетка краёв (fade 70) — у
высоких машин (Матиз, Крета, Рио) ореол дотягивался до кромки с пиками
27-30 против порога 26.

Проверено: пересбор всех 75 из вырезок — «нечёрные края: нет»; HEIC
перезаписаны; BUILD SUCCEEDED; переустановлено на iPhone. Вид на
телефоне подтверждает пользователь. Запись «тень из сырья» ниже этим
отменена — сырьё в финал больше не идёт.

### 2026-09-06 — тень вернулась из сырья, у GL свой кадр

Синтетический ореол пользователь не принял («а тень? мы можем ее
вернуть?»): у сырых генераций Кандинского под колёсами живёт
естественная контактная тень, и её убивала вырезка. `compose_hero()`
переписан: в кадр идёт сырьё целиком, а границы машины для масштаба
берутся из файла вырезки (`bbox_source`). При увеличении фоновое
свечение сырья доезжало до краёв канвы (18 кадров, пики 28-63) —
добавлена виньетка: рамка с GaussianBlur(45), края гаснут к нулю,
центр не тронут. После неё «нечёрные края: нет» у всех 74.

«Мерс не показывается» — у GL-Class не было кадра (общий ассет — это
«не знаю», но пользователю нужен был его Мерседес): сгенерирован
`mercedes-gl` (Кандинский, «2015 Mercedes-Benz GL-Class SUV», настоящий
X166), добавлено правило `Group("gl","гл","gls","x166")` и запись в
catalog_cars.json — теперь 75 кадров.

Проверено: `run-pure-checks.sh` — «Все 148 проверок прошли» (чек
GL-Class перевёрнут с nil на mercedes-gl); пересбор hero2 — «нечёрные
края: нет»; тройка «ассет — Веста — X5» глазами: контактная тень на
месте, глубина как у эталона; BUILD SUCCEEDED, переустановлено на
iPhone. Вид на телефоне подтверждает пользователь.

### 2026-09-06 — каталожные кадры приведены к формату ассета: масштаб и ореол

Пользователь с телефона: каталожные машины мельче эталонного ассета и без
тени — «теряется глубина, всё выглядит аляписто». Замер ассета
`CarPhoto.imageset/car.png` (1110×738): машина 86% ширины, низ на 0.81
высоты, вокруг корпуса узкий мягкий ореол — пик яркости ~17-20 в
25-40 px от борта, к краям кадра ноль (это не широкая тень, а размытый
силуэт). Наши квадраты 1024 с машиной 90% по центру рядом с ним честно
выглядели мелкими наклейками.

В `tools/car_image_gen.py` добавлен `compose_hero()`: канва 1536×1024
(3:2 как ассет), машина 86% ширины / низ 0.81, ореол — маска силуэта →
GaussianBlur(40) → ×0.09 → lighter. Все 74 кадра пересобраны из raw
(cutout → compose_hero), HEIC перезаписаны (3.6 МБ).

Заодно пойман первый ложный матч с телефона: «Mercedes-Benz GL-Class»
получал кадр C-класса — префикс `c*` ловил слово «class». Префиксы у
Мерседеса заменены на явные индексы (c180…e400), «ВАЗ 2115» матчится в
lada-2114 правильно (двойник).

Проверено: пересбор — «hero готов; нечёрные края: нет»;
`run-pure-checks.sh` — «Все 148 проверок прошли» (+2 на GL-Class/C180);
сравнительная тройка «ассет — Веста — Мерседес» глазами: масштаб и
ореол совпали; сборка BUILD SUCCEEDED, приложение переустановлено на
iPhone пользователя. Вид на телефоне подтверждает пользователь.

### 2026-09-06 — каталог машин в приложении

Сгенерированный каталог встроен в главный экран: у машины без своего фото
студийный кадр её модели вместо общего ассета. Файлы —
`AppMVP/Resources/CarCatalog/<slug>.heic` (74 шт., суммарно 2.5 МБ,
`sips` из PNG с качеством 80), в проекте папка одной folder reference
(9B/9C…007E), поэтому грузятся через `Bundle.main.url(...,
subdirectory: "CarCatalog")`.

Матчер — `Core/Pure/CarCatalog.swift` (CA/CB…0008): нормализация
названия в токены, бренд-гейт (правила моделей пробуются только внутри
узнанного бренда — «Москвич Веста» не матчится), группы алиасов RU/EN,
`*`-префиксы для серий BMW, поколения по первому году из
`Car.generation` («XV50 (2011-2017)» → camry-50), без года — новейшее.
Не нашлось — nil и прежний общий ассет: ложный матч хуже пропуска.

Во вью: `catalogImages` рядом с `carImages`, заполняется в том же
`task(id: carPhotoKey)`; ветка в `carPhoto(...)` рендерит кадр каталога
ассетным режимом (scaledToFit, ширина assetWidth, прижат к низу), а не
scaledToFill пользовательского фото.

Проверено: `./tools/run-pure-checks.sh` — «Все 146 проверок прошли»
(+17 на матчер); `plutil -lint` ок, `tools/check-project.py` — «Проект
цел»; сборка `xcodebuild … BUILD SUCCEEDED` без предупреждений (кроме
вечного AppIntents); на стёртом симуляторе с временным посевом «Лада
Веста» (CatalogSeed, удалён после проверки) снят кадр: каталожная Веста
на главной, бесшовный чёрный, `cycle detected` в логе — 0. Скролл-замер
`defaultScrollAnchor(.bottom)` не гонял: правка не меняет структуру
ScrollView, только картинку внутри той же ячейки. Онбординг на чистом
симуляторе обходится `defaults write com.vladislavkotyrev.appmvp
hasCompletedOnboarding -bool true`.

Осталось: каталог не перекачивается при смене имени машины (имя в
приложении не редактируется — не страшно). Путь Nano Banana/Weave
закрыт решением пользователя (см. DECISIONS.md): батчи Weave требуют
платной подписки, каталог остаётся на Кандинском.

### 2026-09-06 — ночной каталог сгенерирован: 74/74

Первый запуск батча упал на первом же сохранении: временный файл
`out_path + ".try"` — PIL выводит формат из расширения и на незнакомом
кидает ValueError, причём упало бы и в finalize. Фикс: `.try.png`.
Урок: временные имена для PIL держать с настоящим расширением.

Итог прогона (00:31–08:49, M4 Max): 74/74 машины, все прошли валидатор
краёв, 13 понадобился второй сид, падений ноль. Утром прогнан финальный
конвейер: Vision cutout → recompose(margin 0.05) → валидатор — 74/74 ок,
итог в `~/beepy-catalog/final/`, сетка-превью в скретчпаде
(catalog-sheet.png). Русский парк узнаваем весь: 2107, 2114, Гранта,
Калина, обе Нивы, Приора, Веста. Кандидаты на ретрай глазами:
lada-niva-travel и omoda-c5 (завалены по диагонали), nissan-terrano
(странный вид сверху), daewoo-matiz (похож на вэн, а не Матиз); часть
машин может смотреть влево — сторону Кандинский держит нестабильно,
flip применён ко всем одинаково.

Проверено: `grep "ЛУЧШИЙ ПИК" gen.log` — пусто; recompose-прогон
напечатал «нечёрные края: нет». Осталось: точечные ретраи по списку
пользователя, выбор стороны морды, слияние с выдачей Nano Banana.

### 2026-09-06 — мастер-промпт v4: кадр под ключевой референс

Пользователь показал два ключевых референса (Веста и RX на чёрном): камера
ближе, машина занимает почти весь кадр, у RX — приподнятая точка съёмки.
Прежний промпт («entire car fully visible with margins, centered»)
давал дальний каталожный план. В `tools/car_image_gen.py` MASTER_PROMPT
переписан: «dramatic front three-quarter view from a slightly elevated
camera… wide-angle perspective, the car is large and fills most of the
frame, tight margins». Требование «машина целиком» оставлено — иначе
валидатор краёв не отличит крупный план от обрезанного бампера.

Проверено: два прогона Кандинского 5.0 (`kandinsky5_local.py`, seed 7,
RX 350 и Веста) → «края: ок» оба. Веста легла в референс почти один в
один уже из генерации; RX остался мелким и с серым «ковром» под колёсами —
голый recompose (bbox по яркости) ковёр захватил и не помог, полный
конвейер «Vision cutout → recompose(margin 0.05)» собрал кадр как надо.
Вывод: cutout в конвейере обязателен, bbox-recompose без вырезки на
тёмных подложках врёт. Приподнятую камеру RX-референса модель выполняет
нестабильно — на карточке это маскируется recompose-нормализацией.

Осталось: решение о стороне морды (референс Весты смотрит влево,
контракт — вправо).

Вечером того же дня: ночной батч каталога запущен —
`tools/catalog_batch.py` (модель грузится один раз на все 75 машин:
энкодеры кодируют промпты пачкой, DiT генерит подряд; ретраи сидами
7/11/23 по валидатору краёв, остаётся лучший пик; готовые слаги
пропускаются — можно дозапускать). Запуск: `nohup caffeinate -is … >
~/beepy-catalog/gen.log & disown`, переживает закрытие терминала.
Проверено: `py_compile` ок, процесс поднялся (pid 57465), лог пишется.
Путь Weave/Nano Banana упёрся в платный план для MCP-запусков
(в UI флоу работает) — пользователь генерит там руками, список из 75
промптов передан. Утром: смотреть ~/beepy-catalog/gen.log, прогнать
cutout+recompose по raw.

### 2026-09-05 — Кандинский 5.0 локально на M4 Max: Веста наконец настоящая

Продолжение разведки ниже: API у Кандинского 5.0 нет нигде (серверлесс HF
его не хостит, `inference: None`; единственный публичный Space — в
RUNTIME_ERROR на бесплатном CPU), зато diffusers 0.40 поддерживает
`Kandinsky5T2IPipeline` нативно, а веса открыты. Поставлен локальный запуск:
`tools/kandinsky5_local.py`, venv `~/Library/Caches/kandinsky5-venv`
(diffusers 0.40, torch 2.14, torchvision 0.29 — без него Qwen2VL-процессор
падает ImportError), веса ~31 GB в `~/.cache/huggingface`.

Грабля: полный стек ~30 GB bf16 рядом с системой в 36 GB не живёт — процесс
дважды убивало на `from_pretrained` без единой строки вывода. Решение —
двухфазная загрузка: сначала только текст-энкодеры (Qwen2.5-VL + CLIP,
~19 GB), эмбеддинги промптов снимаются на CPU, энкодеры выгружаются
(`del` + `gc` + `torch.mps.empty_cache()`), потом DiT + VAE (~13 GB) и
генерация через `prompt_embeds_qwen/clip/cu_seqlens`.

Проверено: `.../kandinsky5-venv/bin/python tools/kandinsky5_local.py --car
"2020 Lada Vesta sedan" --out .../k5-vesta.png` (и RX 350) → «края: ок» оба;
загрузка 10+11 с, генерация 30 шагов — 340 с (~11.3 с/шаг) на M4 Max 36 GB.
Глазами: **Веста настоящая** — ладья на решётке, X-выштамповки, фары; Flux и
SD3 на том же промпте рисуют выдуманный «VESSA». RX 350 тоже точнее всех
провайдеров (синий гибридный шильдик). Сравнение — grid-k5-vs-sd3.png в
скретчпаде сессии.

Осталось: ~6 минут на кадр — для продакшена по рег. номеру нужен либо
прогрев заранее, либо сервер с GPU; решить судьбу 31 GB кэша весов.

### 2026-09-05 — провайдер hf в car_image_gen: Hugging Face вместо недоступного Кандинского

Искали альтернативные API Кандинского. Итог разведки: их фактически нет —
официальная точка одна (fusionbrain.ai, из сети с VPN не резолвится),
naga.ac Кандинского из каталога убрал (258 моделей, `kandin` — ноль),
eachlabs держит только древний 2.2, каталог gen-api.ru его не подтверждает.
Зато на компе нашёлся проект `~/Desktop/motion-studio` (3D Generator,
HY-Motion) с живым HF-токеном в `.env` — тот самый «сайт с апишкой
нейронки». Токен проверен через `whoami-v2`, работает.

Изменён `tools/car_image_gen.py`: третий провайдер `hf` —
`router.huggingface.co/hf-inference/models/stabilityai/stable-diffusion-3-medium-diffusers`,
токен из env `HF_TOKEN`. Ценность против pollinations: SD3 читает
`negative_prompt` (Flux игнорирует), фон стабильнее. Кандинский-3 на HF
лежит только весами (`inference: None`) — серверлесс его никто не хостит.

Проверено: `python3 tools/car_image_gen.py --provider hf --car "2019 BMW X5"
--flip --seed 7 --out .../sd3-x5.png` → «края: ок»; той же командой RX 350 и
Lada Vesta (seed 7) → «края: ок» оба; seed 11 у X5 дал корму и белый пол —
валидатор честно отбил («НЕ ЧЁРНЫЕ, пик 236/255»). Глазами: RX и X5
узнаваемы, Веста — выдуманный седан «VESSA» (западные модели её не знают,
это аргумент за Кандинского, когда доберёмся). На X5 остался зеркальный
номер на бампере — негатив «no license plate» пробивается не всегда.

Осталось: реальный Кандинский так и не прогнан (нужен fusionbrain без VPN
или ключи), номерные таблички на части генераций.

### 2026-09-05 — Мастер-промпт генерации машин и тестовые прогоны

Заготовка под «студийную картинку любой машины» в стиле ассета главной.
Инструмент — `tools/car_image_gen.py`: мастер-промпт, два провайдера
(Кандинский через FusionBrain и публичный Flux/pollinations для быстрых
проб), постобработка и валидатор.

Промпт дожат тремя итерациями на живых генерациях (Lexus RX, Lada Vesta,
BMW X5, VW Multivan): «studio photograph» уводит модель в серый бокс с
полом — чёрный фон говорится позитивной частью («complete darkness, pure
black empty background, image corners pure black»); негативный промпт Flux
не читает; сторона ракурса нестабильна — берём стабильное «влево» и
зеркалим программно (`--flip`).

Итоговый пайплайн, который держит контракт независимо от капризов модели:
генерация → вырезание объекта (Vision `VNGenerateForegroundInstanceMask`,
на маке демо-CLI, на телефоне то же API) → автокадрирование с полями 8 %
(`recompose` — заодно выравнивает масштаб между генерациями) → валидатор
чёрного периметра. Финальная сетка: четыре машины в стиле ассета, «края
ок» у всех.

Наблюдения по качеству: RX и X5 узнаваемы уверенно, VW-вэн похож, Веста —
обобщённый седан (редкие для мира модели — слабое место; Кандинский на
русском корпусе может знать её лучше — интересный тест). У RX генерация
обрезала бампер краем кадра — критерий ретрая: bbox машины не должен
касаться краёв исходной генерации (в валидатор пока не внесено).

**Кандинский подключён кодом, но не прогнан**: `api-key.fusionbrain.ai`
из этой сети не резолвится (VPN), и нужны бесплатные ключи с fusionbrain.ai
(env `FUSIONBRAIN_KEY`/`FUSIONBRAIN_SECRET`). Тестовые генерации сделаны
через pollinations как прокси-проверку промпта.

### 2026-09-05 — Разряды в числовых полях на лету

Просьба пользователя: большие пробеги без разбивки приходится пересчитывать
глазами. Теперь «1200000» в поле на глазах становится «1 200 000»:

- `NumberFormat.groupedInput` — живая маска (цифры → группировка по три
  неразрывным пробелом, потолок девять цифр), +4 проверки (всего 129).
  Обратный разбор — прежний `digits`, он пробелы отбрасывает, сохранение
  не менялось.
- Маска подключена тем же механизмом `format:`, что у госномера: поля
  «Пробег» и «Цена» на экране добавления и в шторке добавления
  (`FigmaGroupedTextField` научился `secondFormat`), «Пробег» и «Сумма»
  в форме ТО (`fieldRow` научился `format`), алерт правки цены — через
  форматирующий Binding.
- Начальные значения полей тоже показываются с разрядами: пробег и суммы
  при правке записи, разобранный бланк, черновик цены.

**Как проверено.** 129 pure-проверок; сборка чистая; типографика 0 ошибок.
**Не проверено:** сам ввод пальцем (курсор при вводе в конец — штатный
случай; вставка в середину скакнёт в конец — известное свойство масок).

### 2026-09-05 — Парсер бланка ТО с фото и PDF, возврат первой анимации

**Парсер.** «Добавить фото или PDF» теперь работает по-настоящему и целиком
на устройстве. Структура заказ-наряда (форма БО-14 и производные): шапка с
датой, данные авто с пробегом, таблицы «работы» и «запчасти» (наименование
слева, сумма в правой колонке), итоги.

- `Core/Pure/ServiceDocParse.swift` — чистый разбор: группировка
  OCR-фрагментов в ряды по вертикали, сумма — последняя числовая
  последовательность ряда (разряды пробелами склеиваются), название — до
  числовых колонок, ведущий номер строки отброшен; стоп-слова отсеивают
  итоги, реквизиты и строку про сам автомобиль (хвост госномера иначе
  принимался за сумму — поймано живым прогоном); дата и пробег — по своим
  правилам. 11 проверок в pure-checks (теперь 125), включая построчный
  поток текстового PDF.
- `Core/Services/ServiceDocScanner.swift` — обвязка: фото и сканы через
  Vision OCR (ru+en, on-device, accurate), у цифрового PDF сначала
  текстовый слой (точнее и мгновенно), рендер страниц — фолбэк; превью
  первой страницы PDF идёт в чеки.
- Флоу: «Добавить фото или PDF» → системный диалог «Из галереи / Файл PDF
  или скан» (галерея не отдаёт PDF — нужен fileImporter; новые кейсы
  `CarSheet.docSource`/`.filePicker`) → разбор → форма ТО с датой,
  пробегом и работами из бланка. Пустой разбор — честная пустая форма.
- Прогон на синтетическом «скане» заказ-наряда через приложение: дата
  14.08.2026, пробег 92 450, итог 8 850, все 5 строк работ и запчастей с
  точными суммами.

**Анимация выезда** возвращена к первой версии по выбору пользователя:
пружинный докат с клевком (`interpolatingSpring(110, 15)`, наклон −1.6°)
вместо expo-out; ветки Reduce Motion и своего фото не тронуты.

**Не проверено:** реальные бланки (у каждого СТО своя вёрстка — синтетика
покрывает канонику БО-14, кривой скан под углом может распознаться хуже);
путь fileImporter пальцем; PDF с текстовым слоем в приложении (в
pure-checks покрыт построчный поток).

### 2026-09-05 — Оптимизация: подлагивало на iPhone 15 Pro

Жалоба на лаги. Два виновника, оба — свежие правки этого же дня:

- **Живой Liquid Glass на каждой карточке списка.** После унификации с
  «Ошибками» карточки главной стали `glassEffect` — материал пересчитывает
  преломление фона каждый кадр, а на главной таких поверхностей 6 плюс
  каждая запись истории, и всё это едет при прокрутке. При этом под
  карточками чёрный фон — преломлять нечего, и стекло на чёрном неотличимо
  от заливки. Оба экрана переведены на общий painted-рецепт
  `darkCardSurface` (LiquidGlass.swift): #1A1A1A + кромка white 10 %.
  Попиксельная разница с glass-версией — 0.97/255. Настоящее стекло
  осталось у двух кнопок тулбара (`darkGlassChip`): они поверх уезжающего
  контента, там преломление осмысленно.
- **Свет слева через `.plusLighter`** заставлял перекомпозичивать весь
  экран каждый кадр. Заменён обычной альфой: на чёрном фоне результат
  математически тот же, поверх фото разница неразличима.

Заодно вынесено лишнее: удалён `IssuesScreen 2.swift` — дубль вне сборки,
единственная «Проблема: 1» в check-project с давних времён (теперь «Проект
цел»); удалены неиспользуемые ассеты `Dimmerwelcome`/`Dimmersurvey`
(подложки светлого онбординга, сняты ещё при тёмной теме). Проверено, что
блик по гироскопу не при чём: `DeviceTilt` включает CoreMotion только при
видимых бликах, в покое главной подписчиков ноль.

**Как проверено.** Сборка чистая; 114 pure-проверок; типографика 0 ошибок;
`check-project` — «Проект цел»; кадр главной: карточки и свет на местах
(дифф 0.97/255 против стеклянной версии), `cycle detected` 0.
**Не проверено:** сам фреймрейт на устройстве — профилировщик требует
Xcode-сессии на телефоне; оценка по механизму: минус ~десяток live-стёкол
и оффскрин-композитинг на кадр.

### 2026-09-05 — Шторка бара по HIG, «Продолжить», кнопка на месте

Три замечания пользователя.

- **Подложка бара** была сплошной заливкой с тенью — «читается плашкой».
  Теперь по HIG: размытие (`ultraThinMaterial`) плюс градиент затемнения,
  оба растворяются книзу; у блюра маска-градиент — резкая нижняя кромка
  размытия выдаёт прямоугольник так же, как заливка.
- **«Готово» → «Продолжить»**: кнопка панели клавиатуры переводит фокус на
  следующее поле (сверху вниз, слева направо), после последнего закрывает
  клавиатуру. Реализация — `focusNextField()` в BottomSheet.swift рядом с
  `dismissKeyboard()`: обход UIKit-окна до следующего `UITextField`; у
  SwiftUI понятия «следующее поле» нет, а общий FocusState пришлось бы
  заводить в каждой форме. Вызывается только из замыканий (грабля про
  чтение окна в body).
- **Кнопка «Добавить» липла к клавиатуре.** Корень: клавиатура раздувала
  нижний inset в `DeviceMetrics` (GeometryReader у корня приложения) —
  контейнеры экранов сжимались. Метрики теперь снимаются с
  `.ignoresSafeArea(.keyboard)`: они описывают экран, а не клавиатуру.
  Шторке AddCarSheet добавлен свой `.ignoresSafeArea(.keyboard)` — кнопка
  стоит на месте, клавиатура её просто накрывает.

**Как проверено.** Сборка чистая; 114 pure-проверок; типографика 0 ошибок;
кадр прокрутки: блюр-градиент растворяется в контент без кромки (якорь снят).
**Не проверено:** «Продолжить» по полям и поведение кнопки при вводе — жесты,
проверить пальцем.

### 2026-09-05 — Инпуты умирали после выбора фото

Жалоба: «добавляю машину вручную, первым шагом выбираю фото — и не могу
ничего ввести». Механизм — новая грабля (записана в TRAPS.md):
**`scaledToFill` вылезает за рамку не только отрисовкой, но и хит-зоной** —
`clipShape` режет пиксели, а тапы нет. Портретный снимок в превью 370×200
рисуется 370×1110 и невидимо накрывает поля ввода выше: тапы уходили в
картинку, а не в TextField. Превью не интерактивны — лечение
`.allowsHitTesting(false)` в трёх местах: AddCarView, AddCarSheet и
мини-превью чеков в AddServiceSheet (там вылезшие поля крали тапы у
крестиков соседних превью; сама кнопка-крестик в overlay и не гаснет).

**Как проверено.** Посев портретного фото 800×2400 в превью AddCarView —
кадр: клип держит 200pt, вёрстка целая, кнопки на местах; хит-зоны в 1110pt
больше нет по построению. Сборка чистая, 114 pure-проверок, типографика
0 ошибок. **Не проверено:** сам тап по полю после выбора фото — жест,
проверить пальцем на телефоне.

### 2026-09-05 — Шесть замечаний пользователя одним заходом

- **Скачок прокрутки.** «Прокрутил вниз, оставил — позиция возвращается
  сменой кадра»: переключение `toolbarVisibility(.visible/.hidden)` меняло
  инсеты UIScrollView без анимации. Бар теперь существует всегда, прячется
  только содержимое (`ToolbarFade`, прозрачность + `allowsHitTesting`) —
  инсеты стабильны. Диагноз по механизму, жестом не воспроизводился
  (симулятор не тапается) — проверить пальцем.
- **Шапка по HIG.** Подложка бара — чёрная плашка с мягкой тенью вниз
  (`toolbarBackdrop`, живёт прозрачностью с баром), отделяет шапку от
  уходящего под неё контента. Название отодвинуто от «Добавить ТО» на 12pt
  (ширина 167 → 155 — просьба пользователя, отступление от ноды).
- **Цифровую клавиатуру нечем закрыть.** `keyboardDismissBar()` в
  FormControls: панель над клавиатурой — слева иконка сворачивания, справа
  «Готово», оба закрывают (подача Сбера, по просьбе; Mobbin подтверждает
  паттерн — Monzo/American делают то же). Повешен на CarMainView (покрывает
  шторки-оверлеи), AddCarView и AddCarSheet (отдельная sheet-иерархия).
  Tint белый на кнопках, а не на вью: общий перекрасил бы курсоры.
- **Запись ТО «улетала вверх» при зажатии**: системное контекстное меню
  поднимало превью к центру экрана. Заменено на `Menu` — открывается у
  пальца, карточка на месте, работает и тапом, и удержанием.
- **Карточки главной приведены к «Ошибкам»**: `darkGlassCard` теперь тот же
  рецепт, что `darkCardSurface` — системное стекло с тоном #1A1A1A, обводка
  white 10 % только в фолбэке. Painted-профиль с подобранной кромкой отменён
  по прямой просьбе (тексты не тронуты). Прежняя попиксельная сверка карточек
  с рендерами более неактуальна.
- **Свет слева**: неподвижный радиальный источник у левого края
  (white 9 %, поверх контента аддитивно `.plusLighter` — подложкой он
  упирался в непрозрачную ячейку фото и выдавал её границу прямоугольником).
  В макете света нет — просьба пользователя.

**Как проверено.** Сборка чистая; 114 pure-проверок; типографика 0 ошибок;
кадры покоя и прокрутки: тень бара, зазор, кромки карточек, свет без швов
(замер: 16–18/255 слева, 0 справа, граница фото-ячейки исчезла);
`cycle detected` 0. **Не проверено:** сам скачок прокрутки (жест), панель
клавиатуры (нужен фокус в поле), меню записи ТО пальцем.

### 2026-09-05 — Выезд машины на первом кадре

По просьбе пользователя (после двух тестовых прогонов с видео): при входе в
приложение блок фото машины выкатывается слева и плавно тормозит без отскока
(expo-out `timingCurve(0.16, 1, 0.3, 1)`, 1.35 с, задержка 0.45). Едет только
машина — подписи, номер, точки и карточки стоят. Проявлением вместо движения
играют два случая: Reduce Motion (HIG) и режим своего снимка — у реального
фото видны края кадра, слайд читался бы как летящий прямоугольник. Повторных
проигрываний при возврате на вкладку нет (`guard !carDriveIn`).

**Как проверено.** Сборка чистая; раскадровка записи с симулятора — выкат,
торможение, остановка по центру; кадр покоя после анимации попиксельно
совпадает с эталоном (0.17/255); `cycle detected` — 0. **Не проверено:**
Reduce Motion и ветка своего фото (жесты/настройки — руками), холодный старт
на устройстве.

### 2026-09-05 — Шторка цены с объяснением и карандашом

По референсу пользователя (гараж Т-Банка): тап по плитке «Цена авто» теперь
открывает шторку `PriceInfoSheet` — крупная цена по центру, рядом карандаш,
под ценой капсула-статус, внизу карточка-объяснение. Прежний системный алерт
правки никуда не делся: его открывает карандаш, замена шторки алертом идёт
через тот же слот `CarSheet` (новый кейс `.priceInfo`; каталог состояний
`CarSheet.allCases` не перечисляет — прогону ничего не нужно).

Подача по приоритету плитки: своя цена — «ваша цена», без неё рыночная —
«≈ … ₽» и «средняя по рынку» с объяснением «На основе N объявлений о продаже
похожих авто» (число объявлений теперь сохраняется — у `Car` появилось
`marketOffers`); без обеих — прочерк и подсказка, откуда цена берётся.

**Как проверено.** Сборка чистая; 114 pure-проверок; типографика 0 ошибок
(396 строк); кадр шторки на симуляторе с посеянной рыночной ценой — заголовок,
«≈ 994 683 ₽», карандаш, капсула и объяснение с «2 379 объявлений» на местах
(патч снят, база возвращена из бэкапа). **Не проверено:** тап по карандашу и
свайп закрытия — жесты, смотреть пальцем; состояние «своя цена» и «цены нет»
собраны той же логикой, но кадра не имеют.

### 2026-09-05 — Рыночная цена машины на карточке

Карточка «Цена авто» на главной теперь заполняется сама — средней ценой по
объявлениям из «Рыночной оценки» AvtoVinCod (`GET /price?vin=`): тот же
поставщик и токен, что у поиска по номеру. Оценка нужна по полному VIN,
поэтому работает у машин, добавленных по номеру; замаскированный или
отсутствующий VIN оценки не получает — карточка живёт как раньше.

- **Приоритет**: своя цена пользователя главнее рыночной, рыночная — с «≈»
  (это средняя по рынку, не цена этой машины), без обеих — прочерк.
- **Правила** — `Core/Pure/MarketPrice.swift` (+7 проверок в pure-checks,
  теперь 114): пригодность VIN (17 знаков, без «*») и устаревание оценки
  (неделя — запрос платный, чаще обновлять значит платить за то же число).
- **Модель**: у `Car` появились `marketPrice`/`marketPriceDate` —
  необязательные, лёгкая миграция.
- **Обновление**: `.task(id: marketPriceKey)` на главной; ключ — состав
  машин и их VIN, без даты обновления (иначе запись даты перезапускала бы
  задачу). «Данных нет» запоминается на неделю датой без цены; сетевой сбой
  даты не пишет и повторится при следующем входе.
- **Грабля документации**: пример ответа у них плоский, живой сервис кладёт
  числа во вложенный объект `price` (снято curl-ом: average 994683,
  min/max/count). Декодер терпит оба формата.

**Как проверено.** Живой запрос curl-ом с токеном (VIN Лады из их доков) —
200, average 994 683 ₽ по 2379 предложениям; сборка чистая; 114 pure-проверок;
типографика 0 ошибок; карточка с посеянной ценой на симуляторе показывает
«≈ 994 683 ₽» (патч снят, база возвращена из бэкапа). **Не проверено:**
полный путь запроса из приложения — DNS симулятора мёртв под VPN (та же
грабля из CLAUDE.md), на устройстве должен пройти; фактическая цена запроса
(по докам «от 0.50 ₽», реальное списание видно только в кабинете — у
gos2vin заявленные «от 5.5» оказались 8 ₽).

### 2026-09-05 — Аудит: техчасть, кеширование, безопасность

По просьбе пользователя пройдены три направления; три находки, все починены.

- **WAV-записи копились во временном каталоге**: `AudioLevelMeter` писал
  `engine-<UUID>.wav` на каждую запись и не удалял ни одной (~11 МБ на
  минуту звука). Теперь старт новой записи удаляет предыдущую — позже неё
  файл никому не нужен: разбор либо уже прошёл, либо не случится.
- **Ответы поиска авто могли осесть в дисковом кэше**: оба поставщика ходили
  через `URLSession.shared` с общим `URLCache`, а в ответах госномер и VIN —
  персональные данные. Заведена одна эфемерная сессия без дискового кэша и
  кук (`lookupSession`), оба клиента переведены на неё.
- **Токен api-cloud уходил в URL-query** — URL с токеном оседает в кэшах и
  логах промежуточных узлов. Переведён в заголовок `Token:` (документированная
  альтернатива; curl подтвердил, что заголовок сервис понимает).

Что проверено и чисто: предупреждений сборки 0 (включая полный вывод, без
фильтра); TEMP-маркеров в коде 0; print в проде 0; скан паттернов секретов —
одно легальное совпадение (`setApiKey` карт); ATS разрешает только локальную
сеть (наследие отладочного диагноз-сервера — убрать вместе с ним); SwiftData
и записи живут в песочнице с системной защитой файлов. Боевой
`LocalDiagnosis.diagnose()` прогнан на demo.wav через временный хук (снят):
`is_engine=false, engineP=0.254, heard=music` — ровно серверное поведение.
Главная попиксельно совпадает с эталоном (0.17/255), `cycle detected` 0.

Известные ограничения, не чинились: `loadAudio` читает файл в память целиком
(получасовая запись — сотни МБ; запись мотора — секунды, а после чистки tmp
длинных файлов и не остаётся); `LocalDiagnosis.Models` после первого разбора
живёт до конца процесса (~сотня МБ RAM — цена мгновенного повторного разбора).

### 2026-09-05 — Разбор звука переехал на устройство

Раздел «Ошибки» больше не зависит от сервера в домашней сети: весь пайплайн
cardiag работает на телефоне. «CLAP ~2 ГБ и torch» из прежней оценки оказались
про питон целиком; телефону нужна только аудио-башня — **58 МБ** в fp16.

**Конверсия** — `tools/clap_to_coreml.py` (venv с torch 2.7 в
`~/Library/Caches/beepy-convert-venv`; на torch 2.13 конвертер падает):

- Мел-фронтенд `ClapFeatureExtractor` (ветка rand_trunc: slaney-фильтры,
  hann 1024, hop 480, dB) переписан в torch: STFT — conv1d с DFT-ядрами
  (unfold и torch.stft конвертер не умеет). Бикубическая интерполяция
  1001 → 1024 внутри HTSAT подменена постоянной матрицей — оператор
  не поддержан, а при фиксированном входе это численно то же самое.
- **Две модели, не одна**: у power-спектра динамический диапазон ~200 дБ,
  fp16 жмёт его с обеих сторон — сверху переполнение (громкая запись давала
  cos 0.982), снизу денормалы. `ClapMelFrontend.mlmodelc` (4.2 МБ, fp32) +
  `ClapAudioEncoder.mlmodelc` (58 МБ, fp16).
- `torch.export` вместо `jit.trace`: в трассированном графе HTSAT остаются
  `aten::Int` над массивами.
- Головы sklearn (kind/knock/cause/region/knock_region: scaler + логрег +
  температуры) и текстовые эмбеддинги промптов привратника — в
  `DiagnosisSupport.json` (504 КБ); текстовая башня телефону не нужна.
- **Грабля паритета**: у сервера ДВА разных паддинга — привратник
  (`Clap.score`, padding=True) дополняет запись нулями, эмбеддинги голов
  (`Clap.embed`) — повтором записи (repeatpad). Все пороги сняты именно так;
  зафиксировано полями `gate_padding`/`embed_padding` в JSON.

**Swift** — `Core/Services/LocalCarDiagnosis.swift`: чтение WAV +
AVAudioConverter к 48 кГц моно, привратник, окна `_window_vectors` (до трёх
по 10 с, тихие отброшены), головы с пулингом вероятностей, сборка того же
`Diagnosis` (у него появился обычный init). `IssuesScreen.analyse()` идёт
локальным путём всегда, когда модели в бандле; сервер остался отладочным
запасным. Каскад изоляции кусков (Silero VAD) не портирован: локальный путь —
всегда фолбэк «окна файла», ровно тот, которым сервер фактически шёл на всех
замеренных записях (segments == 0 у всех).

**Как проверено.**

- Паритет конверсии (`clap_to_coreml.py`): мел |Δ|max ≤ 0.03 дБ и cos = 1.000000
  на всех реалистичных сигналах; Core ML цепочка — cos ≥ 0.999985 на всех
  шести, включая живую запись demo.wav. Чистый цифровой синус и нулевая
  тишина — вырожденные craш-тесты с полом −100 дБ, у микрофона таких не
  бывает; у них мягкий порог 0.97.
- Сквозная сверка (`clap_parity_e2e.py`, demo.wav): привратник |Δ|max 0.0002
  (0.695/0.256/0.033/0.016 против серверных 0.696/0.256/0.033/0.016), эмбеддинг
  cos 0.999992, головы: fault 0.6362 против 0.6375, exhaust 0.9915 против
  0.9914. Числа совпали и с задокументированными в DIAGNOSIS.md (гейт 0.26,
  вердикт 0.64, выхлоп 0.99).
- Swift-путь на симуляторе (временный `debugProbe` на demo.wav, снят):
  гейт 0.697/0.254/0.033/0.016, fault 0.6351, knock 0.9421, exhaust 0.9914 —
  третий знак, как и обещал fp16.
- Сборка чистая, 107 чистых проверок, типографика 0 ошибок; ресурсы в бандле
  (58М + 4.2М + 504К). Поставлено и запущено на iPhone 15 Pro.
- **Не проверено:** скорость инференса на устройстве (на симуляторе разбор
  ~секунды; замерить на телефоне); настоящий мотор — как и раньше, без
  машины не проверить; ресемплер AVAudioConverter против librosa на записях
  не-48 кГц (микрофон iPhone пишет 48 кГц, ветка почти мёртвая).

**Осталось.** Портировать каскад изоляции (Silero VAD → Core ML) — тогда
появятся segments и на длинных записях с речью станет чище; убрать
`DiagnosisEndpoint`/серверный клиент, когда локальный путь поживёт;
`tools/diagnosis_server.py` оставлен для отладочных сверок.

### 2026-09-05 — Кнопка добавления, фото во весь верх, поиск по номеру

Три просьбы пользователя одним заходом.

**1. «Добавить авто» больше не меняет размер.** Страница добавления
наследовала метрики последней машины: после машины без ТО карточка выходила
92pt, после машины с историей — 148. Теперь `metrics(of:)` в
`CarMainView.swift` отдаёт странице добавления константу (148, история 0);
переходы сглаживает то же смешивание весами.

**2. Свой снимок машины — во весь верх экрана** (референс `IMG_2402.PNG`
пользователя): фото под статус-бар на всю ширину, название с номером на его
нижней кромке, растворение фото в чёрный фон. Шапка `CarMainView` пересобрана
из потока в `ZStack` с офсетами: низ фото, точки и всё, что ниже, стоят на
месте в обоих режимах; различия — числа, которые смешивает `photoMix`
(вес страниц × наличие снимка). Студийный ассет прижат к низу ячейки, чтобы
при смешивании не ездить по вертикали; свой снимок — `scaledToFill` с клипом.
Новые константы — `HeaderLayout`; `photoMix` = 0 воспроизводит прежнюю
раскладку до пикселя.

**Грабля по пути** (записана в TRAPS.md): прокси `visualEffect` меряет рамку
уже после `offset` — пружина фото получила ложную натяжку 104pt и рисовала
ассет в масштабе 1.21 в покое. Точка покоя теперь следует за офсетом режима.

**3. Настоящий поиск по госномеру.** `RSAVehicleLookup` в
`Core/Services/VehicleLookup.swift`: база полисов ОСАГО (НСИС/РСА) через
`api-cloud.ru/api/rsa.php?type=osago&regNumber=…` — по номеру приходят марка
с моделью и VIN (бывает замаскирован — таким его уже отсеивает
`displayVIN`); года, поколения и пробега источник не знает. Токен — в новом
`Core/Services/VehicleLookupKey.swift` (прописан в четырёх местах
`project.pbxproj`, ID `…0077` проверен грепом); пока строка пустая, работает
прежняя заглушка — приём раздела «Карта». Решение «ключ поставщика — только
на сервере» нарушено сознательно по прямой просьбе пользователя, отметка в
DECISIONS.md. Экраны получают поставщика через `VehicleLookupProvider.make()`.

**Как проверено.**

- `./tools/run-pure-checks.sh` — «Все 107 проверок прошли»;
  `check-typography.py` — 0 ошибок; `check-project.py` — «Проблем: 1»
  (давний `IssuesScreen 2.swift` вне сборки); `plutil -lint` pbxproj — OK.
- Сборка — `BUILD SUCCEEDED`, ошибок 0; `cycle detected` в логе — 0.
- Режим своего фото: снимок временно посеян в первую машину через сам код
  приложения (`ImageLoader.encode`, продовый путь `photo != nil`), кадр
  совпал с референсом по композиции; база возвращена из бэкапа, патч снят.
- Страница добавления: временно `carPage = 4` — карточка крупная; патч снят.
- Скролл жив: `.defaultScrollAnchor(.bottom)` в режиме фото показал низ
  истории и тулбар; патч снят.
- Макетный режим не разъехался: попиксельное сравнение финального кадра с
  кадром до правок — **0.17/255**, разошедшихся пикселей 0.096 %.
- Эндпоинт отвечает по документации: `curl …token=test` →
  `{"error":"503","message":"TOKEN_NOT_REGISTERED_IN_THE_SYSTEM"}`; форма
  ошибки без поля `status` учтена в декодере.
- **Не проверено:** запрос с настоящим токеном (токена нет — нужен кабинет
  api-cloud.ru); свайпы карусели между машинами с фото и без — смешивание
  режимов посмотреть пальцем; ветка «номер не найден» на живых данных.

**Осталось.** Получить токен и вписать в `VehicleLookupKey.token`; свайп
карусели в смешанном режиме проверить на устройстве; формы правки фото у
существующей машины нет — снимок задаётся только при добавлении.

**Тем же днём: поставщик для физлица.** Пользователь не юрлицо — Автокод B2B
(договор) отпадает, а у api-cloud для физика жёсткий вход: пополнение картой
от 2000 ₽ и гарантийный платёж 700 ₽/мес при объёме до 2000 запросов.
Добавлен `AvtoVinCodLookup` (avtovincod.ru, метод `gos2vin`): физлиц и
самозанятых пускают по регистрации, от 5.5 ₽ за успешный запрос, ненайденное
бесплатно, без месячных платежей, пополнение картой от 100 ₽ — и данных
больше: VIN, марка, модель, **год выпуска**, мощность. `VehicleLookupKey`
теперь держит два токена (`avtoVinCodToken` предпочтительный,
`apiCloudToken` запасной), выбор — в `VehicleLookupProvider.make()`.
`FoundVehicle.year` показывается строкой «Год выпуска» в шторке
подтверждения; в модель `Car` год пока не пишется — некуда. Проверено:
сборка чистая, 107 чистых проверок, типографика 0 ошибок; эндпоинт отвечает
по документации (`curl` с тестовым токеном → 401 `TOKEN_INVALID`, у нас это
`unavailable`). С настоящим токеном не проверено — токена нет.

**Тем же днём: токен вписан, но в git не попадает.** Пользователь получил
токен AvtoVinCod и вписал его в `VehicleLookupKey.swift`. Живой прогон:
токен принят, сервис ответил «Недостаточно средств. Баланс: 0.00 ₽, цена
запроса: 8.00 ₽» — то есть авторизация работает, осталось пополнить баланс
(цена метода оказалась 8 ₽, а не «от 5.5»). Репозиторий оказался
**публичным** (github.com/Vladislavkotyreb/carapp228), поэтому токен в
коммиты не идёт: на файле выставлен `git update-index --skip-worktree` —
git видит закоммиченную версию с пустой строкой, а рабочая копия держит
настоящий токен. Обратная сторона: **любые правки этого файла git теперь не
видит**; чтобы закоммитить изменение кода в нём — сначала
`git update-index --no-skip-worktree`, вынуть токен, закоммитить, вернуть
всё обратно. Отмечено и в CLAUDE.md.

**Тем же днём: живой прогон поиска.** Пользователь пополнил баланс. Curl с
его токеном вернул 200 и полную машину: `А007ОК178` → volkswagen multivan
2013, VIN целиком, мощность дробным числом (в декодер она не входит, и
хорошо). Марка и модель приходят **в нижнем регистре** — добавлена
капитализация `prettyName` («volkswagen multivan» → «Volkswagen Multivan»,
короткие латинские слова — аббревиатурами: gl → GL). Прогон того же запроса
через сам код приложения на симуляторе упёрся в окружение: на маке активен
VPN (utun6), и DNS у процессов симулятора мёртв — `-1003 hostname not
found, Resolved 0 endpoints … from cache` при живом curl на хосте;
перезагрузка симулятора не лечит. Грабля записана в CLAUDE.md. Итого:
контракт и токен проверены живым запросом, сетевой слой приложения до
сервера не добрался из-за VPN — проверять в приложении с выключенным VPN
или на устройстве. На проверку ушло ~3 платных запроса по 8 ₽.

**Тем же днём, по замечанию пользователя: надпись не прыгает.** Название с
номером больше не съезжают на нижнюю кромку фото — стоят сверху на своём
месте в обоих режимах, как во всём интерфейсе; `HeaderLayout.titleDrop`
удалён, в режиме снимка фото уходит под надпись, читаемость держит усиленное
затенение верха (0.65 → 0 на 260pt). Проверено кадром с посеянным снимком
(патч снят, база возвращена из бэкапа) и попиксельным сравнением макетного
режима — те же 0.17/255; `cycle detected` — 0.

### 2026-09-05 — Всё приложение в тёмной теме, акцент кнопок белый

Пользователь показал тёмную редакцию макета (секция `46225:7442`
«раздел „машина"» и тёмные шторки в ней) и попросил перевести приложение в
тёмную тему целиком, по HIG, с оговоркой: **акцент кнопок — белый**, синие
кнопки недоделанного прототипа не берём. Прототип покрывает только раздел
«машина»; онбординг, добавление авто, «Карта», «Ещё» и шторка находок
переведены по тому же лейтмотиву: чёрные экраны, шторки #1C1C1E, тёмные
заливки, белые акценты.

**Что изменилось.**

- **`Core/Design/Theme.swift`** — светлые токены переведены на тёмные варианты
  тех же переменных библиотеки (сняты `get_variable_defs` с тёмной ноды и
  пипеткой с рендеров): `backgroundsPrimary` → чёрный, `labelsPrimary` → белый,
  `labelsTertiary`/`labelsQuaternary` → #EBEBF5 с той же прозрачностью,
  `vibrantControlsPrimary` → #F5F5F5, заливки Fills → тёмные проценты,
  `overlaysDefault` → 48 %, грабберы → #333333. Новые токены: `sheetBackground`
  #1C1C1E, `sheetControl` #2C2C2E (кружок крестика), `segmentPill` #636366,
  `separatorsOnDark` #545458 65 %. **Не тронуты** `separatorsVibrant`,
  `fillsPrimary`, `graysGray2`, `vibrantSecondary` — ими живёт уже выверенная
  чёрная главная. Удалён `mainBackground` #F2F2F7 — светлых разделов больше нет.
- **Кнопки** (`OnboardingButtons.swift`): Glass Prominent — белая капсула с
  чёрным лейблом и чёрным индикатором занятости. `FigmaRowLabel`
  («Добавить фото») — белый вместо Accents/Blue: указание пользователя.
- **Сегментед-контрол** (`FormControls.swift`): пилюля #636366, текст белый
  в обоих состояниях — как у системного тёмного.
- **Шторки** (`CarFoundSheet`, `AddServiceSheet`, `AddServiceChoiceSheet`,
  `AddCarSheet`, шторка находок в `IssuesScreen`, `AddPlaceSheet` в карте):
  поверхность #1C1C1E, крестик — тёмный кружок #2C2C2E с белым глифом,
  галочка — белый круг с чёрным глифом (в прототипе синяя), разделители
  `separatorsOnDark`. Карточка «Добавить фото или PDF» — чистый чёрный с
  кромкой white 10 % (по рендеру 46225:7551). Карточки находок держатся
  заливкой Fills/Tertiary вместо тени.
- **Схема одна на всё приложение**: `.preferredColorScheme(.dark)` переехал из
  `CarMainView` в `RootView` (онбординг и добавление авто идут до главной),
  сняты все точечные развороты — `.light` с `TabView` (бар теперь тёмное
  стекло, как на рендере 46225:7788), с пикера карты, со шторки выбора и со
  шторки находок; парные `.dark` на вкладках стали не нужны и убраны.
- **Онбординг**: декоративные подложки `Dimmerwelcome`/`Dimmersurvey` сняты —
  светлые пятна под светлый фон, на чёрном читались бы как блик.
- **«Карта» и «Ещё»**: заглушки «нет ключа»/«скоро» — на чёрном фоне.

**Как проверено.**

- `./tools/run-pure-checks.sh` — «Все 107 проверок прошли».
- `python3 tools/check-typography.py` — 0 ошибок, 38 предупреждений (старые).
- `python3 tools/check-project.py` — «Проблем: 1», и это давний
  `IssuesScreen 2.swift` вне сборки, не из этой правки.
- Сборка `xcodebuild … build` — `BUILD SUCCEEDED`, предупреждений компилятора 0.
- Симулятор iPhone 17 (iOS 26): кадры главной, онбординга (сброс
  `hasCompletedOnboarding` через `defaults write`) и «Добавь авто» (временно
  убран `default.store`, затем возвращён) — все тёмные, статус-бар светлый,
  белые кнопки. `log show … | grep -ci "cycle detected"` → 0.
- Скролл жив: временный `.defaultScrollAnchor(.bottom)` показал низ страницы
  с историей ТО и тёмный тулбар прокрутки; якорь убран, пересобрано.
- **Не проверено** (жесты недоступны): открытие шторок пальцем, свайпы,
  клавиатура поверх тёмных форм. Попиксельной сверки с тёмной секцией не
  было — прототип сам «недоделан», сверять пока не с чем.

**Осталось.** Тёмных нод на онбординг, «Карту», «Ещё» и шторку находок нет —
когда дизайнер дорисует, пройтись сверкой; `Dimmer*`-ассеты остались в
каталоге без использования.

### 2026-09-02 — Главная под новую редакцию макета: чёрный экран

Дизайнер переписал главную **в тех же нодах**, по которым экран уже сверялся
попиксельно (`45854:3547`, `45867:3007`), и добавил третью — `46220:6795`
«Добавьте новый авто». Рендеры перекачаны: старые описывали прежний дизайн, и
сверка с ними подтвердила бы совпадение с тем, чего уже нет.

**Что изменилось.**

- **Фон.** Градиент чёрный → #F2F2F7 убран целиком вместе с `gradientLayer`,
  запасом на оттягивание и токенами `Figma.mainGradient` / `mainGradientHeight`.
  Страница и подложка вкладки — чёрные.
- **Карточки.** Один рецепт `darkGlassCard` на весь экран: заливка #1A1A1A,
  затемнение сверху до 18/255 на 28pt, подсветка снизу, кромка 0.5pt. Профиль
  снят колонкой пикселей с рендера, а не из свойств ноды. Плитки счётчиков и
  сводка стали капсулами, карточки записей — тёмными.
- **Шапка.** Заголовок 26pt Heavy с градиентной заливкой по всей ширине блока,
  зазор до номера 16 → 6, номер 28pt высотой на узком SF Pro, фото 370×245.9
  во всю ширину.
- **История.** Две плитки сводки сложились в одну карточку с колонками
  «Количество ТО» и «Всего потрачено»; дата 16pt, зазор до карточки 16 → 8,
  между группами 32 → 24. Высота блока пересчитана: `176 + 153·n`.
- **Тулбар.** Кнопки на тёмном стекле, заголовок всегда белый. Переключение
  цвета по светлому низу (`isOverLightContent`, порог 537) удалено из
  `ToolbarVisibility` — светлого низа у страницы больше нет.
- **Цена авто** перестала быть заглушкой: `Car.price` (необязательное, чтобы
  облегчённая миграция SwiftData прошла молча), ввод в обеих формах добавления
  и правка тапом по плитке через системный алерт (`CarSheet.priceEdit`).
  Пустая цена показывается прочерком.
- **Ассет `CarPhoto` переснят** из макета: прежний был кропом того же рендера
  в другой рамке, и машина выходила мельче макетной.

**Файлы.** `Features/Main/CarMainView.swift`, `Models/Car.swift`,
`Features/Main/AddCarSheet.swift`, `Features/AddCar/AddCarView.swift`,
`Core/Pure/UIState.swift`, `Core/Design/Theme.swift`,
`Core/Design/Components/FloatingTabBar.swift`,
`Resources/Assets.xcassets/CarPhoto.imageset/car.png`.

**Как проверено.** Симулятор iPhone 17 Pro (`79420072`), перезагружен перед
съёмкой; посев машины с двумя записями ТО, стартовая страница карусели и якорь
прокрутки — временные правки, снятые после замеров.

- `xcodebuild … build` → `BUILD SUCCEEDED`, предупреждений нет.
- Консоль запуска: `grep -ci "cycle detected"` → `0`.
- Попиксельная сверка с перекачанными рендерами (`diffimg`, кадр приведён
  к 402×874), **без статус-бара и таббара**:
  главная **3.3/255**, «Добавьте новый авто» **1.2/255**, машина без ТО
  **2.3/255**, прокрученная история **5.5/255**. Таббар считается отдельно и
  по-прежнему даёт свои ~113 на своей полосе.
- Замеры строк совпали с макетом: заголовок `y 117…136, x 41…361` в обоих;
  карточка «ТО через» `536…681`; плитки `700…793`; шаг групп истории 153pt;
  «История обслуживания» `x 75…326` в обоих.
- Оставшиеся расхождения — данные посева («5 700 км» против «10 000 км»,
  свои суммы и даты) и одна строка на кромке карточки.
- `./tools/run-pure-checks.sh` → «Все 107 проверок прошли».
- `python3 tools/check-typography.py` → «Ошибок: 0, предупреждений: 38».
- `python3 tools/check-taps.py` → «у всех кнопок с фоном объявлен contentShape».
- `python3 tools/check-project.py` → «Проблем: 1» — прежний осиротевший
  `IssuesScreen 2.swift`, к этой работе отношения не имеет.

**Что кадры поймали, а чтение кода — нет** (все пять переехали в `TRAPS.md`).

1. `figmaLineHeight` у однострочного текста уводит его вниз и режет бокс:
   дата 25pt выходила 22pt, и каждая следующая группа истории поднималась на
   3pt относительно макета.
2. Трекинг из ноды до рендера не доходит: с объявленным `−0.45` подписи были
   на 3–5pt уже макета, без него сходятся в пиксель. Заголовку, наоборот,
   понадобился `−0.23`, которого в ноде нет.
3. Концевой пробел в строке макета («10 000 км ») сдвигает центрированный
   текст на 3pt влево.
4. `foregroundStyle(градиент)` тянет шкалу по буквам, а не по рамке — левый
   край названия был темнее макетного. Лечится маской.
5. `scaledToFill` обрезает широкий кадр по бокам: машине срезало нос и корму.

**Что осталось.**

- **Жесты проверяет человек.** Свайп карусели, тап по плитке цены, контекстное
  меню записи и прокрутка собраны и компилируются, но пальцем не проверялись:
  тапать по симулятору нечем.
- **Порядок работ в карточке ТО не гарантирован.** SwiftData не хранит порядок
  элементов связи, и перечисление может собраться не так, как его вводили.
  Дефект не этой работы, но виден именно здесь — записан в `STATE.md`.
- Поле «Цена авто, ₽» в формах добавления — отступление от их нод: в макете
  форм его нет. Появилось по решению пользователя вместе с настоящей ценой.

### 2026-09-01 — Избранное на карте и переключатель «Карта / Список»

Найденную у Яндекса организацию теперь можно добавить в избранное — звездой
с карточки на карте или из списка, — а сами места показываются двумя
подачами, которые переключает системный сегментед-контрол.

**Заодно выяснилось главное: ключ MapKit больше не отвергается.** Тайлы
рисуются, поиск по видимой области возвращает организации, свои точки видны
поверх них. Всё, что в [`MAP.md`](MAP.md) и [`STATE.md`](STATE.md) значилось
как «нельзя проверить до валидного ключа», проверено — кроме жестов.
Документы поправлены.

**Что изменилось.**

- `Core/Pure/MapGeo.swift` — новый. `GeoPoint`, расстояние гаверсинусом,
  подпись «350 м / 1,2 км / 15 км» с неразрывным пробелом и ответ на вопрос
  «это одно и то же место?». Последнее не украшение: добавленный в избранное
  бизнес приходит из поиска ещё раз, и без сверки он вставал бы второй точкой
  поверх себя же и второй строкой в списке.
- `Models/Place.swift` — `PlaceOrigin` (`mine` / `favorite`) и поле
  `originRaw`. Поле **необязательное**: у мест, сохранённых до этой правки,
  его в базе нет, а облегчённая миграция SwiftData добавляет молча только то,
  что может оставить пустым. `nil` читается как `.mine` — ими те места и были.
- `Core/Pure/UIState.swift` — `MapMode` (подача) и две новые фазы `MapPhase`
  (`list`, `listEmpty`) с чистой `of(mode:hasKey:hasPlaces:)`. Экран берёт
  фазу из неё, а не разбирает условия в вёрстке.
- `Features/Map/PlacesList.swift` — новый. Системный `List`: разделы
  «Избранное», «Мои точки», «Рядом», расстояние в строке, звезда с целью
  касания 44 и свайп-действия.
- `Features/Map/MapScreen.swift` — сегментед-контрол и чипы уехали в
  `safeAreaInset(edge: .top)`, поэтому список сам получает отступ сверху и
  уезжает под шапку. Звезда добавлена в карточку места.
- `Features/Map/MapController.swift` — `here` стало наблюдаемым (от него
  зависят расстояния в списке), `nearby` вычитает из найденного сохранённое,
  `show(_:)` подводит камеру к точке из списка, `refresh()` ждёт конца поиска
  для «потянуть, чтобы обновить».
- `Core/Design/Theme.swift` — `accentsYellow` (#FFCC00, системный жёлтый:
  раздела в макете нет) и `tabBarHeight`; `FloatingTabBar` берёт высоту оттуда.
- `tools/check-taps.py` — пропускает кнопки внутри `.swipeActions`: там
  раскладку и зону касания даёт система, кнопка занимает всю высоту строки.

**Почему так.**

- **Карта не пересоздаётся при переключении подачи.** Список лежит поверх
  живой карты, а не вместо неё: пересоздание `YMKMapView` заново поднимало бы
  слой пользователя, слушателей и сессии — и теряло бы место, на которое
  человек смотрел.
- **«Убрать из избранного» — это удалить запись,** а не погасить флаг. Место
  никуда не делось и вернётся в раздел «Рядом» следующим же поиском.
- **Свои точки и избранное — разные разделы, а не один флаг `isFavorite`.**
  «Своя точка, которая не в избранном» и «избранное, которое не своё» — разные
  вещи, и у них разные действия: удалить против убрать.

**Как проверено.** Симулятор iPhone 17 Pro (`79420072`), Debug, временный
посев машины и трёх мест, геопозиция `55.7539,37.6208` (Красная площадь);
посев, стартовая вкладка и стартовая подача — временные правки, снятые после
съёмки.

- `xcodebuild … build` → `BUILD SUCCEEDED`, предупреждений нет.
- Кадр раздела: тайлы Яндекса, найденные парковки/шиномонтаж/СТО точками,
  свои места — заливкой. Сегментед-контрол наверху, чипы под ним.
- Кадр списка: «Избранное» — «Шиномонтаж 24» 380 м и «Автосервис „Гараж“»
  710 м с закрашенными звёздами, «Мои точки» — «Стоянка у дома» 870 м без
  звезды, «Рядом» — найденное с пустыми звёздами, 180 м / 180 м / 200 м.
  Внутри разделов ближнее сверху.
- Кадр карточки: у выбранной «Подземной парковки» пустая звезда рядом с
  крестиком.
- Консоль запуска: `grep -ci "cycle detected"` → `0`.
- `./tools/run-pure-checks.sh` → «Все 107 проверок прошли» (было 78; из новых
  20 на `MapGeo` и 8 на `MapPhase.of`).
- `python3 tools/check-typography.py` → «Ошибок: 0, предупреждений: 38».
- `python3 tools/check-taps.py` → «у всех кнопок с фоном объявлен contentShape».
- `python3 tools/check-project.py` → «Проблем: 1» — прежний осиротевший
  `IssuesScreen 2.swift`, к этой работе отношения не имеет.

**Что кадры поймали, а чтение кода — нет.**

1. **Сегментед-контрол над картой был нечитаем.** Системный контрол
   полупрозрачен — он рассчитан лежать на фоне экрана, а не на карте, и его
   подписи сталкивались с названиями улиц. Под него подложено то же стекло,
   что под чипами.
2. **Подпись расстояния разваливалась в колонку по одному символу.**
   Приоритет раскладки отдан названию, и `HStack` начал сжимать «710 м».
   Лечится `fixedSize()` — подпись короткая и сжиматься не должна вовсе.

**Что осталось.**

- **Жесты проверяет человек.** Тапнуть по симулятору нечем: тап по строке
  («показать на карте»), звезда, свайп-действия и «потянуть, чтобы обновить»
  собраны и компилируются, но пальцем не проверялись.
- **Замер «скролл жив» не удался.** Временный `.defaultScrollAnchor(.bottom)`
  список не сдвинул: на первом кадре в нём только сохранённые места, они
  влезают целиком, а найденные приходят позже. Своего состояния прокрутки
  список не держит вовсе — ломаться там нечему, — но проверкой это не назвать.
- Длинные названия в строке обрываются раньше, чем могли бы: на название
  остаётся около 165pt из 402. Терпимо и как в системных «Картах», но если
  мешает — сокращать звезду или переносить расстояние под название.

### 2026-08-24 — нижняя кнопка «Добавить ТО» убрана

Действие осталось в тулбаре; рядом друг с другом две одинаковые кнопки
читались дублем. Снята кнопка из `historyCard()`, вместе с ней ушёл внешний
`VStack(spacing: 20)` и последнее применение `ContentAreaStyle` в приложении
(стиль остаётся в `OnboardingButtons.swift`).

Высота блока истории пересчитана: 234 → **164** + список, потому что из базы
ушли 20 отступа и 50 кнопки. Не поправить её значило бы оставить под списком
70pt пустоты — блок обрезан по `.clipped()`, щель была бы видна.

**Файлы.** `Features/Main/CarMainView.swift`.

**Как проверено.** Симулятор iPhone 17 Pro, посев одной записи и якорь
прокрутки вниз — временными правками, снятыми после замера.

- Кадр низа страницы: карточка записи целая, щели под ней нет, фон
  `242,242,247` ровно от y 460 до низа.
- `python3 tools/check-taps.py` → «у всех кнопок с фоном объявлен contentShape».
- `python3 tools/check-typography.py` → «Ошибок: 0, предупреждений: 35».
- `xcodebuild … build` → `BUILD SUCCEEDED` без предупреждений.

**Что осталось.**

- **Побочный эффект, о котором надо знать.** В покое с уже заведёнными
  записями «Добавить ТО» теперь не видно вовсе: тулбар появляется только
  после 48pt прокрутки, а тёмная карточка в этом состоянии показывает
  «ТО через» и на нажатие не отвечает. Пустая база не страдает — там карточка
  сама и есть кнопка добавления.

### 2026-08-24 — три поломки визуала от вчерашнего тулбара

Кадр с устройства показал то, чего не показали мои замеры: белый шов поперёк
страницы, размытая карточка «ТО через» под баром и тёмные кольца вокруг кнопок
тулбара. Все три — следствия перехода на `NavigationStack`, и все три я
пропустил, потому что мерил опорные строки и геометрию, а фон и материал не
проверял вовсе.

**1. Белый низ и шов.** `NavigationStack` подкладывает непрозрачный
`systemBackground`. Схему стека мы нарочно оставили светлой — иначе стекло
тулбара выходит чёрным, — поэтому подложка стала **чистой белой** и вылезала
ниже контента страницы, давая ступеньку 242 → 255 поперёк экрана. Замер по
кадру устройства: `(241,242,246)` на y 796 и `(255,255,255)` на y 800.
Лечится своим фоном на содержимом стека: `.background(Figma.mainBackground)`.

**2. Размытие под баром.** Краевой эффект прокрутки iOS 26 размазывал
карточку «ТО через» на треть экрана. В макете под тулбаром контент чёткий →
`.scrollEdgeEffectHidden(true, for: .top)`.

**3. Тёмные кольца вокруг кнопок.** Системная подложка элемента бара рисует
своё стекло **под** нашей белой капсулой; над чёрной карточкой она темнеет и
читается кольцом. Гасится `.sharedBackgroundVisibility(.hidden)` — модификатор
`ToolbarContent`, не `View`, и требует `@available(iOS 26.0, *)` на
`carToolbar`.

**Файлы.** `Features/Main/CarMainView.swift`.

**Как проверено.** Симулятор iPhone 17 Pro, посев одной записи и якорь
прокрутки — временными правками, снятыми после замеров.

- Фон по левому полю, прокрутка ~130: от `(8,8,8)` до `(242,242,247)` без
  единой ступеньки; на самом низу `242,242,247` на всём протяжении
  y 400…760. Прежде там было `255,255,255` начиная с y 275.
- Кадр в состоянии пользователя: колец вокруг кнопок нет, фото машины под
  баром чёткое.
- `python3 tools/check-taps.py` → «у всех кнопок с фоном объявлен contentShape».
- `python3 tools/check-project.py` → «Проблем: 1» — прежний `IssuesScreen 2.swift`.
- `xcodebuild … build` → `BUILD SUCCEEDED` без предупреждений.

**Что осталось.**

- **Размен, о котором надо знать.** Краевой эффект — это ровно то, чем iOS
  разводит контент и прозрачный бар. Убрав его, мы получили момент, когда
  название в тулбаре наезжает на текст карточки «ТО через» — видно на кадре
  самой глубокой прокрутки. Вернуть можно `.scrollEdgeEffectStyle(.hard,
  for: .top)`: жёсткая кромка вместо размазанной. Пока оставлено по макету.
- На главной две кнопки «Добавить ТО» разом — в тулбаре и внизу истории.
  В макете `45867:3007` нижняя есть, но рядом с тулбарной выглядит дублем;
  вопрос к дизайнеру.
- Проверял снова только симулятором: жесты и force touch на устройстве — за
  человеком.

### 2026-08-24 — тулбар, градиент и список записей ТО

Три макета сразу: тулбар `46165:3132`, градиент главной `45879:3002`,
новое расположение записей `45867:3007`. Force touch по записи сохранён.

**Градиент.** Стопы разошлись, менять было что. Сняты колонкой пикселей с
рендера (нода — два прямоугольника внахлёст с матричными преобразованиями,
читать её значения бесполезно), 15 стопов, `mainGradientHeight` 656 → 800.
Сверка по колонке, макет против кадра:
`300:0 360:16 420:35 480:53 540:72 600:112 660:185 720:233 780:241` против
`300:0 360:16 420:35 480:51 540:62 600:109 660:177 720:233 780:242` —
наибольшее расхождение 10 из 255 против прежних 21.3.

**Записи ТО.** Горизонтальная лента 230×84 заменена вертикальным списком:
дата заголовком 20pt Semibold, карточка 370×96 со стеклом, внутри сумма
15pt Semibold и работы 13pt `#727272` в две строки. `contextMenu`
(«Изменить»/«Удалить») перенесён вместе с превью, размер превью приведён к
370×96 — иначе возвращается сжатие-отскок не по той геометрии. Вместе с
лентой ушёл `shadowInset` и компенсация отступов, нужные только ей.

**Тулбар.** Своя чёрная полоса `scrollHeader` снята, вместо неё системный
`.toolbar` в `NavigationStack`: «…» с меню слева, название по центру-слева,
«Добавить ТО» справа. Кнопка «Удалить авто» внизу страницы убрана — действие
живёт только в контекстном меню, по HIG деструктивное действие не дублируется.
Подтверждение прежнее (`sheet = .deleteConfirm`).

**Четыре дефекта, найденные замером, а не чтением кода:**

1. **`ToolbarVisibility` лежал в `@State`** — по образцу `TabBarState`. Но
   `TabBarState` читает подписанный на него `FloatingTabBar`, а `isVisible`
   читает сам `body` экрана. Ссылка без подписки экран не перерисовывает, и
   тулбар не появлялся ни при какой прокрутке. Стал `@StateObject`; прокрутку
   это не ломает — оба флага меняются только на переходе через порог.
2. **Стекло кнопок бара было чёрным, текст на нём — чёрным.** Системное
   стекло берёт схему у навигационной панели, а не у своего элемента, и
   объявленный внутри `.environment(\.colorScheme, .dark)` бар выходил
   невидимым. Содержимое обёрнуто в `ZStack`, тёмная схема надета на него, а
   тулбар объявлен снаружи. Заливка кнопок — своя, тонированная белым: так же,
   как в `AddServiceChoiceSheet`, потому что настоящее Liquid Glass над
   чёрным фото темнеет, а в макете капсула светлая.
3. **Белый заголовок пропадал на светлом низу страницы.** Считать это система
   не может: страница целиком `ignoresSafeArea`, бар её прокрутки не видит и
   краевого эффекта не рисует. Добавлен второй порог `isOverLightContent`
   (537 = 620 − 83, из замера градиента), по нему цвет переключается.
4. **Высота истории оставалась константой 350.** Для горизонтальной ленты она
   от числа записей не зависела, для списка зависит — всё, кроме первой
   записи, уходило под `.clipped()`. Стала считаться из тех же токенов:
   234 + n·137 + (n−1)·32.

Заодно: `onAppear` читателя смещения синхронизировал только `scroll.offset`,
но не пороги; висячие `.opacity`/`.allowsHitTesting` на `Spacer`, оставшиеся
от снятой кнопки «Удалить авто», убраны.

**Файлы.** `Features/Main/CarMainView.swift`, `Core/Design/Theme.swift`,
`Core/Design/Components/FloatingTabBar.swift`.

**Как проверено.** Симулятор iPhone 17 Pro (`66CA609D`), Debug, посев машины
с двумя записями и `.defaultScrollAnchor(.bottom)` временными правками,
снятыми после замеров.

- Тулбар против рендера `46165:3132`, полоса y 62…105:
  приложение `x 20…63 (w 44)` / `71…137` + `147…178` / `243…381 (w 139)`
  против макета `16…59 (w 44)` / `73…141` + `151…183` / `247…385 (w 139)`.
  Ширины совпали точно, вертикальный центр — 83.5 у обоих.
  **Не совпало и не совпадёт:** поля бара 20 против 16 и высота стекла
  36 против 44 — это метрики системного навигационного бара. Отрицательный
  отступ капсулу не сдвигает, а обрезает (проверено: 40 и 135 вместо 44 и 139),
  поэтому оставлены системные.
- Карточка записи: ширина по колонкам `16…385` = 370, высота по профилю
  яркости `358…452` = 95, шаг групп 322 → 491 = 169 (137 + 32), дата → карточка 16.
- Опорные строки покоя после перехода на `NavigationStack` — главный риск
  плана — не сдвинулись: `109…127 x 44…357`, `160…172 x 148…254`,
  `234…329 x 60…338`.
- `python3 tools/check-taps.py` → «у всех кнопок с фоном объявлен contentShape».
- `python3 tools/check-typography.py` → «Ошибок: 0, предупреждений: 35».
- `python3 tools/check-project.py` → «Проблем: 1» — прежний осиротевший
  `IssuesScreen 2.swift`, к этой работе отношения не имеет.
- `./tools/run-pure-checks.sh` → «Все 78 проверок прошли».
- `xcodebuild … build` → `BUILD SUCCEEDED`, предупреждений нет.

**Что осталось.**

- **Force touch по записи проверяет человек.** Тапнуть по симулятору нечем:
  macOS не даёт `osascript` доступ в «Универсальном доступе». Код меню и
  превью перенесён целиком, но пальцем он здесь не проверялся.
- Промежуточное состояние прокрутки (белый заголовок над тёмным верхом)
  снято только при принудительно включённом тулбаре — там же, где мерился
  макет. Настоящим жестом не проверялось по той же причине.
- Порог 537 привязан к замеру градиента: поменяется `mainGradient` —
  поедет и точка смены цвета заголовка.

### 2026-08-22 — зона касания у всех кнопок, стекло стало видно

**Причина одна на все три жалобы, и прошлая правка её не закрыла.**

В SwiftUI зона касания кнопки берётся от **содержимого метки**, а не от
оформления. `.background()` и `.liquidGlass()` рисуют фон, но цель нажатия не
расширяют: нажимаются буквы, а залитая капсула вокруг них — нет. Объявляет зону
только `.contentShape()`.

В прошлый раз я добавил его **внутрь новых стилей** и починил ровно те кнопки,
что через эти стили проходят. Замер показал масштаб: **14 кнопок из 28** —
ровно половина приложения. Названные пользователем места были из их числа.

**Стекло не видно из-за тона.** `liquidGlass()` вызывает системный
`glassEffect`, но под непрозрачной чёрной заливкой его не видно вовсе. Тон
приглушён до 0.86 — материал проступил, цвет остался.

**Системную кнопку iOS 26 попробовал и отверг — по замеру, не по вкусу.**
Макет объявляет высоту 54, а `.buttonStyle(.glassProminent)` даёт:

| Вариант | Высота |
|---|---|
| без ограничений, `.buttonSizing(.flexible)` | 67pt |
| `.controlSize(.large)` | 49pt |
| `.controlSize(.extraLarge)` | 49pt (то же) |
| внешняя рамка 54 | стекло сжимается до **33pt** |

Ни одно значение в 54 не попадает, а рамка снаружи ломает сам материал: стиль
кладёт отступы поверх метки и рисует стекло по своему размеру. Решение принял
пользователь: оставить свою капсулу 54 ради совпадения с макетом.

**Про Code Connect в MCP Figma.** Недоступен на этом тарифе («Dev or Full seat
on an Organization or Enterprise plan»). И он не то, чем кажется: он не
генерирует SwiftUI, а связывает ноду с **уже написанным** компонентом, чтобы
будущие скрейпы возвращали наш код. `get_design_context` отдаёт React+Tailwind
и просит переложить самому.

**Чтобы не вернулось — `tools/check-taps.py`.** Дефект невидим при чтении кода:
`.liquidGlass(in: Capsule())` выглядит рабочим фоном, и на экране кнопка
правильная. Ловится только пальцем. Проверка ищет `Button`, у которого есть фон
и нет `contentShape`; системные меню, алерты и шторки пропускает.

**Как проверено.**

```
python3 tools/check-taps.py   → было 14 нарушений, стало 0
```

Проверка ловит регресс: снял `contentShape` у карточки выбора — нашла её и
назвала файл со строкой. Геометрия не поехала: главный экран `109…127`,
`161…172`, `234…316`; кнопки шторки `733…784` против эталонных `732…785`.

**Не проверено:** сам тап по краю карточки. Тапать по симулятору нечем —
macOS не даёт `osascript` доступ в «Универсальный доступ». Что зона объявлена,
видно по коду и проверке; что палец попадает — проверит пользователь.

**Осталось:** в репозитории лежит `Features/Issues/IssuesScreen 2.swift`, 25 КБ,
в git есть, в `project.pbxproj` нет — не собирается. Похоже на копию от iCloud.

### 2026-08-22 — кнопки на своих стилях, зоны касания по HIG

**Что.** `Core/Design/Components/OnboardingButtons.swift`,
`Features/Main/CarMainView.swift`, `Features/Map/MapScreen.swift`.

**Главное, что нашлось попутно: у всех кнопок стояла `.buttonStyle(.plain)`.**
Она гасит не только системный вид, но и **любую реакцию на палец** — ни
подсветки, ни сжатия. Кнопки выглядели нарисованными и ощущались мёртвыми,
причём в коде это не бросается в глаза: `.plain` читается как «без лишнего
оформления», а на деле снимает и отклик.

Теперь у каждой свой `ButtonStyle` — `ProminentCapsuleStyle`,
`PlainCapsuleStyle`, `ContentAreaStyle`. Подложка, нажатое состояние и цель
касания стали свойствами кнопки, а не текста внутри неё; раньше `.background()`
висел на лейбле, и нажималась только надпись.

Соответствие макету: «Button - Liquid Glass - Text» (`45824:2607`) — 370×54,
капсула; «Button - Content Area» (`45883:4144`) — 370×50. Раскладка осталась
своя: системный `.glassProminent` назначает лейблу собственные отступы, а
геометрия снята с макета до точки.

**Зоны касания.** HIG требует 44×44. Были меньше:

| Что | Было | Стало |
|---|---|---|
| Крестик карточки места | 28×28 | 44×44, отрицательным отступом, чтобы не раздвинуть карточку |
| «Убрать маршрут», «Удалить место» | высота строки, ~20pt | 44 |
| Чипы фильтров на карте | ~35pt | 44 (капсула стала заметно выше) |

Уже были правильными и не тронуты: круглые кнопки в форме ТО и удаление фото —
у них 44×44 с `contentShape` поверх меньшего рисунка.

**Как проверено.** Опорные строки главного экрана не поехали: `109…127`,
`161…172`, `234…316` — как в эталоне, один пиксель разницы на нижней границе
блока. Кнопки шторки `732…784` против эталонных `732…785`. Чипы карты и обе
кнопки шторки осмотрены кадром.

**Что осталось.** В репозитории лежит `Features/Issues/IssuesScreen 2.swift` —
25 КБ, отслеживается git, в `project.pbxproj` не входит, то есть не собирается.
Похоже на копию от файлового провайдера iCloud. Сам не удаляю — чужой файл.

### 2026-08-22 — файл передачи сессии

**Что.** [HANDOFF.md](HANDOFF.md) — состояние на конец недели для входа в новую
сессию: что проверено, что нет, обо что спотыкались, чем меряем.

**Зачем.** Контекст сессии подошёл к пределу. Постоянные знания уже разложены
по STATE/TRAPS/DECISIONS, но осталось то, что иначе выясняется заново:
сменившийся IP мака, запущенный сервер разбора, эталонные числа шара, список
непроверенного.

**Замечено при сборке файла:** IP мака сменился снова (141 → 117), а в коде
лежит старый. Третий раз за неделю — адрес просится в настройки, а не в
константу.

### 2026-08-22 — шар не реагировал на звук: две ошибки, обе мои

**Симптом.** Шар выглядит живым, но на звук не отвечает.

**Причина первая — арифметика.** Я поднял `idleHigh` до **1.0**, догоняя
живость референса. Это значит, что на пиках дыхания шар **уже стоял на
максимуме**, и звук поверх не мог добавить ничего: `max(idle, level)` при
idle = 1.0 всегда равен единице. В промежутках требовался уровень выше 0.36.
Урок общий: у любого фонового движения должен оставаться запас сверху, иначе
полезный сигнал в него не помещается.

**Причина вторая — шкала громкости была абсолютной.** Замер с микрофона в
обычной комнате: **фон 0.40–0.56, речь 0.68–0.74**. То есть фон съедал половину
шкалы, а после усиления и фон, и речь упирались в потолок — разницы на экране
не оставалось. `normalizedLevel` линейно тянет −50…0 дБ в 0…1, и для комнаты
это не годится.

**Что сделано.** `Core/Services/AudioLevelMeter.swift` считает **превышение над
фоном**, а не абсолютную громкость. Фон отслеживается сам: вниз быстро (стало
тише — это новый фон), вверх еле-еле, около тридцати секунд. Медленный подъём
обязателен: мотор звучит ровно, и при быстром отслеживании он целиком ушёл бы
в фон за время записи, а шар снова замер бы. Побочная польза — шар одинаково
работает и в тихой комнате, и в гараже.

`SoundOrb.idleHigh` опущен до 0.52: дыхание живёт ниже коридора речи. Живость в
тишине держит не размах, а скорость набухания, она осталась прежней.

**Как проверено.** Обе половины по отдельности, потому что звук `say` доходит до
микрофона симулятора рывками и сквозной тест недостоверен.

Микрофон: `тишина → сырой 0.000, фон 0.005, итог 0.000`;
`речь → сырой 0.335, превышение 0.324, итог 0.999`.

Отрисовка, при подставленном уровне:

```
уровень 0.0 → толщина 17–30 % высоты пузыря
уровень 0.9 → толщина 26–72 %
```

**Не проверено:** сквозной путь на живом моторе. Оба конца работают, но вместе
на настоящем звуке их проверит только пользователь.

### 2026-08-21 — макеты новых состояний перенесены в Figma

**Что.** Новая секция «раздел «ошибки»: пустые состояния и карта» (`46147:2413`)
на странице «4. », под существующей секцией «раздел «ошибки»». Три кадра
402×874: два пустых состояния «Двигателя не слышно» (речь и тишина) и раздел
«Карта» с реальными точками. Существующие ноды не тронуты.

**Грабли, стоившие двух заходов: «SF Pro» в этой среде отдаёт нулевую ширину.**
`listAvailableFontsAsync()` его возвращает, `loadFontAsync` не бросает ошибку,
текст создаётся без нареканий — а рендерится пустым. Диагноз ставится одной
пробой: текстовая нода в SF Pro меряется `0×15`, та же в Inter — `275×39`.
Существующие макеты в файле набраны SF Pro и рисуются нормально: они сделаны в
другой среде, где шрифт настоящий. Макеты набраны Inter, оговорка положена
прямо в секцию — иначе выглядит как отступление от гайда.

**Второе:** жёсткие переносы строк из кода в Figma не переносятся. Они
рассчитаны на метрики SF Pro, у Inter строка рвётся в другом месте. Перенос
отдан движку, ширина блока задана явно.

**Как проверено.** Каждый кадр запрошен обратно через `screenshot()` и осмотрен:
текст на месте, вёрстка совпадает со сборкой, на карте видны парковки,
шиномонтаж и СТО.

**Что осталось.** Карта в макете — растровый снимок сборки: карта по своей
природе растр, рисовать её слоями смысла нет. Два пустых состояния — живые
слои с настоящим текстом.

### 2026-08-21 — живой шар, объяснение отказа, пустое состояние; карта заработала

**Шар перестал быть вялым — и «вяло» удалось измерить.** Толщина ленты в
средней колонке по кадрам через 1/30 с, в процентах высоты пузыря:

```
референс:  21 26 30 34 37 42 44 46 47 45 43 42 36 31 26 21 16 13 16 21   (13→49, полный цикл)
было:      35 36 36 37 37 37 37 38 37 36 35 34 32 30 28 26 24 23 22 21   (21→38, сползание)
стало:     60 62 62 58 51 43 34 27 26 30 31 32 33 30 28 33 39 45 50 53   (26→62, полный цикл)
```

Размах 36 пунктов против 36 у референса, цикл укладывается в те же 0.67 с.
Разница между кадрами через 1/6 с: **26.5–27.8** при референсных 24–29 (было
14–22).

Правки в `Features/Issues/SoundOrb.swift`: набухание ускорено в 2.4 раза
(период 1.6 с → 0.65 с), размах удвоен, огибающая дыхания возведена в степень
0.62 — подъём круче спада, как у голоса. Симметричная синусоида и читалась
сползанием.

**И отдельно `Core/Services/AudioLevelMeter.swift`:** отпускание 0.12 → 0.3.
Оно гасило шар на живой записи сильнее любых правок отрисовки: лента не
успевала опасть между звуками.

**Экран говорит, что услышал.** Сервер отдаёт короткий код `heard`
(`tools/diagnosis_server.py`) — не строки промптов, чтобы приложение не хранило
их копию и однажды не разошлось. Проверено: музыка → `music` 0.99, тишина →
`silence`, мотор → `engine` 0.87.

Найденный по дороге случай: когда «мотор» побеждает, но не дотягивает до порога
(синтезированная речь дала 0.48), текст «мотора не слышно» был бы неправдой —
для него своя формулировка про «слышно слабо».

**Пустое состояние вместо карточки-заглушки.** Отказ показывал обычную шторку с
кнопками «Да, добавить ошибки» / «Нет, не добавлять» — предложение добавить в
историю то, чего нет. Теперь шторка целиком переходит в пустое состояние: знак,
что услышали вместо мотора, и одна кнопка «Записать ещё раз». Заголовок тоже
меняется — «нашли» там было неправдой.

**Карта заработала: ключ приняли.** Ноль отказов в логе SDK, тайлы грузятся.

И сразу нашёлся дефект, который без рабочего ключа было не увидеть: **поиск
уходил раньше, чем камера доезжала**. Он берёт видимую область, а в момент
первой геопозиции она ещё всемирная — запрос уходил по всему глобусу и
возвращался пустым, без единой ошибки в логе. Выглядело как «точек нет».
Теперь поиск висит на `cameraCallback` перелёта. После правки на карте Москвы
парковки, шиномонтаж и СТО.

**Не проверено:** шар на живой записи с мотора — правка отпускания метра
касается именно её.

### 2026-08-21 — разбор двигателя поднят для живого теста

**Что.** `Core/Services/CarDiagnosis.swift` — адрес сервера
`192.168.1.108` → `192.168.1.141`. `tools/diagnosis_server.py` — строка в лог
на каждый запрос.

**Зачем.** У мака сменился IP, а адрес зашит константой: с телефона раздел
сказал бы «сервер недоступен», и тест бы не начался. Сам сервер при этом
работал с прошлого раза, поднимать было нечего.

Логирование — потому что без него неудачный тест не диагностируется. Записи не
сохраняются, переспросить «а что там было» после теста нельзя. Это касается
главного открытого вопроса: даёт ли **настоящий** мотор оценку выше порога
привратника 0.5 и сегменты > 0. До сих пор все проверки шли на синтетике.

**Как проверено.** Обе ветки лога:

```
[разбор] demo.wav 375 КБ | мотор 0.26 отклонён | музыка=0.70 МОТОР=0.26 речь=0.03 тишина=0.02
[разбор] engine.wav 750 КБ | мотор 0.87 ПРОШЁЛ | музыка=0.03 МОТОР=0.87 речь=0.03 тишина=0.07 | сегментов 0 | вердикт uncertain 0.499 | версия exhaust 0.99
```

Клиентский путь на живом сервере тем же отдельным бинарником: синтетический
мотор → «По этой записи не берусь судить», демо-клип → «Это не похоже на звук
двигателя». `curl http://192.168.1.141:8077/health` отвечает.

**Не проверено:** настоящий мотор. Ради этого всё и делалось — тест за
пользователем.

**Что осталось.** Зашитый адрес ловит нас второй раз. Правильное решение —
поле адреса в разделе «Ещё», который сейчас пустая заглушка.

### 2026-08-21 — раздел «Карта», история «Ошибок» в базе, ключ MapKit не принят

**Что.** Три куска.

1. **Раздел «Карта» целиком** — `Features/Map/MapScreen.swift`,
   `Features/Map/MapController.swift` (новый), `Models/Place.swift` (новый).
   Карта Яндекса во весь экран под таббаром, поиск парковок/шиномонтажа/СТО по
   видимой области, свои точки долгим нажатием, маршрут от своей позиции и
   передача его в приложение Яндекс Карт (`yandexmaps://`, схема объявлена в
   `Resources/Info.plist`). Владение объектами SDK собрано в одном контроллере:
   слушатели MapKit держит слабо, сессии отменяет при потере ссылки.
2. **История «Ошибок» переехала в SwiftData** — `Models/EngineCheck.swift`
   (новый), `Features/Issues/IssuesScreen.swift`. Жила в `@State` и исчезала
   при каждом перезапуске. В базу попадает только подтверждённое; порядок
   находок хранится номером, потому что разбор ранжированный, а связь SwiftData
   порядок не гарантирует.
3. **Инициализация MapKit по документации** — `App/AppMVPApp.swift`: инстанс
   создаётся сразу за ключом плюс `onStart()`, раз инициализация идёт не в
   `didFinishLaunchingWithOptions`.

**Зачем.** Карта была пустым разделом с заглушкой. История «Ошибок» — раздел,
ради которого подключался разбор звука, не помнил ничего.

**Ключ MapKit Яндекс не принимает, и это не наша сторона.** Проверено:
строка в собранном бинарнике совпадает с кабинетом побайтово
(`strings` по `Wheelly.debug.dylib`), продукт у ключа — MapKit SDK, статус
«Активен», SDK 4.42.0 — последняя доступная версия, `setApiKey` выполняется,
хост `proxy.mob.maps.yandex.net` с машины доступен. Ответ сервера:

```
Unexpected server response: Forbidden. Body :Invalid api key
```

Два разных ключа, три отказа из трёх на каждый. Подробности и что смотреть в
кабинете — [`MAP.md`](MAP.md).

**Отдельная находка, стоившая падения всего приложения.** Без `-ObjC` в
`OTHER_LDFLAGS` приложение падало на старте: `+[YMKMapKit setApiKey:]:
unrecognized selector`. Метод объявлен в категории, а категории из статической
библиотеки линковщик выбрасывает. Не всплывало раньше только потому, что ключ
был пустой и ветка не выполнялась — заглушка «пока не настроено» годами
скрывала сломанную сборку.

**Как проверено.**

- История переживает перезапуск: подтвердил находки → `simctl terminate` →
  запуск заново → на экране «Прослушиваний 1, Неисправности 6».
- Пустое состояние на чистой базе с подсаженной машиной: `rowsx` даёт описание
  `458…471 / 482…493 / 503…512` и метку кнопки `743…755` — совпало с эталоном
  до строки; блок «История» не появляется.
- Плашка «Яндекс не принял ключ MapKit» появляется по срабатыванию сторожа —
  раньше на экране оставалась пустая сетка, по которой ничего не понять.
- **Не проверено:** тайлы, результаты поиска, тап по точке, построение маршрута
  и переход в Яндекс Карты. Всё упирается в отказ ключа.

**Что осталось.** Ждём рабочий ключ; правок в коде не потребуется. История
прослушиваний общая, а не привязана к машине — вопрос к дизайну, записан в
[`STATE.md`](STATE.md).

### 2026-08-20 — модалки «Машины» сведены в один слот

**Что.** Пять булевых (`showServiceChoice`, `showAddService`, `showAddCar`,
`showPhotoPicker`, `showDeleteConfirm`) стали одним `CarSheet`. Presentation-API
получают вычисляемый `Binding<Bool>` через `presenting(_:)`. Диф — 36 вставок,
26 удалений, значительная часть из них комментарии и сам биндинг.

**Что осталось за бортом намеренно.** `editingRecord` — это данные о том, что
правят, а не то, какое окно открыто; то же разделение измерений, что
«деятельность против истории» в «Ошибках». Тост, `hidesTabBar`, форма машины,
кэш снимков и состояние жеста — тоже отдельные: они не взаимоисключающие.
`CarPhase` и `ServiceSheet` к коду по-прежнему не привязаны: они зависят от
содержимого формы, а не только от того, что открыто. Строкой в
[`TAILS.md`](TAILS.md).

**Почему это не косметика.** Пять флагов дают 32 сочетания, законных шесть.
Две открытые шторки здесь не абстрактная опасность — в этом же файле уже
ловили, как общий пикер фото открывал чужую модалку. Плюс замена одной шторки
другой была двумя присваиваниями подряд (`showServiceChoice = false;
showPhotoPicker = true`), то есть переходом через промежуточное состояние.
Теперь это один переход.

**Грабля, найденная при проектировании биндинга.** Закрытие обязано сбрасывать
слот **только если закрывают именно эту шторку**. SwiftUI досылает `false`
уходящей уже после того, как открылась следующая, и безусловный сброс гасил бы
её — выглядело бы как «шторка мигнула и закрылась сама». Записано
в [`TRAPS.md`](TRAPS.md), проверено отдельным случаем в прогоне.

**Как проверено.** Тем же приёмом, что «Ошибки»: обе машины — старая на пяти
флагах и новая на слоте — описаны в `tools/pure-checks/main.swift` и прогнаны
на **всех последовательностях длиной 4 из десяти событий, 10 000 штук**, со
сравнением на каждом шаге.

Результат: пока старое состояние законно (открыто не больше одной шторки),
поведение совпадает **полностью**; новая машина не открывает две ни разу;
старая — открывала, и перебор это подтверждает, иначе проверка ничего не
стоила бы. Отдельно проверено, что закрытие чужой шторки не трогает открытую
и что выбор «вручную» заменяет шторку одним переходом.

Плюс: 78 проверок чистого слоя, `swiftc -parse` по 36 файлам,
`tools/check-project.py`, `check-typography.py` — чисто.

**Чего проверка не доказывает.** То же ограничение, что и в прошлый раз:
модели переходов — копии кода, а не импорт. И **что приложение собирается,
не проверено**: полного Xcode на машине нет, проверен синтаксис, но не типы.
Здесь это весомее, чем в «Ошибках»: `presenting(_:)` возвращает `Binding`,
и типы его аргументов компилятор здесь не смотрел.

### 2026-08-20 — «Ошибки» переведены на один тип состояния

**Что.** В `IssuesScreen` два булевых поля (`isRecording`, `isAnalyzing`) стали
одним `IssuesActivity` (`idle` / `recording` / `analyzing`). Читатели остались
на месте вычисляемыми свойствами, поэтому вёрстка не тронута вовсе: диф —
24 вставки и 9 удалений, из них большая часть комментарии. Изменились четыре
присваивания и `onDisappear`.

**Зачем.** «Записываю» и «разбираю» одновременно — состояние, которого не
бывает. Пока это два флага, оно выразимо, и тест на него ничего не даёт: надо,
чтобы багу было негде жить.

**Найденный по дороге баг, ради которого всё и затевалось.** Разбор ставил
`isAnalyzing = true`, а отменённая задача возвращалась по
`guard !Task.isCancelled else { return }` **мимо** сброса флага. Задача
отменяется на `onDisappear`, флаг живёт в `@State`. То есть уйти с раздела во
время разбора значило вернуться к кнопке, отключённой навсегда, с вечным
индикатором. Прерванная запись залипала так же: «Стоп» на остановленном метре.
Лечится не сбросом внутри задачи (вью может уже не быть), а возвратом в покой
там, где прерывают. Записано граблей в [`TRAPS.md`](TRAPS.md).

**Как проверено — и почему именно так.** Собрать приложение на этой машине
нечем, полного Xcode нет. Поэтому равенство поведения доказано перебором:
обе машины состояний, старая на двух флагах и новая на одном значении,
описаны в `tools/pure-checks/main.swift` и прогоняются на **всех
последовательностях событий длиной 6** (3⁶ = 729 штук) в двух конфигурациях —
сервер настроен и нет. На каждом шаге сравниваются ровно те два флага, которые
читает вёрстка. Нажатие во время разбора в перебор не идёт: кнопка отключена
`.disabled(isAnalyzing)`.

Результат: расхождений нет ни одного до ухода с раздела; после ухода расхождение
единственное и намеренное — старая машина остаётся в `analyzing` (тот самый
баг), новая возвращается в покой. Отдельно проверено, что перебор ни разу не
производит сочетание «записываю и разбираю».

Плюс: 71 проверка чистого слоя, `swiftc -parse` по 36 файлам,
`tools/check-project.py`, `check-typography.py` — чисто.

**Чего эта проверка не доказывает.** Модели переходов — копии кода, а не
импорт: `IssuesScreen` живёт в SwiftUI и в чистый прогон не собирается.
Если код переходов правят, а модель нет, перебор будет доказывать равенство
двух старых машин. Это ограничение, а не дефект: снять его можно только вынеся
переходы в `Core/Pure`, что стоит следующей правки.

И главное: **что приложение собирается, по-прежнему не проверено.** Проверен
синтаксис, но не типы. Диф механический и читаемый целиком, но это объяснение,
а не проверка.

**Осталось.** `CarMainView` — двадцать с лишним `@State` в файле на 1454 строки;
`CarPhase` и `ServiceSheet` написаны и никем не используются. Строкой
в [`TAILS.md`](TAILS.md).

### 2026-08-20 — каталог состояний интерфейса

**Что.** `Core/Pure/UIState.swift` — девять типов фаз экранов;
`Core/Pure/UIStateCatalog.swift` — 40 состояний с описанием и нодой макета;
проверки каталога против типов; [`state-gallery.md`](state-gallery.md).
`FieldError` переехал из `AddCarView.swift` в `Core/Pure`.

**Что выяснилось до того, как писать.** Выводить каталог было **не из чего**:
типов состояния в проекте нет. В `IssuesScreen` состояние разложено пятью
булевыми (`isRecording`, `hasHistory`, `showFindings`, `isAnalyzing`,
плюс ошибка) — 32 сочетания, из которых осмысленны восемь. В `CarMainView`
таких `@State` больше двадцати. Единственный тип состояния во всём приложении —
`FieldError` на четыре случая.

Это ровно тот случай, ради которого в исходном приёме список выводится из
типов: собранный руками каталог назавтра разойдётся с приложением, и разойдётся
молча. Поэтому типы фаз написаны **раньше** переписывания вью — на них держится
каталог, а перевод вью на них требует Xcode и идёт отдельным шагом.

**Что каталог сразу показал.**

- **Из 40 состояний 25 не имеют ноды в макете.** Сверять их не с чем.
- **Шесть из семи исходов шторки находок никто не видел на экране**: «сервер
  без модели», «это не мотор», «норма», «не берусь судить», «не удалось
  разобрать» и «неисправность» требуют определённых ответов сервера. Читая
  `IssuesScreen` подряд, о них не вспомнить — они разбросаны по трём функциям.

**Как проверено.** `./tools/run-pure-checks.sh` → 48 проверок, все прошли.
Сам инвариант проверен подстановкой на копии в `/private/tmp`: лишний
`case queued` в `IssuesPhase` даёт `✗ ["issues-queued"]`, лишняя запись
`map-ghost` в каталоге — `✗ ["map-ghost"]`. Проверка, которая никогда не
краснеет, ничего не стоит, поэтому её проверяли отдельно.

Плюс: `swiftc -parse` по всем 36 файлам чист, `plutil -lint` OK,
`tools/check-project.py` — только известный `IssuesScreen 2.swift`,
`check-typography.py` — 0 ошибок.

**Чего нет и почему.** Фикстур, фабрики экранов и экспорта PNG. Всё трое
требуют Xcode и симулятора, а здесь только Command Line Tools. Писать 
фабрику экранов вслепую поверх вью, которые не компилируются на этой машине, —
это отдать код, который никто не видел работающим. Порядок работ расписан
в [`state-gallery.md`](state-gallery.md), включая урок про фикстуры: без них
кадры выйдут одинаковыми, потому что содержимое читается из базы.

**Осталось.** Перевод `IssuesScreen` на `IssuesPhase` — тип написан и ждёт.
Строкой в [`TAILS.md`](TAILS.md), туда же фикстуры с экспортом и вопрос
дизайнеру про 25 состояний без ноды.

### 2026-08-20 — проверка русской типографики

**Что.** `tools/check-typography.py` — восемь правил по строкам интерфейса.
Шесть мест получили неразрывный пробел между числом и единицей.

**Зачем.** Правило переносится из канона студии `pgcorpus` через
`levalovushka/propeller` (`design/principles.md` §6). Оно про русский набор,
язык интерфейса тот же, поэтому переносится без правок — но применять его
грепом по файлу нельзя: комментарии в этом проекте русские и полны тире,
и наивный греп утонул бы в них.

**Что оказалось нетривиальным.**

1. **Проверять надо только строковые литералы с кириллицей.** Идентификаторы,
   ключи и имена символов SF — латиница, к ним правила русского набора
   неприменимы. Отсюда же отсев однобуквенных литералов: буквы госномера
   в `PlateFormat` — данные, а не текст.
2. **Литерал с «₽» кириллицы не содержит вовсе.** Первая версия отсеивала
   `"\(сумма) ₽"` как нетекстовую строку — а это ровно то место, где число
   рвётся с единицей. Признак расширен до «₽ … « » °».
3. **Интерполяция скрывает число.** В `"\(grouped(mileage)) км"` цифр нет,
   и правило «число и единица» молчало. Интерполяция схлопывается в один
   символ-заглушку, и разрыв становится виден.
4. **`\b` после «₽» не работает.** Граница слова требует словесного символа,
   а после «₽» стоит пробел — правило молчало именно там, где нужнее всего.
   Заменено на `(?![А-Яа-яЁёA-Za-z])`.

**Замер: строки уже были чистыми.** Ни одного дефиса вместо тире, диапазона
через дефис, прямых кавычек, многоточия тремя точками или двойного пробела на
225 строк. Единственный настоящий класс нарушений — разрыв числа и единицы,
и весь он прятался за интерполяцией.

**Что поправлено.** Шесть строк: три «км», два «₽» и захардкоженная цена
«4 269 999 ₽», у которой и разряды были обычными пробелами, хотя динамические
числа `NumberFormat.grouped` печатает неразрывными. Неразрывный пробел в SF
той же ширины, что обычный, поэтому сверка не должна сдвинуться.

**Что намеренно не поправлено.** Пробел перед ручным переносом строки в шести
местах и однобуквенные предлоги в семнадцати. Оба класса чинятся вставкой или
снятием символа, который меняет ширину центрированной строки, а перенос здесь
проставлен руками под макет — такой же пробел может стоять в самой ноде.
Проверяется сверкой, которой на этой машине нет, поэтому оба остались
предупреждениями и прогон не валят. Туда же — опечатка «сохрнать» в онбординге:
записана в [`STATE.md`](STATE.md), Часть III, потому что текст пришёл из макета.

**Как проверено.** `python3 tools/check-typography.py` → 228 строк, 0 ошибок,
23 предупреждения. После правок: `swiftc -parse` по всем 34 файлам чист,
`./tools/run-pure-checks.sh` — 39/39, `tools/check-project.py` — только
известный `IssuesScreen 2.swift`. Сборку приложения проверить по-прежнему
нечем: полного Xcode на машине нет.

**Осталось.** Правило «нет слов-просьб в состояниях покоя» написано, но лежит
за флагом `--policy` и по умолчанию молчит: это часть доктрины «тупиков не
бывает», а её для Beepy никто не принимал. Пять таких строк в приложении есть
(`python3 tools/check-typography.py --policy`) — решение продуктовое, не моё.

### 2026-08-20 — чистый слой `Core/Pure` и проверки без Xcode

**Что.** Появился `AppMVP/Core/Pure/` — `ServiceMath` (интервал ТО, остаток,
прогресс, итоги, подъём одометра), `NumberFormat` (разряды и разбор ввода) и
переехавший туда `PlateFormat`. `CarMainView` и `AddCarView` переведены на них:
из вью ушли глобальная `formattedNumber`, константа `serviceInterval`, четыре
метода арифметики и четыре разбора `Int(x.filter(\.isNumber))`. Во вью остались
переходники `Car → значения`. Плюс `tools/pure-checks/main.swift` (39 проверок),
`tools/run-pure-checks.sh` и `tools/check-project.py`.

**Зачем.** Арифметика жила приватными методами внутри файла на 1454 строки и
брала `Car` — модель SwiftData. Проверить её было нечем в принципе: чтобы
позвать `kmUntilService`, нужен был запущенный `CarMainView` с базой. Теперь
функции берут значения, и весь слой собирается **без приложения**.

**Что оказалось важнее самих проверок.** `swiftc` из Command Line Tools
собирает `Core/Pure/*.swift` вместе с проверками, если там нет SwiftUI,
SwiftData и UIKit. То есть прогон — это не только тесты, но и граница слоя:
затащишь туда лишнее — сломается прогон, а не приложение через неделю.
Xcode для этого не нужен, поэтому проверки доступны на любой машине
с исходниками, включая эту, где полного Xcode нет.

**Одна правка поведения, не рефакторинг.** У `NumberFormat.grouped` закреплены
`usesGroupingSeparator = true` и `groupingSize = 3`. Раньше разрядность брала
локаль устройства: на индийской локали числа разбились бы 2-2-3 и разошлись
с макетом. Разделитель локаль и так не выбирала — он перебит на неразрывный
пробел.

**Как проверено.**

- `./tools/run-pure-checks.sh` → «Все 39 проверок прошли». В том числе крайние
  случаи, которых во вью никто не проверял: просроченное ТО отдаёт 0, а не
  долг; одометр меньше пробега последнего ТО даёт прогресс 0, а не
  отрицательную ширину полосы; отсчёт идёт от `max` пробегов, а не от
  последнего элемента массива; разбор понимает неразрывный пробел собственного
  вывода.
- `swiftc -parse` по всем 34 файлам `AppMVP/` — синтаксических ошибок нет.
- `python3 tools/check-project.py` — висячих ссылок нет, дублей в фазе
  компиляции нет, каталоги групп существуют, три файла `Pure` собираются.
  Единственная жалоба — известный `IssuesScreen 2.swift`.
- `plutil -lint` на `project.pbxproj` — OK.
- Префиксы ID `CA`/`CB`/`CC` перед вставкой проверены грепом по всем 24-символьным
  идентификаторам, а не взяты наугад: на этом уже обжигались.

**Чего проверить нельзя.** Приложение здесь не собирается — полного Xcode на
машине нет, только Command Line Tools. Что `CarMainView` компилируется после
правок, **не проверено**: проверен синтаксис, но не типы. Первая сборка на
машине с Xcode это покажет; правки механические (переименование вызовов), но
это объяснение, а не проверка.

**Осталось.** XCTest-таргет — только на машине с Xcode, вслепую в ручной
`pbxproj` его писать нельзя. Строкой в [`TAILS.md`](TAILS.md), туда же —
следующие кандидаты на вынос: `DiagnosisVocabulary` и будущий парсер PDF.

### 2026-08-20 — журнал разрезан на слои

**Что.** Из `docs/JOURNAL.md` (855 строк) выделены четыре файла: `TRAPS.md`
(грабли: 36 собранных по ходу работы плюс 14 вынутых из разборов), `DECISIONS.md` (решения
тремя таблицами: продукт, дизайн, разбор звука), `STATE.md` (состояние
двенадцати компонентов, что не сломать, ждём от дизайнера, дизайн-источник,
карта документации), `TAILS.md` (хвосты). В журнале остались хроника и
девятнадцать разборов, понижённых до `###`.

**Зачем.** Журнал был четырьмя документами в одном файле, и самый ценный текст
в нём — «Грабли» — лежал на 103-й строке из 855. Прочитать его целиком в начале
сессии невозможно, поэтому он и не читался: правки вслепую и повторные грабли
брались отсюда. Разрез сделан по правилу «один факт живёт в одном месте»:
состояние — в `STATE.md`, причина — в `DECISIONS.md`, механизм — в `TRAPS.md`,
хронология — здесь. При расхождении верить коду, потом `STATE.md`.

**Как перенесено.** Блоки переехали **дословно**, без переписывания: сохранён
текст и голос автора, потерять содержание при разрезе нечем. Написаны заново
только рамки файлов, таблица решений по дизайну и разбору звука (собрана из
прозы разборов) и часть II `TRAPS.md` — четырнадцать граблей, которые лежали
внутри повествования и не находились поиском.

**Как проверено.** `.claude/hooks/require-journal.sh --audit` — чисто. Потери
текста проверены скриптом: каждая содержательная строка старого журнала ищется
в новых файлах, не нашлась одна — путь к инструментам попиксельной сверки
(`diffimg`, `rowsx`, `crop`), он возвращён в `CLAUDE.md`. Объём: было 855 строк
в одном файле, стало 660 в журнале плюс 240 + 55 + 196 + 40 в новых, то есть
1191 — рост за счёт рамок файлов, части II `TRAPS.md` и таблиц решений, которых
не существовало. Ссылки между файлами проставлены руками и автоматически не
проверялись: линтера markdown в проекте нет.

**Осталось.** Разборы не датированы: у них нет дат ни в тексте, ни восстановимых
без `git log` (а он по этой папке уходит за две минуты). Оставлены как есть.
`CHECKLIST.md` и `STATE.md` частично пересекаются в разделе «Дальше» — это
терпимо, пока `CHECKLIST.md` адресован владельцу, а `STATE.md` коду.



Записи по кускам работы, новое сверху, заголовок `### ГГГГ-ММ-ДД — тема`.
Что изменилось и в каких файлах, зачем, **как проверено** (точная команда и
настоящий вывод — или прямо «не проверено»), что осталось. Тематические разделы
ниже — не хроника: они переписываются под текущее состояние, а не дополняются.

Записи требует заслонка `.claude/hooks/require-journal.sh`: коммит, меняющий
код без записи, не проходит.

### 2026-08-20 — заслонка на коммит и CLAUDE.md

**Что.** Появились три файла: `.claude/hooks/require-journal.sh` (175 строк),
`.claude/settings.json` (регистрирует его на `PreToolUse(Bash)`) и `CLAUDE.md`.
Из этого журнала в `CLAUDE.md` переехали два раздела — «Как проверять» и
«Ограничения окружения», — на их местах остались указатели.

**Зачем.** Правило «дополнять журнал после каждой заметной задачи» записано в
шапке этого файла и всё равно не соблюдается: контекст сессии кончается раньше.
Коммит — единственный момент, где проверка дешёвая и однозначная. Заодно
заслонка закрывает главную грабли ручного `project.pbxproj`: файл в гите,
которого нет в сборке. Образец — `require-worklog.sh` из репозитория Propeller
(`levalovushka/propeller`), там та же заслонка появилась после четырёх
пропущенных коммитов подряд.

**Что проверяет заслонка.** (1) запись в `docs/JOURNAL.md`, если коммит трогает
`AppMVP/`, `tools/` или `.pbxproj`; (2) каждый `.swift` из коммита прописан в
`PBXSourcesBuildPhase`, и наоборот — у удалённого файла не осталась ссылка;
(3) `plutil -lint` на `project.pbxproj`. Пропускает `--amend`, `--dry-run`,
пустой стейдж, коммит одних доков и следующие шаги того же дня после уже
закоммиченной записи. Падает открытой: нет `jq`, не репозиторий, ошибка git —
exit 0, потому что сломанный сторож не должен заклинить все вызовы Bash.

**Как проверено.** Восемь сценариев на синтетическом репозитории в `/private/tmp`
(настоящий склонировать не вышло: `git clone --local` из iCloud-папки виснет на
материализации файлов). Пропускают: `git log --grep commit`, `git status`,
`--dry-run`, `--amend`, пустой стейдж, коммит одних доков, второй шаг того же
дня через `git commit -am`, удаление мёртвого `.swift`. Блокируют с exit 2:
новый `.swift` мимо проекта, правка кода без записи, удаление файла с
оставшейся ссылкой, битый plist. На настоящем репозитории: заслонка 0.18 с,
ревизия 0.24 с — лимит в `settings.json` 10 с.

**Что поймала проверка.** Первая версия блокировала коммит, который **удаляет**
`.swift`, отсутствующий в проекте: файл попадал в общий список изменённых, на
диске ещё лежал (`git rm --cached` его не сносит), и заслонка требовала
«пропиши его в проект». То есть не пропустила бы ровно ту уборку, ради которой
написана, — и первой её жертвой стал бы `IssuesScreen 2.swift`. Удаляемые пути
теперь вычитаются из этой проверки явно, а не через существование файла.

**Что нашла ревизия.** `.claude/hooks/require-journal.sh --audit` на этом
репозитории:

```
НЕ СОБИРАЕТСЯ: AppMVP/Features/Issues/IssuesScreen 2.swift
```

500 строк, 478 из них общие с живым `IssuesScreen.swift` (682 строки) — копия
Finder'а, отставшая на несколько правок. В `project.pbxproj` её нет ни одной
строкой, то есть она не компилируется вовсе; добавить её туда нельзя — оба
файла объявляют `struct IssuesScreen`. Файл не тронут: удаление 25 КБ чужого
кода — решение владельца, а не работа заслонки.

**Осталось.** Решить судьбу `IssuesScreen 2.swift`. Разрезать журнал на слои
(`STATE.md`, `DECISIONS.md`, `TAILS.md`) — тогда указатели выше станут не
нужны, а «Технический долг» переедет в `STATE.md`. `docs/XCODE_SETUP.md`
ссылается на несуществующий `AppMVP.xcodeproj`, `docs/DEVICE_TESTING.md`
упоминает снятый флаг `hasAddedCar` — оба разошлись с кодом.



---

## Разборы

Записи о сделанной работе до появления хроники: механизм, замер и причина
по каждому куску. Не датированы — дат в тексте нет, а `git log` по этой
папке уходит за две минуты. Новое сюда не дописывается: новое — в хронику.

### Статичная карусель (сделано)

Подложки при свайпе должны **оставаться на месте**, меняться должен только
текст — как в Гараже Т-Банка. Аннотация макета `45895:3569` говорит то же
самое: «все элементы остаются на месте и просто меняются надписи». Она
процитирована в `CarMainView.swift:127`, но построено было противоположное —
`HStack` из двух полных страниц со сдвигом `.offset(x:)`.

**Из этой архитектурной ошибки выросли все недавние баги:** конфликт жеста с
вертикальным `ScrollView` и с лентой ТО, обнажение фона за краем, подложка
под каруселью, раздутый вьюпорт и пропавший скролл, залипший `.scrollDisabled`.
В статичной раскладке ничего этого не существует — двигать нечего.

Целевая архитектура: одна неподвижная страница, прогресс свайпа `p` (0…1)
управляет только содержимым.

- фото машины — единственный едущий элемент: два кадра в ряд со сдвигом
  `p × ширина`, обрезанные по рамке 190.415pt;
- общие подложки (тёмная карточка 92pt, radius 36) стоят, лейблы внутри
  перекрёстно гаснут: «Добавить ТО» → «Добавить авто»;
- заголовок и номер на месте, текст меняется, номер гасится в 0;
- блоки только первой страницы (stat-карточки, история, удаление) гаснут
  целиком при `p → 1`;
- пейдж-контрол уже неподвижен, не трогать.

Удаляется: `HStack` двух страниц и `.offset(x:)`, `pinX` и компенсация точек,
резинка на краях (`dragX / 10`, `atEdge`), `swipeZoneHeight` как защита от
конфликтов, `addNewCarState` как отдельная страница. Остаётся: `carPage`,
`dragX`, замок оси, `Motion.page`, параметр `visible` в построителях карточек
(в него пойдёт `1 - p`).

Проверять: подложки **не сдвинулись ни на пиксель** между кадрами при
`p` = 0, 0.5, 1 — численно через `rowsx`, не на глаз. Плюс обязательный
контроль скролла через `.defaultScrollAnchor(.bottom)`.

Сделано в `955aab4`; в `d838246` починен лейаут (блок фото сообщал наверх
402pt внутри контентных 370 и вылезал за края на 16 с каждой стороны — теперь
он в локальном `GeometryReader`, который принимает предложенную ширину).

### Шапка при прокрутке и запрет свайпа (сделано)

Свайп карусели запрещён, когда список прокручен дальше 8pt: у жеста один
предикат `canSwipe(startY:)` на `onChanged` и `onEnded`. Если жест начался
разрешённым, а список успел уехать за порог, `onEnded` возвращает `dragX`
в ноль — иначе страница залипала наполовину погашенной.

Шапка — нода `46001:6457`: чёрная плашка 402×137 от самого верха кадра
(вместе со статус-баром), контент прижат к низу, номер в ней **мельче**
основного (11pt против 17, паддинги 6/2 против 12/4) — это отдельный
`compactPlate`, а не тот же `plate`. Лежит оверлеем **внутри** прокрутки и
приколочена к верху обратным сдвигом через `.visualEffect` — см. грабли выше
про `@State`. Сверка с рендером ноды: **1.7/255**, зон расхождения нет.
Экран в покое побитово совпал с предыдущим коммитом (0.0/255).

### Пружина увеличивает фото и чистая страница «добавить новую» (сделано)

Натяжка считается тем же приёмом, что и прозрачность шапки: `.visualEffect`
на блоке фото, масштаб `1 + натяжка/500`, потолок +35%. Масштаб навешен
**после** `.clipped()` — иначе вторая копия фото вылезала бы из-под клипа.

Натяжка = положение блока фото минус `PhotoStretch.restingY` (206.2 — сумма
координат макета: 103 + 31.2 + 16 + 32 + 24). Это хрупкое место: ошибка в
константе увеличила бы фото уже в покое. Проверяется тем, что кадр в покое
обязан совпасть с предыдущим коммитом побитово.

На экране без ТО пружины нет — прокрутки там нет вовсе, и пространства
координат `ScrollHeader.space` не существует, поэтому `header` получает флаг
`stretches`, а не читает геометрию вслепую.

Проверено: покой ниже статус-бара 0.2/255 без зон расхождения (то есть
масштаб ровно 1), при имитации натяжки 80pt машина выросла с 830 до 963px —
ровно 1.16, центр не уехал. Подложка тёмной карточки стоит на одном пикселе
при p = 0, 0.5 и 1.

**Расхождение в самом макете, вопрос к дизайнеру:** на «главная_добавить
новую» (`45949:3265`) фото машины стоит на 19pt ниже, чем на «главная_то_
добавлено» (`45867:3007`) — 253…332 против 234…313. Наша страница совпадает
с главной, потому что по принципу статичной карусели фото не должно прыгать
по вертикали при свайпе. Заголовок и тёмная карточка на обеих страницах
совпадают с макетом точно.

### Как имитировать жест, которого нет в симуляторе

Тапать и тянуть симулятор нельзя, но эффект, зависящий от геометрии, можно
проверить сдвигом опорной константы: чтобы увидеть натяжку 80pt, в патченой
сборке `PhotoStretch.restingY` уменьшается на 80 — формула видит ровно эту
натяжку и рисует итоговый кадр. Так же проверялись страницы карусели через
`dragX`/`carPage` и низ списка через `.defaultScrollAnchor(.bottom)`.

### Свечение кромки в углах (сделано)

`motionRim` больше не наматывает круги по периметру. Пятна привязаны к
диагонали «левый верхний — правый нижний»: опорный угол считается из размера
фигуры (`atan2(-h/2, -w/2)`), основное пятно стоит на `location 0.25`, поэтому
градиент поворачивается на `угол - .pi/2`, а второе пятно на `0.75` попадает
в противоположный угол само. Наклон даёт не оборот, а качание: `sin` от
отклонения угла от `DeviceTilt.neutralAngle`, не дальше ±28°. В покое и при
Reduce Motion качание нулевое, то есть пятна ровно в углах.

Гироскопа в симуляторе нет, и это удобно: там угол всегда нейтральный, значит
кадр показывает именно «пятна в углах» и это проверяемо.

### Раздел «Карта» (подключён, ждёт ключа)

- Дизайна раздела в файле нет — пройдены все 548 фреймов доски. Содержание
  согласовано отдельно: голая карта с позицией пользователя.
- `YandexMapsMobileFull` 4.42.0 подключён через SPM. Ключ лежит в
  `AppMVP/Core/Services/MapKitKey.swift` и **пока пустой**: пока он пуст,
  раздел показывает объяснение, а не пустой экран.
- Ключ в приложении — это нормально и не противоречит решению «ключ
  поставщика только на сервере»: у мобильных SDK карт другая модель, ключ
  привязан к бандлу и секретом не является.
- Разрешение геопозиции идёт через `INFOPLIST_KEY_NSLocationWhenInUseUsageDescription`
  в настройках сборки: `Info.plist` у проекта генерируемый.
- **Саму карту проверить нельзя, пока нет ключа** — без него она не рисуется.

### Флоу раздела «Ошибки»

Порядок ровно такой, три раза собирал его неверно:

1. история пуста → «Слушать» пишет **на месте**, без модалки;
2. история есть → запись идёт **в модалке** поверх притемнённого экрана
   (`46105:3970`). Модалка нужна, чтобы закрыть уже непустой экран, — это не
   «состояние записи»;
3. «Стоп» → шторка **«Вот что мы нашли»** (`46102:3369`) со списком находок;
4. «Да, добавить ошибки» → находки уходят в историю, и она появляется;
   «Нет, не добавлять» → история не меняется.

**История пуста на старте и растёт только подтверждением.** Предзаполненной
заглушки на экране быть не должно.

### Шар на экране «Ошибки»: четыре варианта

**Главный вывод, к которому шли долго: элемент в макете — растр, а не фигура.**
Я несколько раз подбирал синусоиды, пытаясь попасть «точь в точь», хотя нода
`46102:2999` это просто картинка. Достаточно было выгрузить её через MCP и
положить в ассеты: расхождение по зоне шара сразу упало до **1.2/255**, чего
никаким подбором не добиться. Если элемент в Figma растровый — выгружаем, а не
воспроизводим.

Палитра живой волны снята пипеткой с этого же ассета, чтобы анимация
продолжала его цвета: ядро 144/251/244, тело 87/200/179, зелёный 50/144/105.

### Живая волна: что дал разбор референсной записи

Референс — экранная запись чужого приложения. Брать оттуда графику нельзя, но
**измерить движение по кадрам можно и нужно**: это отвечает на вопросы, на
которые не отвечает ни макет, ни глаз.

Из 24 кадров за 14 секунд (`AVAssetImageGenerator`, `ffmpeg` в системе нет):

- **Двигается не средняя линия, а толщина.** Центр тяжести ленты гуляет на
  ±3 px из 106, тогда как её полувысота ходит от 7 до 75 px, и утолщение
  переезжает вдоль оси. Отсюда главная правка: лента стала **заливаемой
  фигурой с переменной толщиной** вместо обводки постоянной ширины. Штрихом
  такое набухание не нарисовать в принципе — сколько ни подбирай синусоиду.
- **Цвет распределён по вертикали**: бирюзовая кромка сверху (154,255,254),
  мята в середине (117,247,148), зелень снизу (79,174,105). Один слой с
  вертикальным градиентом заменяет три обводки разного цвета.
- **Наружу не выходит ничего.** Свет живёт строго внутри пузыря.
- Слои складываются `.plusLighter`: в местах перекрытия канал уходит в
  потолок, отсюда почти белая середина.

**Грабли: пузырь заметно меньше кадра.** На растре тело занимает 783 × 573 из
1024 × 822, а картинка ещё растянута по высоте на 124.3 % и поднята на 15.9 %.
И лента, и свечение кромки рисовались по краю **кадра** — то есть вокруг
пустоты. На тихом звуке это незаметно, на громком читалось как лишняя рамка
вокруг шара. Геометрия пузыря вынесена в `OrbBubble` и используется и для
обрезки света, и для кромки.

Сверка после правок: покой против предыдущей сборки **0.2/255**, зон
расхождения нет — растр не тронут. Профиль ленты на громкости 0.75 (толщина в
долях высоты пузыря) 0.15…0.47 против 0.13…0.50 у референса, центр 0.45…0.56
против 0.39…0.45; кадры подряд различаются — волна действительно колеблется.

### Шторка находок против макета `46102:3005`

Три расхождения, каждое со своим механизмом:

1. **Кнопки висят над списком, а не стоят под ним.** В макете блок кнопок
   начинается на 584 при высоте списка 692 — то есть перекрывает его, и
   шестая карточка честно уходит под кнопку. У меня они были соседями в
   стопке, из-за чего под списком оставалась пустая белая плита в треть
   экрана. Стало: `overlay(alignment: .bottom)` плюс отступ снизу у контента
   ровно в высоту блока кнопок (142), иначе последняя карточка остаётся под
   ними навсегда.
2. **Заголовок карточки — Subheadline (15pt), а не Body (17pt).** Проверяется
   арифметикой: 20 (падинг) + 20 (заголовок) + 6 + 36 (два футнота) + 20 = 102,
   ровно высота карточки из макета. С 17pt получалось 104.
3. **Высота карточки не собирается из строк.** Системные метрики SF дают
   100.7 вместо 102, и на шестой карточке список уезжал на 6pt. Добор снизу
   через `minHeight: 102` — текст остаётся на месте макета.

Замер после правок: шаг карточек 118 как в макете (было 116.5), чёрная кнопка
попала точь в точь — `y 732…785, x 16…385` в обоих кадрах.

**Расхождение, которое воспроизводить не стал:** в макете заголовок шторки
стоит слева и его перекрывают крестик и галочка — экземпляр компонента
`Toolbar - Top - iPhone` не растянут под длинный заголовок (trailing-слот на
x=174 вместо правого края). Сделано как в системе: крестик слева, заголовок по
центру, галочка справа. Вопрос дизайнеру.

Шесть находок в заглушке — столько карточек в шторке макета. В самом макете
все шесть с одинаковым текстом, то есть наполнитель.

### Раздел «Карта»: сделан, ключ Яндексом не принят

Подробности в [MAP.md](MAP.md), здесь — то, обо что можно споткнуться снова.

**`-ObjC` в `OTHER_LDFLAGS` обязателен, и без него падает не карта, а всё
приложение.** `+[YMKMapKit setApiKey:]` объявлен в категории, а категории из
статической библиотеки линковщик выбрасывает. Диагноз обманчивый: падение
происходит в `AppMVPApp.init`, будто дело в SwiftUI. Ловушка ещё и в том, что
раньше это не всплывало — ключ был пустой, и ветка не выполнялась. То есть
любая настройка, спрятанная за «пока не настроено», может годами скрывать
поломку сборки.

**Ключ отклонён, и это проверено вне приложения.** Тем же ключом отбиваются
Геокодер, поиск по организациям, статичные карты и JS API v3 — тем же ответом,
что и заведомо мусорный ключ. Значит дело не в типе API. Такую проверку стоит
делать до того, как чинить код: полдня можно потратить на «наверное, я неверно
инициализирую SDK».

**SDK не сообщает об отказе по ключу в обработчик.** Он уходит в бесконечные
повторы, и индикатор поиска крутился бы вечно. Поставлен сторож на 15 секунд.
Вывод шире карты: у любого запроса в SDK, который не обязан ответить, должен
быть свой предел ожидания — иначе интерфейс врёт, что работа идёт.

**Владение объектами SDK собрано в одном месте.** Слушатели он держит слабо,
сессии отменяет при потере ссылки. Разложенное по мелким типам владение теряется
молча: карта просто перестаёт отвечать, и искать это потом дорого.

## Модалка записи: заливка и таббар поверх неё

**Заливка.** В коде стояла `Color(white: 0.11)` (это 28) плюс белая обводка
0.14 — с комментарием «плотнее, чем Liquid Glass - Clear в макете». Пипетка по
рендеру ноды `46105:4088` показала ровный **rgb(25,25,25)** по всей панели,
сверху донизу, и **никакой светлой кромки**. Поставлено точное число, обводка
снята; после правки замер даёт 25,25,25 в тех же трёх точках.

**Таббар оставался поверх модалки.** Это неверно: модальное окно на то и
модальное, что забирает управление себе. Скрытие висело только на шторке
находок, панель записи его не поднимала.

Две грабли по дороге, обе стоили по сборке:

1. **`onChange` молчит, если состояние истинно уже на входе** в раздел. Скрытие
   не срабатывало, пока не добавил `onAppear`.
2. **`toolbarVisibility(_:for: .tabBar)` на самом `TabView` игнорируется.**
   Объявлять видимость должно **содержимое вкладки** — тогда работает.

**Расхождение с макетом, сделанное осознанно:** на рендере `46105:3970` таббар
во время записи виден. Панель там кончается на 613, бар стоит на 780, и
геометрически они не спорят, но затемнение экрана бар не накрывает. Сделано по
HIG, а не по макету, — по прямой просьбе.

**Заодно всплыл мой же регресс.** Жёсткие переносы описания я проставил под
ширину экрана (370), а в модалке блок 338 — там строка ломалась на четыре
вместо трёх. Теперь переносов два набора, каждый под свою ширину, оба сняты с
макета.

### Таббар отдан системе

Бар был написан руками, вместе с `TabBarState`, порогом в 28pt и своим
отслеживанием направления прокрутки. Всё это — ручная копия того, что iOS 26
делает сама.

**Решающий довод нашёлся в макете.** Компонент там — `Tab Bar - iPhone` из
дизайн-кита Apple, причём с вариантом `Minimized`. То есть дизайн с самого
начала описывал системный элемент, а мы его перерисовывали. Правильный код —
не повторять, а взять: `TabView` + `Tab`, `tabBarMinimizeBehavior(.onScrollDown)`,
`toolbarVisibility(_:for: .tabBar)` вместо `hidesTabBar`. Своя реализация
осталась веткой для iOS 17–25.

**Две вещи, без которых системный бар выглядит сломанным:**

1. **Без `tint` выбранная вкладка тусклее невыбранных.** Системный бар красит
   её акцентом, а акцент по умолчанию на стекле не читается. Ставится
   `.tint(Figma.accentsBlue)` — синий из макета.
2. **Схему задаём бару, а не экрану.** В макете бар светлый (`BG mode="Light"`),
   а экран идёт тёмным ради светлого статус-бара. `environment(\.colorScheme,
   .light)` вешается на `TabView`, а каждая вкладка возвращает себе тёмную.

**Шторки, тост и рамка живут снаружи `TabView`.** Внутри вкладки системный бар
рисуется поверх её содержимого, и шторка ушла бы под него.

Замер: страница машины не сдвинулась ни на строку (главный риск — `TabView`
добавляет свою нижнюю safe area, но страница верстается от верха и её
игнорирует). Расхождение зоны таббара с макетом 63.2 → 56.8 из 255.

### Рамка прослушивания (снята)

**Убрана по просьбе 18.08.2026.** Ниже описан механизм — он рабочий, но самой
рамки в приложении больше нет; вернуть можно ревертом коммита. Оставляю запись,
чтобы через месяц никто не искал в коде то, чего там нет, и не собирал заново
уже отвергнутое.


Свет по кромке экрана на время записи, референс — Apple Intelligence.
Уровень доезжает до неё через `ListeningState` тем же приёмом, что
`TabBarState`: экран держит ссылку и **не подписан**, иначе пересобирался бы
на каждом аудиобуфере.

Два прохода — широкий тусклый ореол и узкая яркая полоса, обе угловым
градиентом по палитре орба с едущей фазой. Одного слоя мало: у света должно
быть и рассеяние, и ядро. `plusLighter`, `allowsHitTesting(false)` — рамка
индикатор, а не элемент управления.

**Про радиус экрана честно.** Приватный `_displayCornerRadius` брать не стал,
поставил константу по высоте экрана. Проверить её замером не вышло: кромка
размыта на 22pt, и «первый светящийся пиксель» границу маски не даёт. Проверка
осталась визуальной — свечение садится на угол без зазора и без обрезки.

### Liquid Glass вернулся туда, где он объявлен

Проверка по нодам показала, что стеклом в макете объявлено почти всё, а в коде
стояла плоская заливка: карточка «ТО через» (`45867:3027`, причём **Medium**),
трек прогресса, «Цена авто» и «Пробег» (`45895:3528`), счётчики, карточки
записей ТО, карточки находок и истории «Ошибок».

Кнопки: материал взят системный, а раскладка оставлена своя. `buttonStyle(.glassProminent)`
в системе есть, но он сам назначает лейблу свои отступы, а у наших кнопок
геометрия снята с макета до точки.

**Способ проверки пришлось сменить, и это важно.** Раньше я сверялся с рендером
Figma по пикселям. Со стеклом это перестаёт работать: в Figma оно нарисовано
приблизительно, а на устройстве преломляет то, что под ним, — расхождение в
этих зонах закономерно вырастет. Поэтому теперь проверяется геометрия (позиции
и размеры не поехали) и факт применения материала, а не совпадение цвета.

### Разбор звука подключён: cardiag вместо заглушки

Раздел «Ошибки» ходит в [adam-s/car-diagnosis](https://github.com/adam-s/car-diagnosis)
(MIT). Подробности подъёма — в [DIAGNOSIS.md](DIAGNOSIS.md), здесь только то,
обо что можно споткнуться.

**Приложение вообще не писало звук.** `AudioLevelMeter` считал RMS для шара и
ничего не сохранял. Запись добавлена в **тот же** тап: поднимать второй
микрофонный вход нельзя, вход один и потребители за него дерутся. Пишем в
очереди тапа, не перекладывая буферы, — движок переиспользует их память.

**WAV, а не m4a.** Сервер читает файл через `soundfile`, тот берёт PCM без
ffmpeg. С m4a на сервер пришлось бы ставить ffmpeg ради ничего. Проверено:
`cardiag doctor` ругается на отсутствие ffmpeg, а разбор WAV при этом проходит —
ffmpeg там для скачивания роликов.

**`AVAudioFile` нельзя отдавать читателю живым.** Заголовок WAV дописывается
при освобождении объекта, поэтому ссылка обнуляется в `stop()` до того, как
путь уходит наружу. Иначе сервер получает обрезанный файл.

**ATS и генерируемый Info.plist.** Локальный сервер работает по HTTP, ATS его
блокирует. Дырой `NSAllowsArbitraryLoads` это не лечится — достаточно
`NSAllowsLocalNetworking`, он снимает запрет только для loopback и локальных
адресов. Ключ это словарь, а `INFOPLIST_KEY_*` умеет только скаляры. Решение:
`INFOPLIST_FILE` рядом с `GENERATE_INFOPLIST_FILE = YES` — Xcode берёт файл за
основу и **подмешивает** в него сгенерированные ключи, а не заменяет их.
Проверено по собранному бандлу: в Info.plist оказались и словарь ATS, и все
прежние `NSMicrophoneUsageDescription` и прочие.

**Две вероятности, которые нельзя смешивать.** Я сначала подписал долю
семейства словом «уверенность» — это неверно. `fault_probability` откалибрована
(температура 3.08, ошибка калибровки ≈ 0.04) и читается как обычная
вероятность. А `causes[].p` — распределение по 21 семейству с температурой
**1.0**, то есть не калибровано вовсе: его 99 % значат «сюда модель отдала почти
весь вес среди версий». На демо-клипе это 99 % на выхлоп при 64 % на сам факт
неисправности — подпись «уверенность 99 %» прямо вводила в заблуждение. Теперь
первая карточка отдаёт калиброванное число, а у версий стоит «модель ставит
сюда».

**Голова причин не умеет молчать, и это опасно.** У неё нет класса «ничего»:
100 % всегда раскладываются по деталям. Замер: пять секунд тишины дают
«неисправность 73 %» и «выхлоп 100 %», чистый синус — «неисправность 92 %».
То есть экран уверенно показывал бы выдумку на любой записи не по делу.
Предохранитель — поле `segments`: ноль означает, что каскад не выделил
механического куска и разбирал запись целиком. Тогда не показываем ни вердикт,
ни версии, только просьбу записать ближе к мотору. Непроверенным осталось
главное: ноль сегментов дают все четыре моих тестовых записи, включая
демо-клип самого репозитория, а даёт ли настоящий мотор сегменты > 0 — без
машины не выяснить.

**Одного предохранителя мало: гейт по вердикту.** Порог громкости в каскаде
чистки относительный — `rms > max(0.005, percentile(rms, 20) * 1.5)`. Ровный
звук его не проходит, поэтому тишина, фон комнаты и даже синус дают ноль
сегментов. А вот запись «я молчу», но с парой шорохов от руки даёт **два**
куска, и предохранитель по `segments` её пропускает. Вердикт при этом честный —
«норма, 32 %», — а список деталей всё равно уверенно называл выхлоп на 86 %.
Отсюда второй гейт: версии показываем только при `verdict == "fault"`. Голова
причин отвечать «ничего» не умеет, голова `kind` умеет и вдобавок
откалибрована — она и служит воротами.

**Корень проблемы — открытое множество, и решать его пришлось снаружи.**
Двух предохранителей внутри ответа всё равно не хватило: пользователь включал
запись в комнате, и приложение продолжало называть «дифференциал». Причина в
том, что модель училась отличать неисправный мотор от исправного, а варианта
«здесь вообще нет машины» у неё нет ни у одной головы. Внутри ответа этого не
поймать в принципе.

Привратник вынесен в свой сервер `tools/diagnosis_server.py` поверх cardiag: он
спрашивает у CLAP напрямую, мотор ли это. CLAP текстово-звуковой и в самом
пайплайне уже так отсеивает музыку — приём взят оттуда, просто задан другой
вопрос. Замер: синтетический мотор 0.86 против 0.10…0.38 у пяти записей без
мотора, порог 0.5 разделяет с запасом.

Заодно снят жёсткий запрет по `segments`: он стоял первым и оказался вреден.
Порог громкости в каскаде относительный, поэтому ровно урчащий мотор кусков не
даёт так же, как тишина, и запрет отрезал бы настоящие записи. Отсеивать
не-мотор — работа привратника, а ноль сегментов теперь только оговорка.

**Классы модели не равны таксономии из репозитория.** Словарь я собрал по
`pipeline/build.py`, а в шипованной модели у головы `cause` 21 класс, и среди
них `transmission`, `cooling_other`, `mounts`, `other` и отдельный
`bad_wheal_bearing` (опечатка в их разметке) рядом с `wheel_bearing`. Их не было
в словаре, и они доехали бы до экрана английскими идентификаторами. Проверять
надо `classes_` артефакта, а не список в коде.

**Хвост ранжирования — шум.** Модель отдаёт причины списком целиком, и на
демо-клипе после уверенных 99 % шли 1 % и 0 %. Карточка «уверенность 0 %»
читается как найденная неисправность, поэтому ниже 5 % отбрасывается, но первая
строка остаётся всегда: пустая шторка хуже честной цифры. Там же убран повтор
«Выпускная система — Выпускная система»: у части семейств название совпадает
с зоной.

**Шторка открывается только после ответа.** Показать её сразу и потом подменить
содержимое значит соврать про то, что уже «нашли». На время разбора кнопка
отдаёт индикатор — тем же приёмом, что `GlassProminentButton`, чтобы геометрия
не менялась.

### Четыре замечания по разделу «Ошибки» и их настоящие причины

Все четыре подтвердились замером по рендерам макета. Ни одно не оказалось
«не заметил» — у каждого был механизм, и три из четырёх лежат не там, где
выглядит проблема.

**1. `shadow` без `compositingGroup` достаётся каждому примитиву.** Самая
дорогая находка. Когда подложку шторки сменили со стекла на сплошную заливку,
фон шторки уехал с 255 до 236, и по нему пошли тёмные ореолы вокруг каждой
карточки — при том что тень у карточек стояла с непрозрачностью **ноль**.
В SwiftUI `shadow` применяется к каждому рисуемому примитиву по отдельности,
если слой не склеен: свою тень получала каждая карточка и каждая строка текста
от шторочного `shadow(0.25, radius 24)`. Раньше это гасило стекло —
`glassEffect` сам делает слой. Лечится `compositingGroup()` перед `shadow`.

**2. Карточки в макете держатся тенью, а не заливкой.** Их белое 255 против
фона шторки 252 — разница в три уровня, читаются они целиком за счёт тени:
245 у края карточки, 243 в зазоре между двумя. Тени у меня не было вовсе.
Подобрано по этим числам: `black.opacity(0.10), radius 10, y 2` даёт 243 и 245.

**3. Нижний градиент — это системный scroll edge effect.** Замер по одному
месту: текст шестой карточки под кнопками в макете размыт до 229, у меня был
101 в полную силу и лез на «Нет, не добавлять». По гайдам iOS это не градиент
поверх контента, а панель безопасной зоны: `safeAreaBar(edge: .bottom)` плюс
`scrollEdgeEffectStyle(.soft, for: .bottom)`. Система сама и считает отступ под
панель, и растворяет контент под ней. Для iOS 17–25 оставлен нарисованный
градиент.

**4. Кнопка стояла на 21pt выше макета — из-за описания над ней.** Отступы
48 / 24 / 206 совпадали с макетом до знака, а описание в макете занимает **три**
строки при объявленной высоте 63, тогда как системный шрифт укладывает тот же
текст в две. Одна недостающая строка это ровно 21pt, на которые уезжает вверх
всё, что ниже. Переносы проставлены руками, как у заголовка. **Урок: сверять
надо не только отступы, но и число строк** — блок с «правильными» отступами
всё равно съедет, если текст лёг иначе, чем в Figma.

Заодно радиус карточек 26 → 34: по краевым отступам макета (15 / 6 / 2 на
6 / 15 / 22px ниже верха) выходит 34, у 26 получалось 9 / 3 / 1.

### Шар в покое: почему одной скорости мало

Шар стоял намертво — в `drawLiveWave` был ранний выход при тишине. Убрать его
оказалось мало: с постоянным уровнем в покое движется только геометрия ленты,
и разница между кадрами через 1/6 секунды выходила 5.6/255 против 24–27 у
референсной записи (мерялось по одинаковой центральной полосе пузыря, иначе
пропорции шаров разные и числа несравнимы).

**Не хватало модуляции самого уровня.** На записи каждый кадр гонит голос:
вместе с толщиной ленты скачет и яркость. Поэтому уровень в тишине сам дышит
между `idleLow` и `idleHigh` двумя синусоидами с несоизмеримыми частотами.
С этим и с поднятыми вдвое скоростями стало 14–22/255 — попадание в диапазон
записи.

**Отменённое правило:** прежняя проверка «в покое кадр совпадает с растром
макета до 1.2/255» больше не действует и не должна применяться. Макет статичен
по определению, а шар теперь живёт всегда. Критерий приёмки — совпадение
движения с записью, а не совпадение покоя с картинкой.

### Прежние варианты шара

`SoundOrb.style` переключает вид одной константой, откат — замена значения:

- `.wave` — волна внутри тёмного пузыря, сделана по макету;
- `.particles` — облако точек на сфере, вариант из демо voice-orb. Кода
  частиц в репозитории нет (в `src/core` только `VoiceOrb`, `audio-pipeline`,
  `forces`, `utils`), перенесён приём;
- `.blob` — морфящийся контур, подход перенесён из
  [voice-orb-visualizer](https://github.com/OrbitingBucket/voice-orb-visualizer)
  (MIT). Библиотека веб, на TypeScript и Canvas 2D, поэтому не подключена как
  зависимость, а переписана под наш `Canvas`: 24 радиальные точки, радиус
  гнут три синусоиды, точки соединены квадратичными кривыми.

**`.blob` заметно расходится с макетом**: в макете волна живёт внутри тёмного
пузыря, а у блоба деформируется сам контур, и в тишине он почти ровный
цветной эллипс. Оставлен включённым по просьбе, решение за дизайном.

### Таббар по HIG iOS 26 (сделано)

До правки бар был статичной картинкой из макета: вкладки менялись без
перехода, иконки не отличались ничем, кроме цвета, а четвёртая вкладка
открывала пустоту. Перенесено поведение системного таббара iOS 26.

- **Сворачивание при прокрутке.** Прокрутка вниз стягивает бар в пилюлю с
  одной активной вкладкой, прокрутка вверх разворачивает. Считает `TabBarState`
  по направлению движения: порог 28pt отсчитывается **от точки разворота**, а
  не от начала списка, иначе бар отвечает с задержкой в целый экран. Ближе
  24pt к верху бар всегда развёрнут.
- **`TabBarState` — объект, а не `@State` экрана.** Смещение прокрутки
  приходит на каждом кадре, и `CarMainView` намеренно не держит его в
  состоянии (см. грабли ниже). Экран хранит **ссылку** в `@State`, то есть на
  объект не подписан; подписан один `FloatingTabBar` — перерисовывается только
  он. По той же причине `isMinimized` присваивается через `set(_:)` с
  проверкой на равенство: условие «уехали дальше порога» истинно всё время,
  пока список едет, и прямая запись рассылала бы `objectWillChange` 60 раз
  в секунду.
- **Ширины вкладок явные, а не `maxWidth: .infinity`.** Стянуть бар в пилюлю
  нечем: `.infinity` не анимируется. Развёрнутый бар делит ширину поровну,
  свёрнутый оставляет активной вкладке 92pt, остальным ноль; стекло тянется
  за суммой. Ширина берётся из `GeometryReader`, а не замером в `@State`, —
  замер давал бы первый кадр не в ту ширину.
- **Клип на `HStack` после горизонтального паддинга.** Ужатая в ноль вкладка
  продолжает рисоваться в натуральную ширину — без клипа иконки повисают
  снаружи стекла. Паддинг именно до клипа: пилюля выделения торчит из вкладки
  на 2pt и должна остаться целой.
- **Иконка перетекает контур → заливка** (`contentTransition(.symbolEffect(.replace))`)
  и подпрыгивает. Прыжок висит на счётчике **своей** вкладки: общий признак
  «выбрана» меняется сразу у двух, и прыгали обе — и та, что выбрали, и та,
  что бросили.
- **Пилюля переезжает пружиной** `Motion.tabSelection` с коротким перелётом.
  У свёрнутого бара она гасится прозрачностью, а не снимается условием:
  условие сломало бы `matchedGeometryEffect`.
- **Повторный тап по активной вкладке** возвращает список в начало (свёрнутый
  бар сначала разворачивается). Скроллим живой `UIScrollView` — тот же, что
  уже найден `ScrollViewFinder` ради глушения панорамирования.
- **Разделы меняются растворкой**, приходящий чуть подрастает. Уезжающих вбок
  страниц нет намеренно: вкладки — не соседи, а разные места приложения.
- Reduce Motion снимает прыжок символа, сжатие по нажатию и масштаб перехода;
  Reduce Transparency переводит бар на непрозрачную подложку.
- Вкладка «Ещё» получила `MoreScreen` — честную заглушку вместо пустого
  экрана. Дизайна раздела в Figma по-прежнему нет.
