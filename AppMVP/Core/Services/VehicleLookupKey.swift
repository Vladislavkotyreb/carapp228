import Foundation

/// Токен поиска авто по госномеру: сервис api-cloud.ru, метод `rsa.php` —
/// база полисов ОСАГО (НСИС/РСА). Выдаётся в кабинете https://api-cloud.ru
/// после регистрации; запросы платные, порядка рубля за штуку.
///
/// Решение «ключ поставщика — только на сервере» из DECISIONS.md здесь
/// сознательно нарушено по прямой просьбе пользователя: своего сервера нет,
/// а поиск нужен сейчас. Риск прежний — токен вынимается из `.ipa`, и за
/// него могут списать баланс; с появлением сервера запрос обязан переехать
/// туда (docs/BACKEND.md).
///
/// Пока строка пустая, работает заглушка с данными из макета — так же, как
/// раздел «Карта» живёт без ключа MapKit.
enum VehicleLookupKey {
    static let token = ""

    static var isConfigured: Bool { !token.isEmpty }
}
